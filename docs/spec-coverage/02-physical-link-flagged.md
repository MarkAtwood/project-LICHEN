# spec/02-physical-link.md — flagged set for Opus verification (sweep 2026-09-09)

Scope: every requirement classified **divergent** or with **low confidence** in
`02-physical-link.md`. (No ambiguous rows this sweep; section is not 06-security,
though §4.1/§4.2 evidence intersects link-signature semantics — rows touching the
canonical transcript are included with domain-separation questions.)

## Prior-log reconciliation

The surviving `02-physical-link-verify.log` verified a prior matrix (lost) with
its own numbering. Verdicts that CHANGED since that pass:

| Prior ID (old log) | Prior verdict | Current state |
|---|---|---|
| R-02-006 (sync word) | divergent (C programs 0x12) | C FIXED (lr1110.c:502-504 programs 0x34 unconditionally); divergence moved to Rust (bead b7z9.103) |
| R-02-011 (channel formula) | divergent spec text (`hash(src^dst)`) | spec line 42 now carries the 02a FNV-1a32 formula; residual issue is vector-consumer asymmetry (b7z9.106) |
| R-02-016 (ASSIGNED_SF) | divergent (emission missing) | C emit landed (rpl_root.c:212-228) but advertises a never-set field; Rust emit dormant + consume absent (b7z9.104) |
| R-02-029 (density>10) | divergent residual sites | gradient.c and adaptive_sf.py since FIXED (:325/:130); residues now only dead/stale code (link_load_balance.*, gradient.h:195, link.h:650, rf_health.rs:384 doc, ccp11.json/ccp16.json descriptions) — b7z9.106 |
| R-02-046 (legacy transcript reject) | upgraded to implemented+tested | CONFIRMED (C schnorr48_interop reject_noncanonical_transcripts, Rust schnorr.rs:655-699, Python test_protocol_vector_security) |
| R-02-010 (N_CHANNELS) | divergent (Rust+C hardcode) | Rust now parameterized (no hardcode found); C Kconfig decoupled from plan (b7z9.111) |
| R-02-026/027 (TX_SF, RF metrics) | not-implemented, zero hits | TX_SF codec landed all three (announce.rs:433-467 / coords.py / announce.c:1119-1135); emission still absent; C RF-metrics option 0x16 codec+unit test landed, never emitted/parsed in production (b7z9.74 description now stale) |
| R-02-057→R-02-063 (trust bound/evict) | divergent (C persist -ENOSPC) | Confirmed unchanged at HEAD (b7z9.109) |
| R-02-064→R-02-071 (epoch init 128) | divergent (EpochStore) | Confirmed unchanged at HEAD (b7z9.75) |
| R-02-072→R-02-080 (C replay binding) | divergent (no bitmap/gen-id) | Confirmed unchanged at HEAD (b7z9.76) |
| R-02-079/080→R-02-086/087 (C DAD) | divergent (no loop/consumer) | Confirmed unchanged at HEAD (b7z9.77) |
| R-02-012 (rendezvous TX) | ambiguous | Upgraded to concrete divergence: pinned rx_channel never read for unicast TX in Python or C (b7z9.107) |
| R-02-009 (CH0 idle listen) | ambiguous | Upgraded: C radio never returns to CH0 after data-channel TX; Python drops channel field (b7z9.108) |
| R-02-035 (§3.6 SFN) | not-implemented | Confirmed: time-sync substrate only; no SFN-delta/combining anywhere; still untracked by any bead (overflow note) |
| R-02-013 (gateway channel DIO) | not-implemented | Confirmed; superseded by CCP-9 — spec simplification candidate |
| R-02-036/037/040/041 old (LR-FHSS) | divergent (Python-only) | Confirmed (b7z9.78); new detail: lr_fhss.json cites AN1200.64 not AN1200.62; C Kconfig knobs unconsumed |

Old→new mapping for the remaining prior IDs (best-effort, old→new):
001→001, 009→009, 010→010, 011→011, 012→012, 013→013, 014→014, 015→020,
016→016, 029→015/029, 031→031, 035→036, 036→037, 037→038, 040→041, 041→042,
046→048, 057→063, 064→071, 068→077, 072→080, 079/080→086/087.

## Flagged requirements

| Req | Spec text (trimmed) | Classification | Evidence (summary) | Question for Opus |
|-----|---------------------|----------------|--------------------|-------------------|
| R-02-018 | §3.4 join-based: join on SF10; gateway assigns via DIO **or join response**; node switches post-assignment | implemented+untested (low conf) | joined-gate model all three (sf_assignment.rs:69-79; sf_assignment.h:40-53; sf_assignment.py:56-68); C switches post-DIO; `rg join_response` = zero hits anywhere | Is "join response" a real protocol element that should exist, or stale wording superseded by DIO assignment? Spec simplification vs implementation gap? |
| R-02-049 | §4.1: Rule Set v3 uses Link Signature Domain v1; mixed activation = network upgrade; MUST NOT accept both transcripts | implemented+tested (vacuous; low conf) | Domain v1 hardcoded everywhere; no dual-accept window; no upgrade machinery. Rule-version constants tested everywhere | Is vacuous enforcement (single fixed domain, no version field) conformant, or does the spec require an explicit activation/negotiation mechanism? Does this row belong in a 06-security sweep instead? |
| R-02-074 | §4.4 persistence SHOULDs: write every increment; wear-leveling; read failure → random fallback | implemented+tested (low conf, literal divergence) | C epoch_persist.c conforms except read-failure → -EBADMSG aborts init (lichen_l2_init.c:103-107) instead of random fallback | Is C's fail-closed-on-read-failure the intended reading (spec's "SHOULD" bullet vs fail-closed principle elsewhere), or should spec text be amended? |
| R-02-001 | §3.1 SX126x and SX127x | divergent | SX127x = airtime model + Kconfig delegating to Zephyr upstream + Renode model; no LICHEN driver (b7z9.112) | Does delegating SX127x to the Zephyr in-tree driver satisfy the MUST, or is a LICHEN-authored driver required? Spec wording amendment candidate |
| R-02-006 | §3.2 sync word 0x34 | divergent | C fixed (lr1110.c:502-504); Rust LORA_SYNC_WORD zero consumers; no test pins the value (b7z9.103) | Confirm lora-phy's SX1262 default sync word is not already 0x34 (i.e. that the Rust gap is real on hardware), and whether a host-side register-level test is feasible |
| R-02-009 | §3.3 all nodes MUST listen when idle (CH0) | divergent (latent) | C post-TX RX re-arm never restores CH0 freq; Python drops tx_queue channel (b7z9.108) | Confirm the latent-vs-live reading: is any current C caller able to pass channel>0, or is data-channel TX fully unwired today (making b7z9.108 + b7z9.107 one workstream)? |
| R-02-010 | §3.3 N_CHANNELS from regional config | divergent (C) | C Kconfig default 8, range 1-16, decoupled from op-class table; US915 inexpressible (b7z9.111) | Should C N_CHANNELS derive from the op-class table (needs a channel-count field) or stay build-time with a documented limit? |
| R-02-011 | §3.3 hash SelectChannel; all impls MUST match 3 pinned vector files exactly | divergent (coverage) | Formula identical in 3 stacks; endianness/load_balancing vectors Python-only; Rust uses ccp15.json instead of ccp16.json; C consumes SF cases only; dead divergent C lichen_lb_select_channel; python link/channel.py 16B preimage variant; selection unwired from TX (b7z9.106) | Should spec 3.3 name the per-stack vector sets actually consumed, or should Rust/C gain consumers? Is the python link/channel.py 16-byte preimage a bug or a separate CCP-12 chain stage? |
| R-02-012 | §3.3 rendezvous rx_channel (TOFU pinning) | divergent | Announce codec all three; pinned channel never used for unicast TX (python node.py/_peer_rx_channel; C gradient entry->rx_channel write-only) (b7z9.107) | Confirm whether rendezvous TX belongs at LinkLayer.send (channel param) or scheduler/queue level, and how it interacts with CCP-9 slot timing |
| R-02-013 | §3.3 gateway-assigned channel DIO (MRHOF variant) | not-implemented | No channel-carrying DIO option in any registry | Drop from spec 3.3 (superseded by CCP-9/ASSIGNED_SF) or keep as future MRHOF extension? |
| R-02-014 | §3.3 regional params: US915 64 uplink + 8 downlink | divergent (spec text) | No stack represents US915 downlink; Python has 64 uplink only; C/Rust synthesize channels arithmetically | Confirm amendment to "US915: 64 channels" (prior Opus recommendation) — uplink/downlink asymmetry is LoRaWAN vocabulary, not mesh |
| R-02-016 | §3.4 ASSIGNED_SF end-to-end | divergent (per stack) | C emits constant SF10 (never-set dodag.assigned_sf); Rust emit dormant + consume absent; trackers production-dead; no e2e test (b7z9.104) | Is least-loaded assignment intended for the C gateway root DIO at all (single-root DODAG), or only multi-gateway/CCP deployments? Determines whether C needs a real tracker or just config-driven SF |
| R-02-023 | §3.4 sf_assignment.json oracle | divergent (consumers) | Rust functional; C hardcoded rows; Python none (6g4z open) | Is C's hardcoded-row consumption acceptable as a "behavioral oracle", or must C parse the JSON like Rust? |
| R-02-026 | §3.5 TX_SF in DIO options AND Announce | divergent (codec only) | Announce TLV codec landed 3 stacks, zero emitters; no DIO TX_SF option type exists (b7z9.74) | Does CCP-16 require BOTH the Announce TLV and a DIO option, or is Announce sufficient with DIO carrying ASSIGNED_SF only? Spec reads as both |
| R-02-027 | §3.5 RF metrics (EMA SNR α=1/4, loss) signaled in DIO | divergent (partial) | C 0x16 RF-metrics codec+test landed, never emitted/parsed in production; Rust/Python absent (b7z9.74) | Confirm the C [0x16, 4, snr, loss, dens, util] wire shape against 02a 2a.10 before wiring; is the provisional 0x16 acceptable alongside Rust's DODAG-version-auth use of 0x16? |
| R-02-028 | §3.5 per-neighbor EMA/loss/sample state | divergent (partial) | C per-neighbor SNR EMA + counts; loss EMA node-level from TX counters; Rust node-level only; Python model only (b7z9.29, b7z9.74) | Is node-level loss-rate an acceptable reading of "per-neighbor state MUST track … loss rate", or is per-neighbor loss EMA required? |
| R-02-030 | §3.5 upgrade hysteresis (decision sf-upgrade-hysteresis) | divergent (Rust) | Rust rf_health.rs:668-670 upgrades unconditionally; C conformant (upgrade_count≥3); python gradient.py has it, adaptive_sf.py doesn't (b7z9.105) | Confirm the decision's scope: does "Rust rf_health.rs must match" extend to python adaptive_sf.py/ccp.py step 4 (decision text names only Rust)? |
| R-02-031 | §3.5 utilization = TX-time occupancy of total detected airtime | divergent (own-TX only) | All three record own-node TX airtime only; util_norm=/255 in vectors only; Rust percent-vs-/255 unit divergence (b7z9.29) | Is "total detected airtime" intended to include overheard/other-node airtime (needs RX-path accounting), or is own-node occupancy the intent? Spec wording tighten recommended (prior Opus agree) |
| R-02-034 | §3.5 pinned ccp16/ccp15/ccp_load_balancing cases | divergent (corpus) | Spec-cited exact cases (12.5 / density=12 / density=255) absent from vectors; ccp16.json stale descriptions + desc/output mismatch (b7z9.106) | Should spec text quote exact vector names/cases (regenerate corpus first), or should the sentence defer to the vector files' current categories? |
| R-02-036 | §3.6 SFN coordinated transmission | divergent (substrate only) | Time-sync substrate (DIO Time, GNSS/PPS, desync FSM) all three; no propagation-delay/delta/combining code; python sfn_delta helper validation beads (2hcq, xemz, qefi) | Should §3.6 be marked future-work in spec (prior Opus recommended), or is coordinated transmission on the implementation roadmap (needs an epic)? Overflow — no bead filed |
| R-02-037 | §3.7 LR-FHSS optional SX1262-only | divergent (partial) | C Kconfig knobs unconsumed; Python PhyMode; Rust none (b7z9.78) | Confirm Kconfig-only knobs should be removed or wired; does "works on ALL hardware" §3.4 claim vs SX1262-only §3.7 need reconciliation? |
| R-02-038 | §3.7 LR_FHSS_SUPPORTED DIO reserved bit | divergent (Python oracle) | Python bit codec + vectors; not wired to real DIO builder in any stack (b7z9.78) | Which DIO flag bit is "reserved" for LR_FHSS_SUPPORTED? No registry entry exists — needs an assignment decision before wiring |
| R-02-039 | §3.7 LR_FHSS_CAPABLE in Announce app_data | divergent (Python oracle) | Python bit 0 of app_data + vectors; C/Rust absent (b7z9.78) | Confirm app_data TLV/bit numbering matches the Announce TLV registry (python coords.py 0x06 TX_SF vs LR-FHSS bit placement) before cross-stack wiring |
| R-02-042 | §3.7 downlink matches node's most recent uplink | divergent (Python model) | python lr_fhss.py:96-98 + vectors; no C/Rust (b7z9.78) | Low value to verify further until a real LR-FHSS driver exists — confirm deferral posture |
| R-02-043 | §3.7 node-to-node LR-FHSS negotiation | divergent (Python model) | python lr_fhss.py:101-110 + vectors; no C/Rust (b7z9.78) | Same as above |
| R-02-044 | §3.7 LR-FHSS parameters | divergent (partial) | C OCW/CR Kconfig unconsumed; DR8-DR11 absent everywhere; vectors cite AN1200.64 not AN1200.62 (b7z9.78) | Is the AN1200.62 (hopping) vs AN1200.64 (airtime/sensitivity) citation split intentional? Spec says 62 for hopping sequence |
| R-02-050 | §4.1 dispatch 0x14/0x15/0x16 | divergent (Rust 0x16) | Rust classifies [0x16]→Unknown (l2_payload.rs:83); C/python classify all three (b7z9.110) | Confirm Rust should gain the SOS arm now, or wait until the SOS budget wiring (R-02-051) lands to avoid a dead dispatch arm |
| R-02-051 | §4.1 SOS budget classification + §18.4.1 origin verification before relaying | divergent (wiring) | Origin sig implemented+tested 3 stacks; C link RX admits SCHC only (0x16 unreachable); Python budget limiter unwired; Rust no 0x16 (b7z9.110) | Confirm the intended RX pipeline: is 0x16 classified at lichen_link_rx (C) alongside SCHC, or only at the app/relay layer? Interop with §6.3.3 budget accounting must not double-count relayed vs origin frames |
| R-02-063 | §4.2 trust store ≤256 + MUST LRU-evict on overflow | divergent (C persist) | C persist-mode returns -ENOSPC instead of evicting (lichen_l2_peer.c:341-347); all bounds <256; literal ceiling untested (b7z9.109) | Needs mark's call: implementation change (add LRU eviction in persist mode) or adjudicated spec carve-out for trust-lineage protection (prior Opus lean carve-out) |
| R-02-071 | §4.4 epoch random init [128,255] | divergent (EpochStore) | epoch_store.py:48-58 fixed 128, pinned by test_cold_boot_starts_at_128 (b7z9.75) | The pinning test must be updated with the fix (not weakened) — confirm EpochStore should adopt the LinkLayer/C semantics (random + persist-on-boot) |
| R-02-076 | §4.4 receiver state includes KeyGeneration | divergent (struct) | No stack stores generation inside the replay window struct; Rust binds it on TrackedPeer + durable records; C has no generation concept in replay.c at all (b7z9.76 covers the durable part) | Is pubkey-only in-memory keying + immediate retirement hooks an accepted equivalent of the (pubkey, generation) key, or must the window struct carry the generation? |
| R-02-077 | §4.4 replay key = (pubkey, generation); retirement immediately unusable | divergent (equivalent-or-stronger) | Pubkey-only keying + retire-and-replace semantics; Rust additionally makes in-flight frames invalid post-retirement (is_valid :332-335); C retires by removal only | Prior pass called this "may be accepted equivalent". Confirm equivalence reading (explicit reset on rotation vs compound key) or require compound keying in a future revision |
| R-02-080 | §4.4 persisted replay binding (bitmap + gen id + integrity) | divergent (C) | C floor lacks bitmap + generation id; conservative UINT32_MAX restore; BLAKE2b integrity + CAS present (b7z9.76) | Confirm conservative-restore (fail-closed) satisfies the MUST's intent or whether persisted bitmap is genuinely required (spec says "bind … window bitmap") |
| R-02-086 | §4.5 dad_retry seeds 1..=255 → None | divergent (C; Rust fallback) | C has primitives, no loop/consumer; Rust candidate() adds incremental pool fallback (decided in bd 1.8.2.6) instead of None (b7z9.77) | Two questions: (a) confirm the incremental-pool fallback remains adjudicated (spec says None); (b) does the C DAD retry loop belong in lichen (rpl short-address assignment) — where does C assign short addresses today? |
| R-02-087 | §4.5 implementations MUST match short_addr_dad.json | divergent (C consumer) | Rust include_str! consumer (short_addr_vectors.rs:10,35,61,111,137) + dad_hash_clarification.json (:12); Python consumers (test_vectors.py:4039-4214); C rg short_addr_dad/dad_hash across lichen/ = ZERO hits (b7z9.77) | Confirm C must consume the JSON (like Rust) vs behavioral hardcoding (same question as R-02-023); fold into b7z9.77 or split |
| R-02-023-adj | python link/channel.py 16-byte hash preimage vs 12-byte spec/02a | divergent (variant) | channel.py:141-154 (eui+epoch+sfn) vs channel_plan.py:282-301 + link_ctx.c:701-735 + rf_health.rs:385-395 (eui+epoch, 12B); ccp16-hop.json vectors follow the 16-byte form for synchronized_hop (per its CCP-12 pseudocode) | Are the 12-byte (§3.3 select_channel) and 16-byte (CCP-12 synchronized_hop, epoch+sfn) preimages intentionally different algorithms, or is one wrong? Spec 3.3 pins 12-byte; ccp16-hop.json pins 16-byte for the hop channel |
| R-02-046-adj | Two parallel frame vector corpora: test/vectors/link_frame.json (canonical, 3-stack consumed) vs rust frame.rs:802 pointer to spec/test-vectors/frame.json | note (not a requirement row) | frame.rs:802 doc reference; shared_vectors.rs consumes the test/ tree | Confirm spec/test-vectors/frame.json is stale/should be redirected to test/vectors/link_frame.json (relates to stale-native-corpus inventory bead worker6-1p2r) |

Count: 37 flagged table rows — 35 requirement rows (3 low-confidence + 32
divergent/not-implemented requiring decisions) + 2 adjacent notes. None are
oscore/EDHOC-internal edits; all questions are protocol/conformance-level.
