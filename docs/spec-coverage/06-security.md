## spec/06-security.md — coverage (sweep 2026-09-09)

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-06-001 | Link-layer signatures REQUIRED on all RPL control frames (DIO/DAO/DIS); RPL secure mode optional (§8.1) | implemented+tested | Unsigned frames rejected at link RX in all stacks: rust/lichen-link/src/link_layer.rs:1133-1142, C lichen/subsys/lichen/link/lichen_link_rx.c:52-78, python/src/lichen/link/link_layer.py:1603-1607. Tests: python/tests/rpl/test_rpl_control_link_signatures.py:249, lichen/tests/l2_key_selection/main.c:534, rust relay_re_sign.rs | high |
| R-06-002 | Security layers: LLSec = Schnorr signature (48B); AES-128-CCM optional (§8.2) | implemented+tested | Schnorr48 in all stacks (schnorr.rs:18-28, schnorr48.c, schnorr48.py). Optional link AES-CCM: C only — lichen/subsys/lichen/link/Kconfig:74 ("Link-key AES-CCM MIC/encryption"), lichen/subsys/lichen/l2/lichen_l2.h:144 (shared AES-CCM link key); C link layer rejects E=1 frames fail-closed (link CMakeLists note) | high |
| R-06-344 | Eavesdropping mitigated by OSCORE (CoAP) and DTLS 1.3 (MQTT-SN) (§8.1 table) | divergent | OSCORE implemented+tested (R-06-201). DTLS: not-implemented — only port constants exist (python/src/lichen/constants.py:27 PORT_COAP_DTLS=5684; rust/lichen-coap/src/lib.rs:5 doc); zero DTLS handshake/records code. DTLS is primarily 07-transport scope; mitigation mapping here is aspirational | high |
| R-06-345 | DoS mitigated by rate limiting, admission control (§8.1 table) | divergent | Partial: link RX destination admission deliberately after auth (rust link_layer.rs:1187-1199); C SOS limiter lichen/subsys/lichen/link/sos_ratelimit.c (§18.4.3); python broadcast limiter standalone (R-06-347). RPL DIO/DAO 30/min rate limit absent → existing bead project-LICHEN-worker6-b7z9.1; EDHOC limits divergent (R-06-213) | high |
| R-06-346 | §8.1 broadcast-budget ceiling note: limiter keys on unauthenticated inner source field (acknowledged, not closed here) | divergent | Limiter itself (04-network §6.3.3): python only, standalone — python/src/lichen/link/broadcast_limit.py:96-121 `admit(sender_iid, hop_limit)` keyed on caller-supplied string, never bound to an authenticated identity (consistent with the acknowledged ceiling); vectors test/vectors/broadcast_rate_limiting.json (11 cases, python/tests/link/test_broadcast_limit_vectors.py). Rust/C: not found; python limiter not wired into any RX relay path. Naming drift: vector/param say `sender_iid` while post-migration spec keys the /128 | high |
| R-06-003 | Every originated frame carries a Schnorr signature for sender authentication (§8.3) | implemented+tested | TX signs every frame: rust schnorr.rs:56-85 via link_layer.rs:1026-1066; C lichen_l2_tx.c:108-150; python link_layer.py TX path :1796-1890. Oracles: test/vectors/schnorr48.json (16 vectors) consumed by all three stacks (schnorr48_vectors.rs, lichen/tests/schnorr48/main.c:334, python/tests/crypto/test_schnorr48.py:32) | high |
| R-06-004 | Deterministic nonce (RFC 6979) REQUIRED (§8.3) | implemented+tested | r = H(privkey‖msg) mod L — exactly the spec's own pseudocode (:65): python schnorr48.py:132 (`_hash_to_scalar(privkey + msg)`), C schnorr48.c:80-91 ("Deterministic nonce: r = SHA-512(privkey ‖ msg) mod L"), Rust via external `schnorr48` crate (Cargo.lock:3266). Determinism tests: test_schnorr48.py:82, rust schnorr48_vectors.rs:144. **Spec-text mislabel**: RFC 6979 is HMAC-DRBG; the parenthetical is wrong (→ bead, spec-text corrections) | high |
| R-06-005 | Signature 48B = e[0:16]‖s[0:32]; e = SHA-512(R‖pk‖msg); verify R' = s·B − e·pk, valid = e'[0:16]==e; hash SHA-512 (§8.3) | implemented+tested | python schnorr48.py:137-147 (truncate e 16, e‖s out), :150-202 (R' recovery, low-order table :34-53, zero-challenge :181, canonical-s :186); C schnorr48.c:99-122, :183-201, :204-213; Rust schnorr.rs:94-122. Tests: invalid_wrong_message/tampered_*/all_zeros (schnorr.rs:285-332), invalid_low_order/non_canonical_s (schnorr.rs:339-389), C test_verify_invalid_signatures | high |
| R-06-006 | Receiver MUST verify signature and update authenticated peer's link replay window before processing payload (§8.4) | implemented+tested | Rust link_layer.rs:1128-1240 order enforced: verify :1172-1185 → replay check_and_update :1214-1225 → payload released. C lichen_link_rx.c: authenticate :134-145 → commit_replay :281 → copy after :345-350 ("prevents authenticated-but-replayed data being visible"). Python documented order :1561-1567, replay two-phase check/commit :87-129. Tests: lichen/tests/link_crypto (replay-commit-matches-ipv6-rx :327), link_layer.rs replay_rejected :1575, test_link_rx.py test_receive_rejects_replay :122 | high |
| R-06-007 | Forwarding node: permitted mutations, new frame for next hop, own replay counter, signs complete new frame (§8.4) | implemented+tested | rust/lichen-link/src/relay.rs:1-10 module doc + verify-before-mutation; C lichen_link_tx.c:376-448 (own counter :329, re-sign :336-343); python test_relay_resigning.py. Tests: relay_re_sign.rs — `relayed_wire_matches_independent_python_reference` :101, `next_hop_accepts_relayed_frame_and_relay_counter_is_relays_own` :130, `relay_never_forwards_inbound_signature` :152 | high |
| R-06-008 | DAO relays MUST preserve source and DAO bytes; only Hop Limit + enclosing link frame/signature change (§8.4) | implemented+tested | DAO origin transcript over exact unsigned DAO bytes: rust/lichen-rpl/src/verify.rs:128-142 (domain "LICHEN-DAO-ORIGIN-v1"), dao_origin.rs:148 compute_dao_digest; preserved source threaded receive.rs:558-588. Tests: dao_origin_vectors.rs:144 (transcript-sensitive), test/vectors/authenticated_schc_dio.json | high |
| R-06-009 | Root MUST validate in order: structural → pinned key/IID/transcript/origin-sig → per-key replay → semantic → self-/128 Target → replay-floor persistence → atomic mutation (§8.4) | divergent | Rust conforms: rust/lichen-rpl/src/routing.rs:612-725 (replay :627-640 before semantic :646-655; floor committed step-7 dao_origin.rs:289). C validates semantic/Target before replay classification (rpl_dao_process.c:841-846 vs :862-898) — both reject; precedence differs. → existing bead project-LICHEN-worker6-b7z9.81 | high |
| R-06-010 | Missing, corrupt, or unavailable replay persistence fails closed (§8.4) | divergent | Rust conforms+tested (routing.rs:701-723; dao_origin_vectors.rs:264 `unavailable_replay_storage_leaves_dao_state_unchanged`). C RX-side origin-seq floor volatile (rpl_dao_process.c:862-898); TX floor persisted (rpl_dao_build.c:155-165) → existing bead project-LICHEN-worker6-b7z9.11 | high |
| R-06-011 | Root accepts exactly one /128 Target equal to preserved Source; delegation is future .44.9 (§8.4) | divergent | Implemented generalized profile (own-/128 OR exact delegation): rust routing.rs:771-823; C dao_targets_authorized. Reconciliation tracked by existing bead project-LICHEN-worker6-b7z9.9. Python enforces single-distinct-Target (dao_manager.py:943); Rust has no cardinality check | high |
| R-06-012 | Relay-mutable fields (Hop Limit, 6LoRH SRH, link dst/src) never change under an unchanged link signature (§8.4) | implemented+tested | Signed transcript = LENGTH‖LLSec‖epoch‖seq‖DST‖SIID‖payload (rust schnorr.rs:124-149; C schnorr48.c:370-397); mutation strictly between inbound verify and outbound re-sign (relay.rs:69-72). Tests: relay_re_sign.rs:375, :416 (tamper-after-sign detected downstream) | high |
| R-06-013 | All node identity derives from a single Ed25519 keypair; no separate keys or ULA (§8.5) | implemented+tested | Identity::from_seed identity.rs:69-79; python identity.py:59-80; C identity_addr.c. No ULA construction in addr code. Tests: ipv6_address_vectors.rs, test_identity.py, pubkey_to_iid | high |
| R-06-014 | IID = SHA-512(pubkey)[0:8], iid[0] &= 0b1111_1101; MUST be SHA-512 not SHA-256; local identifier only (§8.5 step 2) | implemented+tested | rust/lichen-core/src/addr.rs:83-89 (`iid_from_pubkey_bytes`, U/L clear); python identity.py:138-154; C identity_addr.c:29-53 (mask `~0x02`, forbids SHA-256 fallback). Tests: `iid_remains_sha512_derived_not_address_low_half` (yggdrasil_addr_vectors.rs:225), `test_iid_has_local_bit_clear` (test_identity.py:270), pubkey_to_iid/main.c. **Exception**: bringup firmware rust/lichen-firmware/wio-e5/src/main.rs:33-39 uses raw seed[0:8] → existing bead project-LICHEN-worker6-b7z9.82 | high |
| R-06-347 | Routable /64s, when used, MUST equal upstream SubnetForKey(pubkey) in 0300::/8 (§8.5 step 3 / §8.7 step 3) | divergent | Rust `subnet_for_key` addr.rs:170-176 (+anchor test `upstream_subnet_anchor_matches_byte_exact` yggdrasil_addr_vectors.rs:153-168); python identity.py:270-281 (+test_yggdrasil_address_vectors.py:141,174-175). C: not found (no 0300::/8 construction; C nodes do not currently use routed /64s — conditional MUST "when used" vacuously met) | high |
| R-06-015 | Routable /128 = upstream Yggdrasil AddrForKey(pubkey) exactly: bit-invert 32B key, addr[0]=0x02, addr[1]=leading-ones count, pack MSB-first, truncate 14B; no hashing; byte-for-byte for every input (§8.5 step 3, post-upstream decision) | implemented+tested | rust addr.rs:119-162 (`ygg_addr_from_pubkey`); python identity.py:209-267; C lichen_identity_ygg_addr_from_ed25519 identity_addr.c:55-117 (+ l2/ipv6_addr.c:317-333). Byte-equality oracles: test/vectors/yggdrasil_address.json (upstream anchor `upstream_addr_for_key` from yggdrasil-go address_test.go @422836ee) — rust/lichen-core/tests/yggdrasil_addr_vectors.rs:134 `upstream_anchor_matches_byte_exact`, python/tests/crypto/test_yggdrasil_address_vectors.py:126-166 (`GO_ANCHOR` verbatim), C pubkey_to_iid/main.c:87-97. UPDATE vs part-1 sweep: spec text and code have both migrated to the upstream profile since the part-1 sweep; legacy yggdrasil-derivation.json is quarantine-pinned only (rust yggdrasil_addr_vectors.rs:44-45 count-only; python test_vectors.py:2403 quarantine pin). Quarantine hygiene: legacy file's `ygg_addr` fields were regenerated to upstream values, and stale "rejected profile" comments remain in lichen/tests/pubkey_to_iid/main.c:42-49 and python test_vectors.py:2413-2422 docstring (cosmetic; noted in flagged file) | high |
| R-06-016 | X25519 priv = clamp(SHA-512(seed)[0:32]) for OSCORE/EDHOC (§8.5 step 4 / §8.7 step 4) | divergent | Python identity.py:97-110 + test/vectors/x25519.json (test_vectors.py:2335); C edhoc_crypto.c:287-307 (RFC 7748 clamp) + lichen/tests/schnorr48/main.c:300. Rust: static seed→X25519 not found (EDHOC ephemeral-only in oscore-fork) → existing bead project-LICHEN-worker6-b7z9.38 (human-only, EDHOC semantics). x25519.json has no Rust consumer | low |
| R-06-017 | TOFU pins pubkey to derived IID and AddrForKey address (cryptographically enforced) (§8.5 step 5) | implemented+tested | rust gateway trust.rs:926-967 verify_tofu (PinAndAccept/AcceptKnown/KeyConflict) + verify_iid_binding :530-549; python trust.py:399-470 verify_or_pin + verify_pubkey_to_ygg_addr :246-260 (0200:: check); C coap_keys_store.c:232-305 + gcp_trust.c:81-93 (ygg derivation). Vectors: tofu_edge_cases.json (14) consumed by all 3 stacks | high |
| R-06-018 | fe80::/10 control only; 0200::/8 primary for all routable traffic, isolated meshes and backhauls; no translation (§8.5) | implemented+untested | Scope checks: C ipv6_addr.c:104-113, router.c:42-46; python node.py:570/604; rust addr.rs link_local vs ygg_addr. No dedicated control/routable split test | low |
| R-06-019 | Derivation MUST match pinned upstream test vectors exactly; yggdrasil_address.json is the conformance oracle; SHA-512 native profile rejected and quarantined (§8.5/§8.7, per decision upstream-yggdrasil-addressing) | implemented+tested | Oracles: test/vectors/yggdrasil_address.json (upstream corpus + Go anchor); legacy/yggdrasil-derivation.json quarantine-pinned (rust ipv6_address_vectors.rs:270-300 compares positives against hardcoded EXTERNAL upstream table, not legacy bytes). No test drives the rejected profile through an implementation as conformance. Spec self-refs still stale (identity.rs:14-48 / `yggdrasil_addr_from_pubkey` symbol) — existing bead project-LICHEN-9k53 | high |
| R-06-020 | §8.6 caching: verify at first hop, cache "verified from <IID>", relays skip re-verify, keyed (source IID, seq) | divergent | Not implemented in any stack — all stacks verify every frame at every receive and relays re-verify before mutation (stronger): rust link_layer.rs:1172-1185 + relay.rs:1-10; python link_layer.py:1618-1688; C lichen_link_rx.c:134-145. No (IID,seq) verification cache exists. §8.6 carries no RFC-2119 keyword → no bead; flagged for spec-editor (per-hop verify is the de-facto baseline; spec should bless it) | high |
| R-06-021 | Cache entries expire after 2× mesh traversal (default 30 s) (§8.6) | not-implemented | Contingent on the R-06-020 cache, which does not exist. Near-miss python link/receipts.py:34 VERIFIED_RECEIPT_TTL_SECONDS=60 is an RxFrame-facade TTL, not a signature cache | low |
| R-06-022 | High-security deployments MAY enable per-hop verification (§8.6 note) | implemented+tested | Per-hop verification is the universal behavior (R-06-020 evidence); relay tamper tests relay_re_sign.rs:216, :416 | high |
| R-06-023 | Single 32-byte seed produces all material (Schnorr48, X25519, IID, 0200::/8); MUST match pinned vectors exactly (§8.7) | implemented+tested | Keypair h=SHA-512(seed), clamp, basepoint: python schnorr48.py:96-106, C Monocypher path, Rust schnorr48 crate; oracles schnorr48.json + yggdrasil_address.json (R-06-015); X25519 gap = R-06-016 | high |
| R-06-024 | Self-provisioned (RECOMMENDED) or BR-provisioned nodes derive identically (§8.7) | implemented+tested | Same derivation both paths: rust gateway provision_configured_peer trust.rs:673-714 (IID collision fails closed :674-680); python provisioning.py decrypt_ack verifies against Identity.from_seed; tests test_provisioning.py:51 | high |
| R-06-025 | All nodes generate/provision a single Ed25519 keypair at first boot (§8.7) | implemented+tested | from_seed paths (R-06-023); C lichen_link_load_key; tests key_generation.rs, test_identity.py:100 | high |
| R-06-026 | Private key stored securely, never transmitted (§8.7; §15.2 as amended) | implemented+untested | No TX path carries a privkey (negative property); storage hardened per R-06-047/048/341 | low |
| R-06-027 | BR-provisioned node derives IID/02xx from provisioned key; BR does not assign IID (§8.7) | implemented+tested | python provisioning.py:595-615 (node derives after decrypt_seed); ack verifies derived pubkey MANDATORY; rust provisioning.rs:817-836. Tests: test_provisioning.py:51, :429, :445 | high |
| R-06-028 | Provisioning channel MUST be encrypted and authenticated (§8.7) | implemented+tested | python provisioning.py:174-216 EDHOC-derived key + AES-CCM seed transfer (AAD "LICHEN-PROVISION-v1"); rust mirror provisioning.rs:711-870. Tests: test_provisioning.py:294 (wrong key), :317 (tampered), :717 (generic auth errors). C: enum only, no flow | high |
| R-06-029 | Private key deleted from BR immediately after transfer (§8.7) | implemented+tested | python BRProvisioningSession.wipe() :491-505; tests test_br_wipe_clears_sensitive_data :1088, wipe-resets-state :1133. Caller-enforced ("immediately" is API contract) | high |
| R-06-030 | Node rejects further provisioning (factory reset to redo) (§8.7) | not-implemented | Persistent lock-out flag absent in all stacks (per-session state machine only) → existing bead project-LICHEN-worker6-b7z9.34 | low |
| R-06-031 | All derived addresses (02xx, IID) MUST match key (§8.7) | implemented+tested | Same binding checks as R-06-017 (verify_iid_binding trust.rs:530-549; C -EKEYREJECTED coap_keys.h:218; python TrustEntry.__post_init__ :144-147); tofu_edge_cases.json `derivation_mismatch` | high |
| R-06-032 | Implementations MUST support TOFU; DANE/PKIX optional upgrades (§8.7) | implemented+tested | TOFU default trust path in all stacks (R-06-017/033); tofu_edge_cases.json corpus × 3 stacks | high |
| R-06-033 | TOFU: accept+pin on first contact; key must match derivation; key change/IID mismatch → reject AND alert (§8.7) | implemented+tested | Pin/reject trust.rs:926-967; RX mismatch rejected+logged: rust link_layer.rs warn + LinkRxError::KeyChange, python "KEY CHANGE DETECTED" :1641-1651, C mismatch audit struct + operator alert callback coap_keys.h:103-203, coap_keys_alert.c (domain "LICHEN-TOFU-OPERATOR-ALERT-v1"). Tests: tofu_edge_vectors, tofu_key_selection.rs, coap_keys_lifecycle | high |
| R-06-034 | Nodes trust BR anchors without TOFU; derivation ensures key matches observed IID (§8.7) | implemented+tested | BrProvisioned level bypasses TOFU as out-of-band anchor: rust trust.rs:672-713 + BrProvisioned=2; python add_trust_anchor trust.py:517; C GCP_TRUST_LEVEL_BR_PROVISIONED gcp_trust.c:258. Anchor-list distribution absent (→ R-06-035) | high |
| R-06-035 | BR revocation: remove from anchor list, push updated list, optional signed revocation broadcast (§8.7) | not-implemented | No anchor-list distribution/revocation-push mechanism anywhere → existing bead project-LICHEN-worker6-b7z9.80. Local revocation flag python-only (trust.py:709-729, tested) | low |
| R-06-036 | Nodes SHOULD fetch trust-anchor updates periodically (hourly) or on BR announcement (§8.7) | not-implemented | No periodic fetcher/announcement listener anywhere → grouped into existing bead project-LICHEN-worker6-b7z9.80 | low |
| R-06-037 | Mixed mode MAY: TOFU + BR-provisioned coexist (§8.7) | implemented+untested | Trust-level enums with ordering coexist (rust trust.rs:107-111 + ordering test :1691; C gcp_trust.h:77). Mixed-mesh interaction itself untested | high |
| R-06-038 | DANE optional: TLSA `_25519._mesh.<name>`, DNSSEC verify, out-of-band via BR (§8.7) | not-implemented | Enum plumbing only (DANE=3 in all stacks); no TLSA/DNSSEC code. Optional → no bead | high |
| R-06-039 | PKIX/ACME optional; /.well-known/cert; certificate chains MUST NOT be embedded in every packet (§8.7) | not-implemented | No ACME client, no /.well-known/cert. The MUST NOT is vacuously satisfied (no certificates anywhere). Optional → no bead | high |
| R-06-040 | Out-of-band verification MAY (fingerprint display, manual compare) (§8.7) | not-implemented | upgrade_trust hook exists (rust trust.rs:1054-1071); no fingerprint/QR flow. Optional → no bead | high |
| R-06-041 | Nodes SHOULD support key rotation announcements (§8.7) | divergent | §8.7 raw-transcript rotation in python (trust.py:604-707), rust gateway (trust.rs:972-1041), C gcp (gcp_trust.c:164-249); **rust link diverges** with `"KEY_ROTATE:"‖new_pubkey` (lichen-link/src/trust.rs:135,160-184) → existing bead project-LICHEN-worker6-b7z9.28. C has no rotation harness | high |
| R-06-042 | Rotation transcript `"LICHEN-KEY-ROTATION-v1"‖0x00‖old_pubkey(32)‖old_iid(8)‖new_pubkey(32)‖rotation_seq(8 BE)`; seq strictly increasing, >0, never wraps (§8.7) | implemented+tested | rust trust.rs:561-574 (exact layout; old IID derived internally :570); python trust.py:38 domain + :668-670 (transcript computed internally); C gcp_trust.c:164-200 (103-byte transcript, BE seq). Tests: trust.rs:1934-2059, test_trust.py | high |
| R-06-043 | Rotation signature from another protocol domain or different old IID/key/seq MUST be rejected (§8.7) | implemented+tested | Domain-separation confused-deputy tests: lichen-link trust.rs:592 `key_rotation_rejects_slot_claim_message`, :654 `slot_claim_rejects_key_rotation_message`; gateway derives old IID internally; python binds transcript internally | high |
| R-06-044 | Key change with valid domain-separated old-key signature → accept; fresh per-peer replay state; old counters MUST NOT constrain new key (§8.7) | implemented+tested | rust gateway process_key_rotation :972-1041 (fresh verify_count:1 entry); python link_layer.py:458-490 retires peer registries + resets replay (ReplayProtector._reset_owned); vector replay_window.json `key_rotation_starts_new_generation_and_retires_old` (test_vectors.py:3721) | high |
| R-06-045 | Key change without signature → reject, require re-verification (§8.7) | implemented+tested | rust KeyChange, python ReceiveError.KEY_CHANGE, gateway KeyConflict; tofu_key_selection.rs pins pin-only-after-verify | high |
| R-06-046 | Revocation: remove from local key store; no global revocation list (§8.7) | implemented+untested | Python only (trust.py:709-729, tested :372-411; revoked peers rejected during rotation :659-660). Rust/C: no revoked handling found | low |
| R-06-047 | Persisted trust entries MUST be treated as untrusted input on load: exact versioned schema + bounds, recompute IID + primary 0200::/8 from each pubkey, reject non-finite timestamps/invalid enums/counters, immutable detached entries (§8.7) | implemented+tested | Python key_persistence.py:738-910 (format_version 2, HMAC body, per-entry exact fields, recompute IID+0200 :819-823, non-finite reject :842-857, bool-reject :994, frozen dataclass + detached copies trust.py:397,773). Rust gateway trust.rs:734-839 (magic LCHNTRS1, sealed, verify_iid_binding :805, enum/timestamp/verify_count checks, rollback floor). C: load-callback auth contract + entry_binding_valid re-derives IID (coap_keys_store.c:84-94); no 0200:: field to recompute. Tests: test_key_persistence.py (13+ load-rejection tests), trust_store_rejects_corrupt_torn_and_wrongly_sealed_records (trust.rs:2322), coap_keys_lifecycle | high |
| R-06-048 | Trust/private-key stores MUST use owned private dirs, non-following opens, interprocess locks, unique mode-0600 temp files, atomic replacement, file+dir fsync (§8.7) | divergent | Python complete (key_persistence.py: O_NOFOLLOW :341/:680, flock :96-123, 0700 dir :77-93, mkstemp+0600 :305-311, os.replace :317, fsync file+dir :316-332). **Rust gateway**: O_NOFOLLOW ✓ (b7z9.79 closed), rename+fsync ✓ (trust.rs:855-866), but NO interprocess lock (no flock/fcntl anywhere in rust/), NO exact-revision CAS (monotonic floor instead), and temp files created without explicit `.mode(0o600)` (umask-dependent; trust.rs:847-851; lichend.rs seed staging does use 0600 :1392). **C**: in-process K_MUTEX only, no flock/CAS. → new bead | low |
| R-06-049 | Concurrent writers MUST use exact revision comparison; fail rather than silently overwrite newer trust state (§8.7) | implemented+tested | Python: stale-revision raise key_persistence.py:541-545 (tests :1076, :1100). Rust: monotonic generation + rollback floor (RollbackDetected on load trust.rs:782-788) argued equivalent-or-stronger by part-1 sweep; not literally an exact-revision CAS → flagged for Opus. C: monotonic revision + -ESTALE on floor failure (coap_keys_lifecycle tests) | high |
| R-06-050 | Key generations and rotation sequences MUST fail closed at integer maximum instead of wrapping (§8.7) | implemented+tested | rust bump_generation checked_add → GenerationExhausted (trust.rs:1107-1113); python revision exhaustion :546-547 + seed-generation max :455-456 + rotation seq bounds :647-651; C revision -EOVERFLOW coap_keys_store.c:104-106 | high |
| R-06-051 | §8.7.4 attestation = COSE_Sign1 [protected h'47A1013A00010000' ({1:-65537}), {4: old-IID kid}, payload {1:old_pk,2:new_pk,3:rotation_seq,4:expiry}, 48B Schnorr48 by OLD key over SHA256(CBOR(Sig_structure))] | divergent | Python library spec-exact + tested (crypto/key_rotation_attestation.py:29-73,117-125; test_key_rotation_attestation.py 21+ tests) but wired to nothing. Rust gateway/C gcp implement the §8.7 raw transcript (no COSE envelope) → b7z9.28; no /.well-known/key-rotation endpoint in any stack → new bead | high |
| R-06-052 | Receiver validation steps 1-11 (kid→old key, alg, old_pk match, sig, expiry, seq>cached, derive new IID, pin preserving seq, clear replay state); failure → reject and retain existing entry (§8.7.4) | divergent | Python verify_key_rotation_attestation (key_rotation_attestation.py:299-358: IID_MISMATCH/OLD_PUBKEY_MISMATCH/SIGNATURE_INVALID/EXPIRED/REPLAY_DETECTED) + get_new_iid :375-387; no production consumer in any stack (store-level rotations use raw transcript) → b7z9.28 + new bead | high |
| R-06-053 | rotation_seq MUST be persisted to NV; increment each rotation; first >0; fail closed at UINT64_MAX; persistence unavailable → rotation MUST fail (§8.7.4) | divergent | Rust gateway: sealed store field, survives restart, blocks replay (trust.rs:2120-2205). Python: crash-safe persistence required (trust.py:675-679) + bounds. **C: not persisted** (volatile parameter) → existing bead project-LICHEN-worker6-b7z9.33 | low |
| R-06-054 | MUST NOT accept a rotation with rotation_seq <= cached value (§8.7.4) | implemented+tested | rust :992-997 InvalidRotationSequence; python trust.py:662-666; attestation REPLAY_DETECTED key_rotation_attestation.py:354-356; C GCP_ROTATION_REJECT_REPLAY gcp_trust.c:212-218 | high |
| R-06-055 | Delivery MAY be CoAP POST /.well-known/key-rotation (OSCORE), DIO/DAO option, OOB; attestation MUST be verified before updating trust state (§8.7.4) | divergent | /.well-known/key-rotation absent in all stacks (endpoint gap → new bead). Verify-before-update satisfied where rotation exists (python trust.py:675-679; rust gateway :1000-1034; rust link verifies first, divergent format) | low |
| R-06-056 | For TOFU-pinned peers a valid rotation attestation is the ONLY way to change the pinned key; unattested key changes MUST be rejected and logged as potential MITM (§8.7.4) | implemented+tested | RX key-change reject+warn: rust link_layer.rs, python link_layer.py:1641-1651, gateway KeyConflict, C operator alert. Caveat: rust link's accepted "attestation" is the divergent KEY_ROTATE format (b7z9.28), and the §8.7.4 COSE path is unwired, so today there is no conformant over-the-air rotation for link-layer TOFU pins outside the gateway raw transcript | high |
| R-06-201 | OSCORE confidentiality: AES-CCM-16-64-128 (§8.8) | implemented+tested | rust oscore-fork lib.rs:46-47 (Ccm<Aes128,U8,U13>); C aes_ccm.c:8 ("COSE Algorithm ID 10") + oscore.h:84-90; python crypto/oscore.py. Tests: oscore-fork tests.rs:43 rfc8613 vectors, lichen/tests/oscore c1-c6, test/vectors/oscore.json | high |
| R-06-202 | OSCORE replay protection via sequence number (§8.8) | implemented+tested | rust oscore-fork context.rs:371-475 is_replay/update (check before decrypt, update after — unprotect.rs:76-77); C oscore_replay.c (32-bit window); python sqlite_store.py:469-504. Vectors: oscore.json 5 replay cases; tests rust tests.rs:895, C :1884, python :346 | high |
| R-06-203 | Key derivation: HKDF from master secret (§8.8) | implemented+tested | C hkdf.c + oscore_ctx.c:39-59; rust oscore-fork crypto.rs:56,75; python crypto/oscore.py. Tests: oscore_context_parity.json consumers | high |
| R-06-204 | Implementations MUST bound OSCORE security contexts to a finite limit; RECOMMENDED 64; constrained nodes MUST support at least 8 (§8.8, amended per decision oscore-context-bound) | divergent | C: Kconfig bound default 8 range 2-32 (oscore/Kconfig:20-26) — **no floor-8 enforcement (range permits 2) and constants 64/min-8 absent**; Python: `_MAX_ACTIVE_PEER_CONTEXTS = 2048` (channel.py:144); Rust: no bound found (lichen-node secure.rs HashMap). → new bead (decision requires all stacks: bound + LRU) | high |
| R-06-205 | MUST evict least-recently-used context on overflow (LRU by last message timestamp) (§8.8) | divergent | C: table full → OSCORE_ERR_NO_MEMORY, no eviction (oscore_ctx.c:377-390; persist layer -ENOSPC :427-430); Python: LRU eviction ✓ but bound 2048 (channel.py:418-449); Rust: no eviction found. → same new bead. (Part-2 marked human-only; decision oscore-context-bound makes this an implementation obligation, not oscore-semantics) | high |
| R-06-206 | OSCORE overhead 8-13 bytes (Partial IV + Tag) (§8.8) | implemented+untested | Tag 8 (`Ccm<Aes128,U8,U13>`; OSCORE_TAG_LEN 8 oscore.h:90) + PIV ≤5 (OSCORE_PIV_MAX_LEN 5 :96) ⇒ 9-13B; 5-byte terminal PIV wire form pinned by rust test observe_requires_explicit_piv... (lib.rs:3580). No dedicated total-overhead test | high |
| R-06-207 | EDHOC x25519_private = clamp(SHA-512(seed)[0:32]); clamping per RFC 7748 §5 REQUIRED (§8.9) | ambiguous | Python ✓ (identity.py:97-110, x25519.json clamping_checks); C ✓ (edhoc_crypto.c:281-307, exercised by lichen/tests/edhoc_handshake + x25519.json). Rust: fork EDHOC uses random ephemeral X25519 (initiator.rs:180) and the Ed25519 seed only for signatures (sign.rs:77); METHOD=0 (sig/sig) never uses static DH, so whether Rust needs the static derivation is an EDHOC-semantics question → b7z9.38 (human-only) | low |
| R-06-208 | EDHOC msg 2/3 authenticate via Ed25519 (or Schnorr variant) signatures from existing link-layer keypairs (§8.9) | implemented+tested | rust oscore-fork edhoc initiator.rs:224 METHOD=0 + credential.rs Ed25519 CCS + optional schnorr48 feature (Cargo.toml:59); python crypto/edhoc.py; C lichen/subsys/lichen/edhoc. Tests: test/vectors/edhoc.json, lichen/tests/edhoc_handshake, test_edhoc.py | high |
| R-06-209 | OSCORE Master Secret = EDHOC-Exporter(...,16); Master Salt = EDHOC-Exporter(...,8) (§8.9) | implemented+tested | RFC 9528 §7.2 numeric labels 0/1 (16/8 bytes): rust oscore-fork kdf.rs:32-135; python edhoc.py:571-572,844-845; C lichen/tests/edhoc_export `test_rfc9529_export_chain`. Oracle: test/vectors/edhoc_export_rfc9529.json. Spec's quoted text labels are informational (spec-text note) | high |
| R-06-210 | When to run EDHOC: lazy on first OSCORE request; explicit POST /.well-known/edhoc; periodic refresh 24h/seq-exhaustion (§8.9) | ambiguous | Python: lazy ✓ (channel.py:126-131,1449-1460), explicit endpoint ✓ (coap/resources/edhoc.py mounted). 24h refresh: not found in any stack. Rust/C: EDHOC library+tests only, no node runtime integration (nodes MAY use PS contexts per §8.9) | low |
| R-06-211 | EDHOC cipher suite 0 REQUIRED (§8.9) | implemented+tested | python edhoc.py:40 SUITE_0 + reject others :696-697; C EDHOC_SUITE_0 edhoc.h:37, responder rejects others edhoc_responder.c:105; rust fork suite fixed. Vectors: suite_negotiation.json (11 cases incl. ERR_WRONG_SELECTED_CIPHER_SUITE) → python TestSuiteNegotiationVectors | high |
| R-06-212 | Nodes unable to run EDHOC MAY use pre-shared OSCORE contexts provisioned out-of-band (§8.9) | implemented+untested (MAY) | C oscore_ctx_create fed by provisioning (oscore_ctx.c:311+); python provisioning.py; vector gcp_psk_oscore.json | high |
| R-06-213 | SHOULD rate-limit concurrent EDHOC handshakes to ≤3 per peer IID and 10 globally (§8.9) | divergent (SHOULD) | Python: global-only cap 64, no per-peer cap (channel.py:145, :1456-1460); Rust/C: none (no runtime EDHOC). SHOULD-gap; omission breaks no documented feature → noted, no bead | high |
| R-06-301 | All RPL control messages MUST carry valid Schnorr signatures; receivers MUST reject unsigned RPL frames; no normative permissive mode for production (§8.10) | implemented+tested | Link-layer mandatory signature rejects in all stacks (R-06-001) + RPL RX accepts only AuthenticatedFrame: rust receive.rs:190-214, C dodag.c:632-712 process_dio_bytes_authorized, python authenticated_dio.py:291-326. Only permissive switch is C CONFIG_LICHEN_LINK_MIC_SKIP_VERIFY (link/Kconfig:45, #warning, test-only). Tests: test_rpl_control_link_signatures.py:249, l2_key_selection main.c:534 | high |
| R-06-302 | Optional RPL preinstalled mode: CONFIG_LICHEN_RPL_SECURE_MODE + CONFIG_LICHEN_RPL_PSK control-plane PSK (§8.10, MAY) | not-implemented (MAY — no bead per protocol) | Absent from C Kconfig (lichen/subsys/lichen/rpl/Kconfig read in full), rust/, python/ | high |
| R-06-303 | Root DIOs MAY carry an additional COSE_Sign1 signature in an RPL DIO option (type 0x17, alg -65537, bstr-wrapped protected, 48B Schnorr48) (§8.10.1) | implemented+tested | Option 0x17: rust message.rs:85 + codec :415-450; C rpl_messages.h:330 + messages.c:218; python root_dio_signature.py:186-242. Rust RX wired in production (receive.rs:523-549); Rust TX exists behind non-default cargo feature `root-sig` (router.rs:905-955, transmit.rs:71-100); C library+Kconfig landed, no production call site; python oracle-only. Vectors: root_dio_signature.json consumed by all 3 stacks | high |
| R-06-304 | Payload CBOR map keys 1-7 (dodag_id, instance, version, rank, expiry, root_seq, mop); sig = Schnorr48(root_privkey, SHA256(CBOR(Sig_structure))) (§8.10.1) | implemented+tested | rust root_sig.rs:150-198 decode + :472-475 alg; C root_dio_sig.c payload decode; python :36-39. Byte parity via root_dio_signature.json; duplicate-label handling diverges (rust rejects fail-closed :124-131; C/py last-wins) | high |
| R-06-305 | Receiver validation: kid→IID, alg check, trust lookup, Sig_structure verify, payload fields match DIO, expiry>now, root_seq>cached keyed (dodag_id,instance), cache, mark root-authenticated (§8.10.1) | divergent (Rust wired in production RX; C library test-only; Python oracle-only) | Rust: receive.rs:685-778 — duplicate option reject :701-707, absent→Baseline :709-711, TOFU pin lookup :719-723, structural kid/DODAGID binding :725-728, sig :729-732, expiry :737-743, carrier cross-check :749-751, root_seq RootSeqCache persist-before-admit :760-775 (root_seq_cache.rs:45-97). C: dodag.c:782-911 verify_root_sig_option + _root_sig entry — called only from lichen/tests/rpl_root_sig (no production call site; CONFIG_LICHEN_RPL_ROOT_SIG Kconfig:21-31). Python: verify_root_dio_signature, no production caller. → existing bead project-LICHEN-worker6-b7z9.88 | low |
| R-06-306 | Nodes MUST NOT reject DIOs solely due to missing root signature validation capability (graceful degradation) (§8.10.1) | implemented+tested | Rust three-way Verified|Baseline|Reject: no option/unpinned/clockless all → Baseline (receive.rs:685-711 + stack tests :2814-3242 `expired_root_signature_admitted_as_baseline_not_rejected`, `unpinned_root_signature_gate_degrades_to_baseline`); C `test_dio_parse_without_option_unaffected` + dodag admission baseline tests (rpl_root_sig main.c) | high |
| R-06-307 | Expired signature → treat as unsigned; accept with link-layer auth only (§8.10.1) | implemented+tested | Rust RX maps expired → Baseline with tests (expired_root_signature_gate_degrades_to_baseline_not_reject :3161, clockless :3175); structural expiry boundary root_sig.rs:489; C ROOT_SIG_ERR_EXPIRED :855-867 (baseline, no policy wiring beyond tests) | high |
| R-06-308 | root_seq MUST NOT wrap; strict increase; replay/superseded seq rejected (§8.10.1) | implemented+tested | rust root_seq_cache.rs:45-97 strict-increase + `post_wrap_counter_is_rejected_not_wrapped` :264+; C root_dio_replay.c + tests; python seq<=cached reject; vector valid_seq_max (u64::MAX) | high |
| R-06-309 | Root re-election: clear cached root_seq; new root starts fresh (§8.10.1) | implemented+untested (transitive) | No explicit same-DODAG clear; cache keyed (dodag_id, instance) and DODAGID is bound to the key-derived root address (root_sig.rs structural binding), so a new root ⇒ new dodag_id ⇒ fresh key. Flagged for Opus | low |
| R-06-310 | Root SHOULD include the signature option when CONFIG_LICHEN_RPL_ROOT_SIG=y; MAY omit in trusted deployments (§8.10.1) | divergent | C Kconfig exists (rpl/Kconfig:21-31, default n) but C has no TX builder and the RX path has no production call site; Rust TX behind cargo feature `root-sig` (non-default) rather than a CONFIG knob; python create_root_dio_signature is an unwired oracle. → existing bead project-LICHEN-worker6-b7z9.88 | high |
| R-06-311 | Egress MUST verify the tunnel was authorized by the current DODAG root before decapsulating and forwarding to external networks (§8.11) | implemented+tested | Egress gates wired: rust resources.rs:1908-1928 authorize_egress (deny → drop); C lichen/apps/gateway/src/forwarding.c:168-211 inside NPF send rule (denial counter :148-153, fail-closed when mesh iface unidentified :173-177; ponytail caveat :192-197 — single-hop SRH extraction only, multi-hop grants fail closed; uptime seconds stand in for unix time so expiry dormant); python authorize_decapsulation + policy scope. Vectors: tunnel_authorization.json decapsulation_cases | high |
| R-06-312 | COSE alg Schnorr48-Ed25519 = -65537; bstr-wrapped {1:-65537} per spec/08 §6.5 (§8.11) | implemented+tested | rust tunnel_auth.rs:11,17 (PROTECTED a1013a00010000); C tunnel_auth.h:18; python SCHNORR48_ED25519_ALG. Decoy -65536 rejected in all stacks (tunnel_auth.rs:560-565 etc.); vector tunnel_authorization.json + gcp_slot_claim_cose_sign1.json `header_alg_decoy` | high |
| R-06-313 | Delivery: POST /.well-known/tunnel-auth, Content-Format application/cose cose-sign1, OSCORE-protected; 2.04 / 4.03 (§8.11) | implemented+tested | rust resources.rs:1935-1957 handle_post_tunnel_auth + dispatch :2350 (2.04 / uniform 4.03); C coap_server.c:560-634 (POST-only, OSCORE-only, BUILD_ASSERT pins codes) + app wiring lichen/apps/gateway/src/main.c:766-835,962-968; python coap/resources/tunnel_auth.py:34-90 + site.py:264-267 mount. Vectors post_cases (26, 204/403) cross-stack | high |
| R-06-314 | Payload CBOR map keys 1-6: target prefix (zero-padded), prefix_len 0-128, route_hash bstr(16), path_seq, expiry, egress_iid (§8.11) | implemented+tested | rust tunnel_auth.rs:604-629; C tunnel_auth.c:148-182; python payload :167-209 (canonical-strict). Vectors payload_hex byte-for-byte (rust tests :124, C test_encoder_exact_vector, python test_tunnel_auth_vectors.py) | high |
| R-06-315 | route_hash = SHA-256(concat of full 16-byte hop ADDRESSES in SRH visitation order)[0:16] — hash the hop ADDRESSES, not IIDs (§8.11 route-hash amendment) | divergent | **Python matches spec**: compute_route_hash hashes full 16-byte addresses (gateway/tunnel_auth.py:155-163, `_addr` enforces 16B). **Rust hashes 8-byte hop values** (tunnel_auth.rs:493-503 `route_hash(hops: &[[u8;8]])`); **C hashes 8-byte IIDs** (tunnel_auth.c:201-216, 8-byte stride); **committed vector pins the 8-byte form** (test/vectors/tunnel_authorization.json:61-65 route_hops_hex are 8-byte values). Spec text + rationale paragraph (:795-809) demand full 16-byte addresses under AddrForKey (no IID recoverable from a 02xx address) → new gap bead (interop: python-signed grants unverifiable by rust/C egress and vice versa) | high |
| R-06-316 | Egress validation on tunnel-auth POST: OSCORE, kid == current root IID, alg -65537, Sig_structure verify, egress_iid == own, expiry > now, path_seq > cached per (target,route_hash), cache, 2.04; failure → 4.03 + no cache (§8.11) | implemented+tested | rust accept_post tunnel_auth.rs:296-345 (MissingOscoreAuthentication/WrongRoot/WrongEgress/InvalidSignature/Expired/floors/Capacity) + wired handler 2.04/4.03 (resources.rs:1935-1957); C lichen_tunnel_auth_receive :338-393; python receive_post with uniform-4.03 + denial-only-in-log. Vector post_cases (26) consumed by all 3 | high |
| R-06-317 | Data packet decapsulation: compute route_hash from SRH, lookup (inner_src.prefix, route_hash); missing/expired → drop + log "unauthorized tunnel"; valid → decapsulate and forward (§8.11) | implemented+tested | rust authorize_decapsulation :386-448 (longest-prefix + expiry + UnauthorizedTunnel) wired :1908-1928; C lichen_tunnel_auth_decapsulate :442-478 + live gate forwarding.c (log "Egress dropped: tunnel denial %d" — literal spec string absent, cosmetic); python :511-554. C caveat: multi-hop SRH not extracted (ponytail, fail-closed) | high |
| R-06-318 | Implementations MUST bound the authorization table; recommended 256 entries with LRU eviction (§8.11) | implemented+tested | rust DEFAULT_AUTHORIZATION_CAPACITY=256 (tunnel_auth.rs:13) + LRU :259 + 4×N replay floors fail-closed; C CONFIG_LICHEN_TUNNEL_AUTH_MAX_ENTRIES default 8 range 1-32 (gateway/Kconfig:41-49) — MUST met, recommendation not; python MAX_AUTHORIZATIONS=256 + OrderedDict LRU. Root re-election clears (rust set_root :216-228, C change_root :395-407, py :428-443) | high |
| R-06-319 | Root SHOULD send tunnel-auth when new egress route installed (DAO), path changes, or nearing expiry; SHOULD NOT send for non-egress routes (§8.11) | divergent (hook + egress gate exist; no DAO/production trigger in any stack) | C lichen_tunnel_auth_route_installed (tunnel_auth.c:248-261, egress_capable gate) — test-only caller; python RootTunnelAuthorizer.route_installed :309-346 — test-only; rust build_root_post :142-161 — no capability gate, test-only. Nothing forwards the C DAO `route_installed` flag (rpl_dao_process.c:823-956) to tunnel-auth → new bead | high |
| R-06-320 | Root re-election: egress MUST clear the authorization table on detecting a new root identity in DIO; no grace period (§8.11) | implemented+tested | clear APIs implemented+vector-tested (R-06-318); DIO-driven root-change detection wiring absent in all stacks (config-driven root today) — tracked alongside b7z9.35 residuals | high |
| R-06-321 | Egress restart clears the authorization table; mesh-lifetime state; rebuilds via CoAP (§8.11) | implemented (by design) | Table is RAM-only in all three stacks (no persistence code) — matches mesh-lifetime semantics | high |
| R-06-322 | Future signed control messages SHOULD use COSE_Sign1 with Schnorr48-Ed25519 (-65537) (§8.11) | implemented (de facto) | Same envelope used by root-DIO sig, capability announcements, GCP slot-claim/handoff, delegation tokens, key-rotation attestation | high |
| R-06-323 | Capability bits: 0=egress, 1=prefix-delegation; bits 2-7 reserved and MUST be zero (§8.12) | implemented+tested (Rust/Python; C absent) | rust RESERVED_BITS_MASK=0xFC capability_announcements.rs:23-24 → ReservedBitsSet :317; python Capability IntFlag + verify :352-354; C RESERVED_MASK → DENIAL_RESERVED_BITS :218-219 (tracked p7mv for C). Vector capability_announcements.json reserved_bits cases | high |
| R-06-324 | Delivery: POST /.well-known/capability-announce, COSE_Sign1 -65537, kid = announcer IID, OSCORE-protected (§8.12) | divergent | Rust endpoint live but under /.well-known/lichen-gw/capability-announce (resources.rs:1845-1890, dispatch :2346-2348; gateway.rs:1416 prefix) — spec path is bare /.well-known/capability-announce; C resource exists (coap_server.c:636-705) but no app wires `s_handlers.capability_announce` → 4.04 in production apps; python resource complete + mounted (site.py:264-267). Python+Rust decoders accept tag-18 AND untagged (cose-sign1-tag18 decision); C rejects tag-18 (capability_announce.h:126-129) → new bead | high |
| R-06-325 | Payload CBOR map keys 1-6: capabilities, prefix, prefix_len, expiry, seq, announcer_iid (§8.12) | implemented+tested | rust decode :150-218 (kid↔announcer_iid :207-211); python :41-47 + __post_init__; C payload decode. Vectors capability_announcements.json byte-identity (rust :377-460; C test_corpus; python) | high |
| R-06-326 | Root validation on capability-announce POST: OSCORE sender auth, kid == sender ID, alg, sig, announcer_iid == kid, expiry, seq > cached, reserved bits zero, cache, 2.04 / 4.03 (§8.12) | implemented+tested | rust verify_announcement :291-323 + handle_post (cached_seq → verify → insert); python verify_capability_announcement :279-332 + resource 2.04/4.03; C verify path in capability_announce.c (wired via resource; see R-06-324 handler gap) | high |
| R-06-327 | Implementations MUST bound the capability table (recommended 256 LRU; MAY reserve up to 25% for egress-bit entries) (§8.12) | implemented+tested | rust capability.rs: 256/64 (25%) + partitioned LRU (non-egress capped at 192; `reserved_partition_protects_egress_entries` :259); C Kconfig default 256 range 4-512 + egress reservation split (capability_announce.c:381-407); python 256/64 + OrderedDict LRU | high |
| R-06-328 | Nodes SHOULD announce capabilities on joining, change, nearing expiry, after root re-election (§8.12) | divergent | Python `_reannounce_capabilities_to_new_root` (node.py:1182-1224, invoked after every processed DIO :1012,:1029) is the only node-side announcer; join-time announce NOT FOUND; zero-capability nodes stay silent by design; rust/c: no announcer → new bead | high |
| R-06-329 | Nodes MUST re-announce capabilities to the new root after detecting root change in DIO (§8.12) | divergent | Python implemented (node.py:1182-1224 + take_root_changes()); rust/c absent → new bead (with R-06-328) | high |
| R-06-330 | Root consults capability table (egress bit + prefix match) before sending tunnel-auth; no tunnel-auth for non-egress nodes (§8.12) | not-implemented (parameter-only glue) | C route_installed(..., egress_capable,...) and Py route_installed(..., egress_capable=...) require the caller to supply the flag; no stack reads a capability table to decide; rust build_root_post has no gate → new bead | high |
| R-06-331 | Nodes MAY hold signed credentials: COSE_Sign1 (bstr-wrapped -65537, optional x5chain header 33 per RFC 9360), payload keys 1-5 (§8.13) | not-implemented (MAY — tracked) | No credential codec/x5chain support in any stack. Tracked: project-LICHEN-worker6-bedw epic (+ children .1-.10) | high |
| R-06-332 | Claim namespaces lichen:/oidc:/vcard:/jwt:/URI; LICHEN claims lichen:fleet … lichen:role (§8.13) | not-implemented | Zero code references (repo grep; lichen:relay/emergency appear only in the §8.13.1 python module). Tracked: bedw | high |
| R-06-333 | Credential verification: alg -65537, x5chain → trust anchor, else issuer pubkey by kid, sig per RFC 9052, subject binding, expiry, seq (§8.13) | not-implemented | No verifier anywhere. Tracked: bedw | high |
| R-06-348 | x5chain Key Binding: all four checks MUST hold (leaf key verifies sig; IID(leaf pk) == kid; SAN /128 IID == kid; subject-iid == kid) (§8.13) | not-implemented | No x5chain parsing or chain-validation code in any stack; only spec/appendix-x509-cert-profile.md. Tracked: bedw (children .8 x5chain parsing, .5 cross-validation) | high |
| R-06-349 | Signing model: x5chain path is exclusively node-key attestation; authority MUST NOT attach its own chain as the credential's x5chain; authority asserts claims only issuer-signed (§8.13) | not-implemented | No credential implementation. Tracked: bedw | high |
| R-06-335 | Presentation: GET /.well-known/auth → 4.01; POST credential → 2.04, cached for session (§8.13) | not-implemented | .well-known/auth has zero code hits. Tracked: bedw | high |
| R-06-334 | Revocation = supersede with higher seq; no explicit revocation; 7-30 day expiry; CRL caching MAY (§8.13) | not-implemented | No credential seq machinery. Tracked: bedw | high |
| R-06-350 | §8.13.2: CA credentials portable; end-entity certificate MUST conform to the LICHEN node attestation profile (appendix-x509-cert-profile.md) (§8.13.2) | not-implemented | No X.509 parsing/verification code in any stack (only spec + appendix). Tracked: bedw (+ appendix migration tracked separately) | high |
| R-06-336 | Gateway issues local facts: POST /.well-known/local-fact (OSCORE) → 2.01 Created with COSE_Sign1 fact; policy out of band (§8.13.1) | divergent | Python library implemented+tested: python/src/lichen/gateway/local_facts.py (issue_local_fact :335-356, verify_local_fact :359-431, issuer-IID↔pubkey binding :412-413); tests test_local_facts.py. **No endpoint in any stack** (grep: no /.well-known/local-fact anywhere); rust/c absent | high |
| R-06-337 | Local fact claims: lichen:emergency, emergency_callback, relay, priority, channel, quota, sponsored (§8.13.1) | implemented+tested (Python lib; rust/c absent) | All nine claim constants + strict type/range validation: local_facts.py:43-53, :113-151 (priority 0..3, unknown-claim reject :190-197, strict CBOR :77-91). Tests: test_local_facts.py | high |
| R-06-351 | Local-fact freshness MUSTs: verifiers reject expiry not > now and seq not strictly greater than per-issuer cache; MUST fail closed (expiry fact without current time; seq fact without cache); first contact baseline −1, never derived from the fact; cache updated only after signature verifies, atomic with acceptance (§8.13.1) | implemented+tested | Python local_facts.py verify_local_fact: fail-closed expiry-without-time :414-417, seq-without-cache :418-422, strict-greater :421-422, bootstrap −1 documented :383-385 ("Never derive cached_seq from the fact being verified"), update-only-after-verify + atomic-with-acceptance as caller contract :387-391. Tests: test_verify_rejects_expiry_claim_without_current_time :434, test_verify_rejects_seq_claim_without_cached_seq :455, test_verify_seq_bootstrap_sentinel :463. Gap: no production caller-side seq-cache store; no endpoint | high |
| R-06-338 | Nodes without lichen:emergency SHOULD warn users emergency services are unavailable (§8.13.1) | not-implemented (SHOULD) | No warning UI; emergency resources implement §18 SOS relaying only, no fact check. Prerequisite feature tracked under bedw | high |
| R-06-339 | Federation facts MAY be co-signed (COSE_Sign multi-signature) (§8.13.1) | not-implemented | All COSE codecs are COSE_Sign1-only | high |
| R-06-340 | Local facts are mesh-lifetime: gateway restart / root re-election invalidates cached facts (§8.13.1) | not-implemented (no fact cache exists) | Follows from R-06-336/337; tracked bedw | high |
| R-06-341 | §15.2 (as amended by decision spec-15.2-key-storage): private keys stored in flash with integrity protection, never transmitted; SE preferred where hardware provides it; readout protection not required | implemented+tested | Plain flash + integrity in all stacks: C identity_store.c (LIKY record, CRC, mutex) + rollback authority identity_store.h:40-104; rust lichend.rs:166-236 (Zeroizing seed, abort on persist-fail) + hal/storage.rs; python key_persistence.py (0600 two-slot crash-safe). Never-TX: no privkey TX path. Spec amended this sweep per decision spec-15.2-key-storage (dropped SE/RDP MUST); the amendment resolves the driver of existing bead project-LICHEN-worker6-b7z9.87 | high |
| R-06-342 | §15.3 link replay: per-sender (epoch,seqnum), 32-entry sliding window; epoch persisted to flash, increments on reboot, never wraps — exhaustion at 255 fails closed requiring identity rotation | implemented+tested | 24-bit logical counter: rust seqnum.rs:121-127 ("Epoch MUST NOT wrap 255→0"), python replay.py:47-57, C replay.c. 32-entry window all stacks. Epoch persistence + boot bump + 255 fail-closed: C epoch_persist.c + replay_persist.c (A/B slots, authority digest, `++ctx->tx_epoch`, 255→-EOVERFLOW :434-437) — tests lichen/tests/link_epoch (wrap 253→255, corruption fail-closed), lichen/tests/replay_persist; python epoch_store.py (cold→128, EpochExhaustedError :56) — test_epoch_store.py; rust lichend.rs:211-236 (cold→128, 128-254→+1, 255→"rotate identity", persist-fail→abort) + per-peer replay seal/restore (link_layer.rs:66-85, seal :764-810, restore :827-910) — shared_vectors.rs, epoch_serial_vectors.rs. Spec amended per decision epoch-never-wrap → existing bead project-LICHEN-worker6-b7z9.86 resolvable | high |
| R-06-342b | (spec 4.4 cross-ref) C TX-epoch reserve-before-publish + reboot rejection of reserved captures | implemented+tested | lichen/tests/replay_persist: test_first_boot_and_reboot_reserve_before_publish, test_reboot_rejects_every_reserved_capture, test_capacity_fails_closed_without_eviction | high |
| R-06-343 | No address randomization / privacy extensions; stable key-derived IIDs and 02xx addresses; short address via crc32_ieee(EUI-64, key=LICHEN) (§15.5) | implemented+tested | rust iid_from_pubkey_bytes + short_addr.rs (LICHEN_CRC32_INITIAL=0x4348_454e); python short_addr.py + identity.py; C identity_addr.c + ipv6_addr.c. No RFC 4941/7217 code. Vectors: ipv6-addresses.json, node-addresses.json, yggdrasil_address.json | high |
| R-06-352 | §15.3 time-assisted replay rejection (MAY): wall-clock window `[now−REPLAY_TIME_WINDOW, now]`, RECOMMENDED 600 s (§15.3) | not-implemented (MAY) | REPLAY_TIME_WINDOW/600s: zero code hits in any stack; only spec text. Monotonic reception evidence exists (rust evidence.rs, python received_monotonic, C copy discipline) but unused for wall-clock rejection. No bead (MAY; primary mechanism is the sliding window) | high |
| R-06-353 | §15.3 time-bounded dedup: implementations MUST retain dedup entries until the message's absolute expiry; MUST NOT accept a message whose ID matches an existing entry (§15.3) | ambiguous | Closest analogues: python node.py:335 relay dedup (canonical-IPv6 + Hop-Limit normalization — not message-ID/absolute-expiry keyed); python sos_relay.py TTL-limited dedup (§18.4.3). No DTN store-and-forward message-ID dedup store with absolute-TTL expiry located. Needs Opus/arbitration whether the DTN dedup lives in 05-routing scope | low |
| R-06-214 | OSCORE sender seq reservations, recipient replay window, response replay state MUST remain valid across restart (§15.3) | implemented+tested | C: NVM callbacks + SSN restore (oscore.h:272, oscore_persist.c; sender reservation before AEAD publish oscore_protect.c:721-742; replay commit before plaintext release :505-544) — lichen/tests/oscore_persist (4 suites); rust: durable reserve→commit (oscore-fork reserve_sender; gateway GatewayOscoreSenderStore sealed records + rollback floor) — key_update.rs, ssn-restore vectors (oscore.json ssn_restore_*); python: sqlite_store.py durable ledgers (:74-80, :469-504) — test_sqlite_store.py :731/:753/:769 | high |
| R-06-215 | Persistent OSCORE state MUST be a versioned, authenticated record bound to the exact Security Context (algorithms, Sender ID, Recipient ID, ID Context, derived key/IV identity) (§15.3; decision oscore-persist-auth) | divergent | C: versioned ✓ (OSCORE_PERSIST_FORMAT_VERSION 1, oscore_persist.h:17) + MAC ✓ (keyed BLAKE2b, domain "LICHEN-OSCORE-STATE-AUTH-v1", oscore_persist.c:185-188,217-223) + context binding ✓ (oscore_settings.c:108-139 binds alg/IDs/ID-context/keys/IV) — tested lichen/tests/oscore_persist (4 suites). Rust gateway: every record sealed with Schnorr48 over domain‖context_id‖state (gateway.rs:155-183, :253-254, :286-287). **Python: NO record MAC** — sqlite_store.py schema has no authenticator (oscore-persist-auth decision unimplemented in Python) → new bead | high |
| R-06-216 | Sender reservation committed before nonce use; replay state committed before plaintext/result released (§15.3) | implemented+tested | rust ownership fencing + reserve ordering (rust/lichen-oscore/src/ownership.rs:26-31; tests ownership.rs, key_update.rs); C oscore_persist suites; python reservation tests (test_failed_reservation_transmits_nothing :321) | high |
| R-06-217 | Persistent state MUST be protected by an independent monotonic rollback-and-deletion authority, updated atomically (§15.3) | divergent | C: oscore_persist_protection_ops CAS + monotonic revision + state_advances() rejects rollback -ESTALE (oscore_persist.c:67-111,262-290) + A/B slots by revision parity — tested. Rust: CAS KeyUpdateStore + save_atomic_with_floor + independent rollback-floor directory (lichen-gateway gateway.rs:102-117, lichen-oscore lib.rs:103-115) — key_update.rs tests. **Python: sqlite transactions but rollback_anchor.py (StateRevisionAnchor CAS) is consumed by dao_persistence.py and key_persistence.py, NOT by any OSCORE store** → same new bead as R-06-215 | high |
| R-06-218 | Missing/corrupt/torn/stale/rolled-back/mismatched/unavailable OSCORE state MUST fail closed (§15.3) | implemented+tested | C: -EBADMSG/-EKEYREJECTED/-ESTALE/-EOVERFLOW on every failure class (oscore_persist.c:198-215,341-367) + context creation fails without authority (oscore_persist.h:115-121) — test_corruption_rollback_wrong_root_and_stray_state etc. Rust: restore_rejects_rolled_back_or_wrong_context_state (key_update.rs:346), Conflict paths. Python: test_replay_persistence_failure_drops_plaintext :753, generation-conflict rollback tests | high |
| R-06-219 | If state cannot be restored, MUST NOT reuse the affected context; fresh context with distinct key/nonce material required (§15.3) | ambiguous | C documents re-create-on-failure flow (oscore.h:219-220,272); rust re-key path tested (key_update.rs). Whether every restore-failure path refuses the old context in python is unpinned. **human-only (oscore semantics)** | low |
| R-06-220 | Clearing a replay window while retaining the old context MUST NOT re-accept previously accepted messages (§15.3) | implemented+tested | rust key updates fence sender sequence without resetting acceptance (lichen-oscore lib.rs:300; key_update.rs); C oscore_replay.c window persistence | high |
| R-06-221 | Every Observe notification MUST carry a fresh, nonzero Partial IV; receivers MUST reject any notification lacking one (deliberate RFC 8613 §4.1.3.5 deviation) (§15.3) | divergent | Rust receiver rejection implemented+tested: oscore-fork lib.rs:1376-1380 (observe && piv_len==0 → InvalidParam) + `observe_requires_explicit_piv_and_accepts_terminal_40_bit_piv_once` :3561; sender fresh PIV by construction (protect.rs:52-74, request PIV feeds AAD only). C: fresh-PIV sender path reserves+persistence (oscore_protect.c:669-725) + tests test_c8_response_with_fresh_partial_iv :730; C receiver-side rejection + python receiver-side rejection absent → existing bead project-LICHEN-worker6-b7z9.32. No interop vector pins the deviation despite spec's claim ("interop vectors pin the fresh-PIV requirement") — coap_observe_sequence.json pins RFC 7641 only (spec-text note → bead) | high |
| R-06-222 | LICHEN servers (Rust and C) MUST always include a fresh Partial IV per notification (§15.3) | divergent | Rust ✓ (secure.rs:3287 protected_observe_registration_notifications_retries_blocks_and_reset). C: fresh-PIV response API exists (oscore_protect.c:669-725) but observe-notification path unwired (b7z9.32: "C fresh-PIV response API unwired") | low |

### Sweep notes (Step 0 + cross-sweep reconciliation)

- **Adjudicated decisions applied this sweep (spec/decisions.jsonl → spec/06-security.md edits made in this sweep):**
  - `spec-15.2-key-storage` (2026-09-02): §15.2 still carried the SE/readout-protection MUST — amended to "stored in flash (with integrity protection), never transmitted; secure element preferred where hardware provides it". The code (plain flash + integrity, all three stacks) now conforms; the driver of existing bead `project-LICHEN-worker6-b7z9.87` is resolved and that bead can be closed.
  - `oscore-context-bound` (2026-09-03): §8.8 said "MUST bound … to at most 64 entries" (hard ceiling) — amended to finite-bound MUST + LRU-eviction MUST + RECOMMENDED 64 + minimum 8. Code lags the decision in all three stacks (R-06-204/205 → new bead).
  - Decisions `oscore-persist-auth`, `cose-sign1-tag18`, `epoch-never-wrap`, `upstream-yggdrasil-addressing` verified: spec text conforms (epoch text was already amended; §8.5/8.7 already cite the upstream decision). `epoch-never-wrap` means existing bead `project-LICHEN-worker6-b7z9.86` is resolved and can be closed.
- **Cross-sweep updates vs part1/part2/part3 matrices** (their numbering is reused): R-06-015/019 reclassified under the post-decision upstream AddrForKey spec text (part-1 swept the superseded SHA-512 native text); R-06-221 updated (Rust receiver-side fresh-PIV rejection now implemented+tested); R-06-215/217 updated (C persist MAC + rollback authority landed; Python is now the gap); R-06-303/310 updated (Rust TX landed behind cargo feature `root-sig`; C Kconfig + library landed, no production call site); R-06-311/313 updated (tunnel-auth CoAP endpoints + egress gates now wired in all three stacks — b7z9.35 is partially stale); R-06-328/329 updated (Python node-side re-announce landed); R-06-336/337 updated (Python local-facts library landed); R-06-342 updated (spec amended; Rust TX-epoch persistence located in lichend.rs).
- **Gap beads filed this sweep** (10; epic `project-LICHEN-worker6-b7z9`): route-hash width divergence (R-06-315); OSCORE context bound + LRU (R-06-204/205, decision oscore-context-bound); Python OSCORE persist MAC + rollback anchor (R-06-215/217, decision oscore-persist-auth); COSE tag-18/untagged accept-both (R-06-324 + §8.7.4/8.10.1/8.11 decoders, decision cose-sign1-tag18); capability-announce production wiring remainder (R-06-324/328/329); DAO→tunnel-auth trigger + capability-table consult (R-06-319/330); key-rotation COSE attestation wiring (R-06-051/052/055); trust/private-key store hardening gaps (R-06-048/049); security spec-text corrections (R-06-004 RFC 6979 label, R-06-221 interop-vector claim, R-06-209 exporter label phrasing); local-fact endpoint + rust/C parity (R-06-336).
- **Overflow (MUST-gaps beyond the 10-bead cap: 2, both already tracked):** node-rejects-further-provisioning (b7z9.34); BR anchor-list distribution/revocation/fetch (b7z9.80). Other unfiled items are non-MUST or tracked: §8.6 signature caching (divergent-stronger, spec-editor flag), EDHOC rate limiting 3/10 (SHOULD), 24h EDHOC refresh, DTLS for MQTT-SN (07-transport scope), DANE/PKIX (optional), credentials/local-fact claims (bedw epic), Observe fresh-PIV C/Python receivers (b7z9.32), C rotation-seq persistence (b7z9.33), rust-link KEY_ROTATE format (b7z9.28), RPL secure mode (MAY), wio-e5 IID (b7z9.82), C root-sig wiring (b7z9.88).
