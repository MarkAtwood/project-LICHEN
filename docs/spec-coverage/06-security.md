# Spec Coverage Matrix — spec/06-security.md (split sweep)

## spec/06-security.md (Part 1: §8.1–§8.7.4) — coverage (sweep 2026-09-01)

**Scope:** spec/06-security.md §8.1 (Threat Model) through §8.7.4 (Key Rotation Attestation).
§8.8 (OSCORE) onward is covered by parts 2 and 3. Internal heading numbers are §8.x; requirement IDs use R-06-NNN.

**Step 0 — adjudicated decisions:** `spec/decisions.jsonl` contains no decision whose `specs` array includes `06-security.md`; no `verify` gates applied, no spec fixes needed.

**Notes:**
- "The codebase" spans three stacks: Rust (`rust/`), Python (`python/src/lichen/`), C/Zephyr (`lichen/subsys/lichen/`). Where stacks differ, the row says so.
- Rust core Schnorr48 math is the published `schnorr48` crates.io crate; in-repo Rust evidence is the frame adapter + JSON-corpus tests against Python/C (acceptable: vectors are the oracle).
- Row 011/016/030/041/051/052/053 gaps already have open beads; no duplicates filed (referenced in Evidence column).

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-06-001 | Link-layer signatures REQUIRED on all RPL control frames (DIO/DAO/DIS); RPL secure mode optional (§8.1) | implemented+tested | Every link frame is signed and verified: rust/lichen-link/src/link_layer.rs:1092-1096 (unsigned rejected), :1128-1142 (verify); C lichen/subsys/lichen/link/lichen_link_rx.c:52-78 ("Signatures are now MANDATORY on every frame per spec/06-security.md"); RPL control is link-local ICMPv6 carried in signed link frames (rust/lichen-node/src/rpl_stack/util.rs:55-80, rust/lichen-node/src/stack.rs:448-466). Tests: rust/lichen-link/tests/relay_re_sign.rs, lichen/tests/replay, lichen/tests/schnorr48 | high |
| R-06-002 | Security layers: LLSec = Schnorr signature (48B); AES-128-CCM optional (§8.2) | implemented+tested | Schnorr48 layer in all stacks (rust/lichen-link/src/schnorr.rs:18-28; lichen/subsys/lichen/link/schnorr48.c; python/src/lichen/crypto/schnorr48.py). AES-128-CCM at link layer absent — optional per spec; C explicitly rejects encrypted frames fail-closed: lichen/subsys/lichen/link/CMakeLists.txt:73 ("AES-CCM is not used by the link layer; encrypted frames (E=1) are rejected"), Kconfig:74. Tests: rust/lichen-link/tests/schnorr48_vectors.rs:69 | high |
| R-06-003 | Every originated frame carries a Schnorr signature for sender authentication (§8.3) | implemented+tested | TX always signs: rust/lichen-link/src/schnorr.rs:56-85 (sign_frame) via link_layer.rs:1026-1066; C lichen/subsys/lichen/l2/lichen_l2_tx.c:108-150; python/src/lichen/link/link_layer.py RX verify path :1609-1612, :1796-1890. Tests: rust/lichen-link/tests/schnorr48_vectors.rs:69 (full corpus, bit-exact re-sign), lichen/tests/schnorr48/main.c:334, python/tests/crypto/test_schnorr48.py:32 | high |
| R-06-004 | Deterministic nonce (RFC 6979) REQUIRED (§8.3) | implemented+tested | r = H(privkey‖msg) mod L — exactly the spec's own pseudocode (spec line 44): python/src/lichen/crypto/schnorr48.py:123 (`_hash_to_scalar(privkey + msg)`), C lichen/subsys/lichen/link/schnorr48.c:81-91 ("Deterministic nonce: r = SHA-512(privkey \|\| msg) mod L"); Rust via schnorr48 crate. Determinism tests: python/tests/crypto/test_schnorr48.py:82, rust/lichen-link/tests/schnorr48_vectors.rs:144, C main.c:334 (bit-exact repeat) | high |
| R-06-005 | Signature 48 bytes = e[0:16]‖s[0:32]; e = SHA-512(R‖pk‖msg); verify R' = s·B − e·pk, valid = e'[0:16]==e (§8.3) | implemented+tested | python/src/lichen/crypto/schnorr48.py:128-138 (truncate e to 16, s = (r+e·d) mod L, output e‖s), :184-193 (R' recovery + compare; low-order-point and canonical-s rejection :25-36, :158-178); C lichen/subsys/lichen/link/schnorr48.c:99-122, :183-201. Oracle: test/vectors/schnorr48.json (13 vectors incl. tampered/low-order/s≥L negatives), consumed by all three stacks | high |
| R-06-006 | Receiver MUST verify signature and update authenticated peer's link replay window before processing payload (§8.4) | implemented+tested | Rust rust/lichen-link/src/link_layer.rs:1085-1223 — order enforced: parse :1090 → unsigned reject :1092-1096 → verify :1128-1142 → replay check_and_update :1157-1168 → payload released :1208-1222. C lichen/subsys/lichen/link/lichen_link_rx.c:248-296 (authenticate :266 → commit_replay :281 → copy to caller only after :375-377). Python link_layer.py:1609-1612. Tests: lichen/tests/replay/main.c (4 tests), lichen/tests/replay_persist/, rust link_layer tests :1655-1657, :2120+ | high |
| R-06-007 | Forwarding node creates a NEW link frame for next hop, allocates own replay counter, signs complete new frame (§8.4) | implemented+tested | rust/lichen-link/src/relay.rs:83-112 (`relay_verified_frame`: full verify+replay :93-95, then build/re-sign :101-110; module doc :1-10 quotes §8.4); inbound MIC never forwarded :76. C lichen/subsys/lichen/link/lichen_link_tx.c:376-448 (auth precedes mutation :422-427), :255-374 (own counter :329, re-sign :336-343). Tests: rust/lichen-link/tests/relay_re_sign.rs — 7 tests incl. independent Python-reference wire oracle (:101), relay counter is relay's own (:130), inbound signature never forwarded (:152); lichen/tests/link_relay/; python/tests/link/test_relay_signer_chain_vectors.py:150-180 (test/vectors/relay_signer_chain.json) | high |
| R-06-008 | Relays MUST preserve source and DAO bytes; only Hop Limit and enclosing link frame/signature may change (§8.4) | implemented+tested | Preserved source threaded to root handler: rust/lichen-node/src/rpl_stack/receive.rs:558-588 via util.rs:90-98; DAO origin transcript covers exact unsigned DAO bytes (rust/lichen-rpl/src/verify.rs:128-142, domain "LICHEN-DAO-ORIGIN-v1"); relay mutates only between verify and re-sign (relay.rs:69-72, lichen_link_tx.c:429-431). Tests: rust/lichen-node/tests/dao_origin_vectors.rs:144 (transcript-sensitive outcomes), relay_re_sign.rs:375 (permitted mutation flows through re-sign) | high |
| R-06-009 | Root MUST validate in this order: structural → pinned key/IID/transcript/origin-sig → per-key replay → semantic parsing → exact self /128 Target → replay-floor persistence → atomic route mutation (§8.4) | divergent | **Rust conforms**: rust/lichen-rpl/src/routing.rs:612-725 — admission :622-624 → per-key replay classification :627-640 → semantic+Target :646-655 → staged atomic mutation :678-696 → persistence :701-722 → `*self = proposed` only after persistence :723; crypto pre-stage verify.rs:52-103 (pinned key mandatory, key↔IID binding :79-81). **C diverges**: lichen/subsys/lichen/rpl/rpl_dao_process.c:807-937 validates semantic/Target (:841-846 dao_targets_authorized) BEFORE per-key replay classification (:862-898), so a semantically-invalid replay is reported as semantic, not replay (both reject; precedence differs from spec). Origin-sig verify itself is early in C (:246-291). Tests: rust/lichen-node/tests/dao_origin_vectors.rs:144 (outcome taxonomy), C lichen/tests/rpl_dao_auth test_dao_origin_authorization_matrix:232. → gap bead project-LICHEN-worker6-b7z9.81 | high |
| R-06-010 | Missing, corrupt, or unavailable replay persistence fails closed (§8.4) | divergent | **Rust conforms+tested**: rust/lichen-rpl/src/routing.rs:701-723 (high-water persisted via update_redundant; route mutation only on success), persistence.rs:566-576 (error mapping); test `unavailable_replay_storage_leaves_dao_state_unchanged` rust/lichen-node/tests/dao_origin_vectors.rs:264. **C RX path not found**: per-key origin-seq floor kept in volatile root_state snapshots (rpl_dao_process.c:862-898); no NV persistence of RX floor located (C TX-side floor IS persisted: rpl_dao_build.c:155-165, lichen/tests/rpl_dao_tx_persist/). Covered by existing bead project-LICHEN-worker6-b7z9.11 (C 64-bit DAO Origin Sequence persistence absent) — no duplicate filed | high |
| R-06-011 | Root accepts exactly one /128 Target whose 16 octets equal preserved Source Address; general prefix delegation remains future .44.9 work (§8.4) | divergent | Code implements a **generalized profile**: own-/128 OR exact delegation to the origin — rust/lichen-rpl/src/routing.rs:771-823 (`authorize_dao_prefixes`, `128 => prefix == origin.octets()` :805, delegation table :808-816, ::/0 fails closed :799-804); C dao_targets_authorized (rpl_dao_process.c, /128==origin check, delegation). Reconciliation of spec text vs implemented delegation profile is tracked by existing bead project-LICHEN-worker6-b7z9.9 — no duplicate filed. Tests: rust/lichen-rpl/tests/dao_prefix_authorization.rs, dao_origin_vectors.rs. Cardinality nuance (re-sweep): Python enforces single-distinct-Target explicitly (python/src/lichen/rpl/dao_manager.py:943 `multiple_target`), Rust has no separate cardinality check (per-Target authorization + sender-is-authorized only, routing.rs:737-741) | high |
| R-06-012 | Hop Limit/TTL and 6LoRH source-routing headers are unsigned/relay-mutable; mutations never occur under an unchanged link signature (§8.4) | implemented+tested | Signed transcript = LENGTH‖LLSec‖epoch‖seq‖DST‖SIID‖payload only (rust/lichen-link/src/schnorr.rs:124-149; C schnorr48.c:370-397); Hop-Limit/RH3 mutation applied between inbound verify and outbound re-sign (relay.rs:69-72, lichen_link_tx.c:429-431); RH3 survey rust/lichen-node/src/rpl_stack/util.rs:264-406. Tests: relay_re_sign.rs:375 (permitted mutation), :416 (tamper-after-sign detected downstream) | high |
| R-06-013 | All node identity derives from a single Ed25519 keypair; no separate keys or ULA (§8.5) | implemented+tested | Single from_seed path yields privkey/pubkey/IID/0200-addr: rust/lichen-link/src/identity.rs:69-72 (Identity::from_seed), python/src/lichen/crypto/identity.py:76, C lichen/subsys/lichen/link/identity_addr.c:28-68; no ULA construction anywhere in addr code (rust/lichen-core/src/addr.rs). Tests: rust/lichen-core/tests/ipv6_address_vectors.rs:130, python/tests/crypto/test_identity.py, lichen/tests/pubkey_to_iid/main.c | high |
| R-06-014 | IID = SHA-512(pubkey)[0:8], iid[0] &= 0b1111_1101 (U/L clear); MUST be SHA-512, not SHA-256 (§8.5) | implemented+tested | rust/lichen-core/src/addr.rs:86-92 (Sha512::digest, `iid[0] &= 0b1111_1101`; re-exported identity.rs:15-17); python identity.py:138-151; C identity_addr.c:28-47 — comment :36-40 explicitly forbids SHA-256 fallback, mask `~0x02` :41-43. Tests: ipv6_address_vectors.rs:130-175, python/tests/crypto/test_identity.py:270 (`test_iid_has_local_bit_clear`), lichen/tests/pubkey_to_iid/main.c:38-74. **Exception (re-sweep)**: bringup firmware rust/lichen-firmware/wio-e5/src/main.rs:33-39 `derive_iid` uses raw `seed[0:8]`, not SHA-512(pubkey); its ponytail comment misstates the canonical upgrade path as "SHA256". → gap bead (re-sweep) | high |
| R-06-015 | 0200::/8 addr = [0x02] + SHA-512(pubkey)[0:7] + IID; lower 64 bits == IID (binding prevents substitution) (§8.5) | implemented+tested | rust/lichen-core/src/addr.rs:109-117 (`ygg_addr_from_pubkey`: addr[0]=0x02, [1..8]=hash[0..7], [8..16]=iid); python identity.py:206-234; C identity_addr.c:52-68 (+ lichen/subsys/lichen/l2/ipv6_addr.c:317-333). Oracle: test/vectors/yggdrasil-derivation.json — incl. `iid_yggdrasil_binding_verification` and negative `iid_binding_mismatch_attack` entries; consumed by ipv6_address_vectors.rs:150-156 | high |
| R-06-016 | X25519 priv = clamp(SHA-512(seed)[0:32]) for OSCORE/EDHOC (§8.5 step 4) | divergent | **Python**: identity.py:97-110 (`x25519_private` = clamp(sha512(seed)[:32])) — tested vs test/vectors/x25519.json (python/tests/test_vectors.py:2335). **C**: lichen/subsys/lichen/edhoc/edhoc_crypto.c:287-307 (exact RFC 7748 clamp :298-300) — tested lichen/tests/schnorr48/main.c:300. **Rust ABSENT**: no static seed→X25519 derivation anywhere under rust/ (EDHOC is ephemeral-only); tracked by existing bead project-LICHEN-worker6-b7z9.38 (human-only, EDHOC semantics) — no duplicate filed | low |
| R-06-017 | TOFU pins pubkey to derived IID/02xx, cryptographically enforced (§8.5 step 5) | implemented+tested | rust/lichen-gateway/src/trust.rs:919-958 (`verify_tofu` → PinAndAccept / AcceptKnown / KeyConflict); rust/lichen-link/src/trust.rs:108-132 (`verify_iid_derivation`, RejectDerivationMismatch); python/src/lichen/crypto/trust.py:104-128 (TrustEntry enforces derivation :144-147); C lichen/subsys/lichen/coap/include/lichen/coap_keys.h:92-99, :218 (`-EKEYREJECTED` on non-derived IID). Tests: test/vectors/tofu_edge_cases.json (14 vectors incl. derivation_mismatch, pinned_key_collision) consumed by python/tests/crypto/test_tofu_edge_vectors.py + rust/lichen-gateway/tests/tofu_edge_vectors.rs; rust/lichen-link/tests/tofu_key_selection.rs; lichen/tests/coap_keys_lifecycle | high |
| R-06-018 | fe80::/10 for control only; key-derived 0200::/8 primary for all routable traffic (§8.5) | implemented+untested | C lichen/subsys/lichen/l2/ipv6_addr.c:104-113 (scope checks), :194 (fe80::+IID build); router.c:42-46 (`is_link_local`); schc_compress.c:167 (Rule 5 elides fe80::/64); python node.py:570,604 (next-hop against fe80::); rust lichen-core addr.rs (link_local_addr vs ygg_addr_from_pubkey), lichen-meshtastic/src/address.rs:16. No dedicated test found asserting the control/routable split | low |
| R-06-019 | Derivation MUST match §8.7 and test vectors exactly (§8.5) | implemented+tested | test/vectors/yggdrasil-derivation.json (7 entries, pubkey-based) consumed by: rust/lichen-core/tests/ipv6_address_vectors.rs:16,130-175; python/tests/test_vectors.py:2401; lichen/tests/pubkey_to_iid/main.c:38-74; lichen/tests/link_crypto/src/main.c:497. Caveat: no Rust consumer of test/vectors/x25519.json (ties to R-06-016). Note: spec cross-refs (`identity.rs:14-48`, symbol `yggdrasil_addr_from_pubkey`) are stale vs actual `rust/lichen-core/src/addr.rs:109` / `ygg_addr_from_pubkey` — spec-drift bead project-LICHEN-9k53 open | high |
| R-06-020 | §8.6 caching: verify at first hop, cache "verified from \<IID\>", relay without re-verify, cache keyed (source IID, seq) with TTL | divergent | **Not implemented — all stacks verify every frame at every receive** (stronger than spec): rust/lichen-link/src/link_layer.rs:1128-1135, relay.rs:1-10 ("verify the inbound signature … BEFORE applying any mutation"); python link_layer.py:1609-1612 + verify calls :1796-1890; C lichen_link_rx.c:134. No cache keyed (IID,seq), no "verified from" state (rg: 0 hits). Non-MUST text (numbered optimization, no RFC-2119 keyword) → no gap bead filed; flagged for spec-editor decision (spec should either bless per-hop verify or the implementations diverge from a normative-looking procedure) | low |
| R-06-021 | Cache entries expire after 2× expected mesh traversal time (default 30 s) (§8.6) | not-implemented | Depends entirely on R-06-020 cache, which does not exist. Near-miss python/src/lichen/link/receipts.py:14-33 (VERIFIED_RECEIPT_TTL_SECONDS=60) is an RxFrame-facade TTL, not a signature cache. No bead (non-MUST, contingent on R-06-020) | low |
| R-06-022 | High-security deployments MAY enable per-hop verification (§8.6 security note) | implemented+tested | Per-hop verification is the universal behavior (see R-06-020 evidence): every RX verifies, relays re-verify before mutation. Tests: relay_re_sign.rs:216 (tampered inbound rejected before emission), :416 | high |
| R-06-023 | Single 32-byte seed produces all material (Schnorr48, X25519, IID, 0200::/8); normative derivation MUST match test vectors exactly (§8.7) | implemented+tested | Keypair: h=SHA-512(seed), clamp(h[0:32]), pubkey=d·B — python schnorr48.py:96-106, C link crypto (Monocypher), Rust via schnorr48 crate; IID/addr per R-06-013..015; X25519 per R-06-016 (Rust gap). Oracles: test/vectors/schnorr48.json, x25519.json, yggdrasil-derivation.json — all consumed by tests (see rows above) | high |
| R-06-024 | Self-provisioned (RECOMMENDED) or BR-provisioned nodes derive identically (§8.7) | implemented+tested | Both paths call the same derivation: BR-provisioned entries land in the same store with same IID check — rust/lichen-gateway/src/trust.rs:672-713 (`provision_configured_peer`, BrProvisioned level, IID collision fails closed :674-680); python provisioning.py decrypt_ack verifies node-derived pubkey against Identity.from_seed; rust/lichen-oscore/src/provisioning.rs:838-870 (create_ack verifies derived pubkey). Tests: python/tests/crypto/test_provisioning.py:51 (happy path: node derives, BR verifies) | high |
| R-06-025 | All nodes generate (or are provisioned) a single Ed25519 keypair at first boot (§8.7) | implemented+tested | Identity::from_seed (identity.rs:69), python identity.py from_seed, C lichen_link_load_key. Tests: rust/lichen-link/tests/key_generation.rs, test_identity.py:100 (from_seed determinism) | high |
| R-06-026 | Private key stored securely (never transmitted) (§8.7) | implemented+untested | No code path transmits a private key (privkey used only in local sign/derive calls across all stacks). Storage: rust/lichen-hal/src/storage.rs:489-496 (0700 dir, regular-0600-file check, symlink refusal), python key_persistence.py (0600 temp files), C flash/settings-backed. Negative property ("never transmitted") has no dedicated test | low |
| R-06-027 | BR-provisioned: node derives IID/02xx from provisioned key; BR does not assign IID (§8.7) | implemented+tested | Node derives after decrypt_seed: python provisioning.py:595-615 ("node MUST store the new identity securely and exit commissioning mode"); rust/lichen-oscore/src/provisioning.rs:817-836; ack verification is MANDATORY (provisioning.py decrypt_ack compares against Identity.from_seed(seed).pubkey). Tests: test_provisioning.py:51, :429 (ack roundtrip), :445 (wrong-key ack fails) | high |
| R-06-028 | Provisioning channel MUST be encrypted and authenticated (§8.7) | implemented+tested | EDHOC-derived provisioning key encrypts seed and ack: python provisioning.py:174-216 (_derive_provisioning_key, _encrypt_seed/_decrypt_seed), :261-287 (_encrypt_ack/_decrypt_ack); rust mirror provisioning.rs:711-870; state machine rejects out-of-order transitions (provisioning.rs:788-790). Tests: test_provisioning.py:294 (wrong key), :317 (tampered ciphertext), :579/:648 (error indistinguishability), :717 (generic auth errors) | high |
| R-06-029 | Private key deleted from BR immediately after transfer (§8.7) | implemented+tested | BRProvisioningSession.wipe() clears _provisioned_seed and _prov_key, session unusable after (python/src/lichen/crypto/provisioning.py:491-502); docstring on encrypt_seed mandates deletion after node ACK. Tests: test_provisioning.py:1088 (test_br_wipe_clears_sensitive_data), :1133 (wipe resets state to IDLE). Note: "immediately" is caller-enforced (API contract), not automatic | high |
| R-06-030 | Node rejects further provisioning after commissioning; factory reset to redo (§8.7) | not-implemented | Persistent provisioning lock-out flag absent in all stacks — enforcement is per-session state machine only (rust/lichen-oscore/src/provisioning.rs:788-790 InvalidState; python provisioning resource close() is runtime-only :267-271; rg "provisioning_locked\|factory.reset" = 0 hits). Tracked by existing bead project-LICHEN-worker6-b7z9.34 — no duplicate filed | low |
| R-06-031 | All derived addresses (02xx, IID) MUST match key (§8.7 security requirements) | implemented+tested | Same binding checks as R-06-017: rust gateway verify_iid_binding (trust.rs:800), C `lichen_key_store_put` returns -EKEYREJECTED if iid is not key-derived (coap_keys.h:218), python TrustEntry.__post_init__ derivation enforcement (trust.py:144-147). Tests: tofu_edge_cases.json `derivation_mismatch`, lichen/tests/coap_keys, rust/lichen-gateway/src/trust.rs inline TOFU vector tests :1829-1867 | high |
| R-06-032 | Implementations MUST support TOFU; DANE/PKIX optional upgrades (§8.7) | implemented+tested | TOFU is the default trust path in all stacks (R-06-017/033 evidence). Tests: tofu_edge_cases.json corpus (3 stacks), tofu_key_selection.rs | high |
| R-06-033 | TOFU: accept+pin on first contact; key must match derivation of observed IID/02xx; subsequent contacts verify; key change/IID mismatch → reject AND alert (§8.7) | implemented+tested | Pin/reject: trust.rs:919-958; RX mismatch is rejected AND logged: rust link_layer.rs:1142-1155 (`warn!("link_layer: key change detected for IID …")` + LinkRxError::KeyChange), python link_layer.py:1635-1642 ("KEY CHANGE DETECTED" + ReceiveError.KEY_CHANGE), C mismatch event struct with saturating attempt counter (coap_keys.h:111-117). Tests: tofu_edge_vectors (py+rs), tofu_key_selection.rs, coap_keys_lifecycle (pin idempotence + reboot) | high |
| R-06-034 | Nodes trust anchors from BR without TOFU; derivation ensures key matches observed 02xx/IID (§8.7) | implemented+tested | BrProvisioned trust level bypasses TOFU as out-of-band anchor: rust/lichen-gateway/src/trust.rs:672-713 + BrProvisioned=2 :107; python trust.py add_trust_anchor :517; C GCP trust level PROVISIONED (gcp_trust.h:77). Tests: trust-level ordering (trust.rs:1691). Distribution *mechanism* for the anchor list is absent (→ R-06-035/036) | high |
| R-06-035 | BR revocation: remove from anchor list, push updated list to managed nodes, optional signed revocation broadcast; effective on receipt (§8.7) | not-implemented | No anchor-list distribution/revocation-push mechanism in rust/, python/, lichen/ (only "anchor" hits are persistence rollback anchors). Local revocation flag exists in python only (trust.py:709-729, tested test_trust.py:372-411); not wired to any distribution path. → gap bead filed (G2, SHOULD-class text but breaks the documented BR-provisioned feature) | low |
| R-06-036 | Nodes SHOULD fetch trust-anchor updates periodically (e.g., hourly) or on BR announcement (§8.7) | not-implemented | No periodic anchor fetcher or announcement listener anywhere (rg: no hits). Grouped into gap bead G2 with R-06-035 | low |
| R-06-037 | A mesh MAY contain both self-provisioned (TOFU) and BR-provisioned nodes; BR-provisioned trust each other via anchor list; interact with TOFU nodes normally (§8.7) | implemented+untested | Trust-level enums with ordering coexist in all stacks (rust gateway trust.rs:107-111 + ordering test :1691; python trust.py TrustLevel; C gcp_trust.h:77). Mixed-mesh interaction itself untested | high |
| R-06-038 | DANE optional: TLSA `_25519._mesh.\<name\>`, DNSSEC verify, upgrade TOFU→DANE, cached, out-of-band via BR not over LoRa (§8.7) | not-implemented | Enum plumbing only: python trust.py:50 (`DANE = 3`), rust gateway trust.rs:109-111 + upgrade_trust hook :1047-1064, C coap_keys.h:64 (comment). No DNS/TLSA query code (rg `_25519._mesh\|TLSA\|dnssec` = 0 code hits). Optional feature → no bead | high |
| R-06-039 | PKIX/ACME optional: cert via ACME (RFC 8555) or manual; certificate chains MUST NOT be embedded in every packet (§8.7) | not-implemented | No ACME client, no /.well-known/cert resource (rg = 0 hits). The MUST NOT is vacuously satisfied (no certificates exist anywhere in the stacks). Optional feature → no bead | high |
| R-06-040 | Out-of-band verification MAY: display pubkey fingerprint, manual compare, upgrade trust to "Verified" (§8.7) | not-implemented | upgrade_trust hook exists (rust gateway trust.rs:1047-1064) but no fingerprint display/QR/manual-compare flow in any stack. Optional → no bead | high |
| R-06-041 | Nodes SHOULD support key rotation announcements (§8.7) | divergent | Spec-transcript rotation implemented in Python (trust.py:38 domain, _rotate_key :604-707), Rust gateway (trust.rs:67 KEY_ROTATION_DOMAIN, build/verify/sign :560-603, process_key_rotation :965-1034), C gcp (gcp_trust.c:168-253). **Rust link layer diverges**: raw `"KEY_ROTATE:" \|\| new_pubkey` message, no domain tag/old-IID/sequence/expiry (rust/lichen-link/src/trust.rs:135, :160-184). Covered by existing bead project-LICHEN-worker6-b7z9.28 — no duplicate filed. Tests: python test_trust.py, gateway inline rotation tests :1917-1975; C has no harness (noted in b7z9.28) | high |
| R-06-042 | Rotation transcript exactly `"LICHEN-KEY-ROTATION-v1" \|\| 0x00 \|\| old_pubkey(32) \|\| old_iid(8) \|\| new_pubkey(32) \|\| rotation_seq(8 BE)`; seq strictly increasing, >0, never wraps (§8.7) | implemented+tested | rust gateway trust.rs:560-573 (exact byte layout; old IID derived from old_pubkey internally :557-559 so a substituted-IID signature is rejected); python trust.py:38 (`KEY_ROTATION_DOMAIN = b"LICHEN-KEY-ROTATION-v1\x00"`) + :668-670 (transcript computed internally, caller message never accepted); C gcp_trust.c:168-216 (103-byte transcript, BE sequence). Tests: gateway :1917-1975, python test_trust.py | high |
| R-06-043 | Rotation signature from another protocol domain or over a different old IID, key, or sequence MUST be rejected (§8.7) | implemented+tested | Domain-separation confused-deputy tests: rust/lichen-link/src/trust.rs:577 (`key_rotation_rejects_slot_claim_message`), :639 (`slot_claim_rejects_key_rotation_message`); gateway derives old IID internally (:557-559); python binds transcript internally (:668-670). Tests as named | high |
| R-06-044 | Key change with valid domain-separated old-key signature → accept new key; fresh per-peer replay state; old-key counters MUST NOT constrain new key (§8.7) | implemented+tested | rust gateway process_key_rotation :965-1034 — unchanged key rejected (:981-983), seq<=0 or <=old rejected (:985-990), valid rotation removes old entry, inserts new-IID entry carrying rotation_sequence with verify_count:1 (:1014-1028); python _rotate_key creates fresh entry :690-691. Tests: `trust_store_rotation_sequence_survives_restart_and_blocks_replay` (trust.rs:2127), zero-seq rejection :2173 | high |
| R-06-045 | Key change without signature → reject, require re-verification (§8.7) | implemented+tested | Unsigned/unpinned key changes rejected at RX: rust link_layer.rs:1142-1155 (KeyChange), python link_layer.py:1635-1642 (KEY_CHANGE), gateway KeyConflict (trust.rs:161-164). Tests: tofu_edge_cases.json `pinned_key_collision`; tofu_key_selection.rs (pin only on VERIFIED contact) | high |
| R-06-046 | Revocation: remove from local key store; no global revocation list (§8.7) | implemented+untested | Local revocation implemented and tested in Python only (trust.py:709-729; test_trust.py:372-411; revoked peers rejected during rotation :659-660). Rust gateway TrustEntry/rotation path and C stores: no `revoked` handling found. Cross-stack parity unproven | low |
| R-06-047 | Persisted trust entries MUST be treated as untrusted input on load: exact versioned schema + bounds, recompute IID/0200 from each pubkey, reject non-finite timestamps/invalid enums/counters, immutable detached entries (§8.7) | implemented+tested | Rust gateway trust.rs:733-811 — "Missing, corrupt, forged, and rolled back stores all fail closed" (:733-734), magic `LCHNTRS1` + version 2/legacy-1 (:87-89, :770-776), length/capacity bounds (:747-753), recompute verify_iid_binding → CorruptStore (:800), enum from_u8 (:801), timestamp checked_add (:1148-1152), verify_count==0 rejected (:810-811). Python key_persistence.py:738-756 (_decode_document re-parse, format_version 2) + trust.py:86-101 (finite timestamps, math.isfinite). Tests: corrupt-store/rollback-floor tests (gateway :2127 region). C: settings-backed floor only, no equivalent hardening layer (embedded scope — flagged for Opus opinion) | low |
| R-06-048 | Trust/private-key stores MUST use owned private dirs, non-following opens, interprocess locks, unique mode-0600 temp files, atomic replacement, file+dir fsync (§8.7) | implemented+untested | **Python complete**: key_persistence.py:71/:101 (O_NOFOLLOW, 0600 exclusive create), :106-121 (flock), :78-90 (0700 private dir), :311-330 (unique temp, fsync file+dir, atomic rename). **Rust hal complete**: storage.rs:489-496, :565-572, :622-649 (0700 dir, exclusive-create 0600 temp refusing symlink, rename, dir fsync). **Rust gateway trust.rs fixed 2026-09-01** (bead project-LICHEN-worker6-b7z9.79 closed): O_NOFOLLOW now at all three opens — verified in source (:742 load, :849 temp, :898 floor temp; rg custom_flags = 3); rename :851, file+dir fsync :849-859. Rust uses rename+monotonic-floor instead of flock for interprocess exclusion (Python uses flock) — equivalent-or-stronger, see flagged A13. No dedicated symlink-rejection test for the gateway store yet; CI compile gate pending per close reason | low |
| R-06-049 | Concurrent writers MUST use exact revision comparison; fail rather than silently overwrite newer trust state (§8.7) | implemented+tested | Python: expected_revision mismatch → "stale trust-store revision" raise (key_persistence.py:540-544). Rust gateway: monotonic rollback floor — RollbackDetected {stored, minimum} on load (:778-783, :181-183) + save_atomic_with_floor ordering (:869-873) (floor-based, equivalent-or-stronger than exact revision). Tests: rollback tests (gateway inline) | high |
| R-06-050 | Key generations and rotation sequences MUST fail closed at integer maximum instead of wrapping (§8.7) | implemented+tested | rust trust.rs:1100-1105 (`checked_add(1).ok_or(GenerationExhausted)`); python key_persistence.py:546-547 (revision==2^64-1 → exhausted); rotation seq bounded 1..2^64-1 (key_rotation_attestation.py:48, :114-115; trust.py:39, :647-651). Tests: exhaustion tests in gateway inline suite | high |
| R-06-051 | §8.7.4 attestation = COSE_Sign1 [protected h'47A1013A00010000' ({1:-65537}), unprotected {4: old-IID kid}, payload CBOR {1:old_pk,2:new_pk,3:rotation_seq,4:expiry}, 48-byte Schnorr48 sig by OLD key over SHA256(CBOR(Sig_structure))] | divergent | **Python library exact**: python/src/lichen/crypto/key_rotation_attestation.py:35 (alg -65537), :51-57 (protected header = cbor2.dumps({1:-65537}) ≡ a1 01 3a 00 01 00 00), :175 (kid=old IID), :42-45/:123-131 (payload map), :60-79/:256-258 (Sig_structure per RFC 9052, SHA-256, Schnorr48). Tested: python/tests/crypto/test_key_rotation_attestation.py. **Not wired to any CoAP endpoint** (/.well-known/key-rotation absent in all stacks; module imported only by its test). **Rust/C diverge** (raw formats) — covered by existing bead project-LICHEN-worker6-b7z9.28 — no duplicate filed | high |
| R-06-052 | Receiver validation steps 1-11 (kid→old key, alg check, old_pk match, sig verify, expiry>now, seq>cached, derive new IID, pin preserving seq, clear replay state); on failure reject and retain existing entry (§8.7.4) | divergent | Python library implements the chain: verify_key_rotation_attestation (key_rotation_attestation.py:265-319) — IID_MISMATCH :296, OLD_PUBKEY_MISMATCH :300, SIGNATURE_INVALID :309, EXPIRED :313, REPLAY_DETECTED :316-317; get_new_iid :336-348; tested (test_key_rotation_attestation.py). No production consumer in any stack (store-level rotation uses raw transcript instead); Rust/C validation chains follow their divergent formats. Tracked under project-LICHEN-worker6-b7z9.28 | high |
| R-06-053 | rotation_seq MUST be persisted to NV; increment each rotation; first rotation >0; fail closed at UINT64_MAX; persistence unavailable → rotation MUST fail (§8.7.4) | divergent | **Rust gateway conforms+tested**: rotation seq survives restart and blocks replay (trust.rs:2127), zero-seq rejected (:2173), u64 no-wrap (:985-990 + :1100-1105 exhaustion). **Python**: rotation requires crash-safe persistence (trust.py:675-679), seq bounds enforced. **C ABSENT**: gcp rotation sequence not persisted (volatile); exactly tracked by existing bead project-LICHEN-worker6-b7z9.33 — no duplicate filed | low |
| R-06-054 | MUST NOT accept a rotation with rotation_seq <= cached value (§8.7.4) | implemented+tested | rust gateway :985-990 (InvalidRotationSequence), python trust.py:662-666 (raise on <=), C gcp_trust.c anti-replay (GCP_ROTATION_REJECT_REPLAY), python attestation REPLAY_DETECTED (:316-317). Tests: trust_store_rotation_sequence_survives_restart_and_blocks_replay :2127, python test_trust.py | high |
| R-06-055 | Delivery MAY be CoAP POST /.well-known/key-rotation (OSCORE), DIO/DAO option, or out-of-band; attestation MUST be verified before updating trust state (§8.7.4) | divergent | /.well-known/key-rotation absent in all stacks (only /.well-known/core, lichen-gw/*, tunnel-auth, edhoc exist). The MUST is satisfied where rotation exists: python store verifies before mutation (trust.py:675-679 + _rotate_key), rust gateway verifies sig + seq before entry replacement (:965-1028), rust link verifies before pin update (trust.rs:160-184, divergent format). No new bead: the MAY delivery mechanisms are optional; format convergence is b7z9.28 | low |
| R-06-056 | For TOFU-pinned peers a valid rotation attestation is the ONLY way to change the pinned key; unattested key changes MUST be rejected and logged as potential MITM (§8.7.4) | implemented+tested | RX key-change = reject + warn-level log: rust link_layer.rs:1142-1155, python link_layer.py:1635-1642; gateway KeyConflict (trust.rs:933-938). Tests: tofu_key_selection.rs, tofu_edge_cases.json. Caveat: rust link's accepted "attestation" is the divergent raw KEY_ROTATE format (b7z9.28), not the §8.7.4 COSE_Sign1 | high |

## Summary

**Requirements extracted:** 56 (R-06-001..R-06-056)

**Classification histogram:**

| Status | Count |
|--------|-------|
| implemented+tested | 34 |
| implemented+untested | 5 (R-06-018, 026, 037, 046, 048) |
| divergent | 10 (R-06-009, 010, 011, 016, 020, 041, 051, 052, 053, 055) |
| not-implemented | 7 (R-06-021, 030, 035, 036, 038, 039, 040) |
| ambiguous | 0 |

**Gap beads filed this sweep (3 new, cap 10; re-sweep adds 1):**
- **project-LICHEN-worker6-b7z9.79** — R-06-048: Rust gateway trust store opens state file without O_NOFOLLOW (spec 8.7 store-hardening MUST) — **CLOSED 2026-09-01, fix verified in source (trust.rs:742/:849/:898)**
- **project-LICHEN-worker6-b7z9.80** — R-06-035/036: BR trust-anchor list distribution, revocation push, and periodic node fetch absent (spec 8.7)
- **project-LICHEN-worker6-b7z9.81** — R-06-009: C `process_dao` validates semantic/Target before per-key replay classification — spec 8.4 mandates replay-before-semantic order (Rust conforms)
- **(re-sweep) project-LICHEN-worker6-b7z9.82** — R-06-014: wio-e5 bringup firmware `derive_iid` uses raw seed bytes, not SHA-512(pubkey) (comment misstates canonical as "SHA256")

**MUST-gaps already tracked by existing beads (6, not duplicated):** R-06-010 → b7z9.11 (C DAO origin-seq persistence), R-06-011 → b7z9.9 (.44.7 vs delegation profile), R-06-016 → b7z9.38 (Rust static X25519, human-only), R-06-030 → b7z9.34 (provisioning lock-out), R-06-041/051/052 → b7z9.28 (rotation attestation format divergence), R-06-053 → b7z9.33 (C rotation-seq persistence).

**Overflow:** none (3 new + 6 pre-tracked = 9 ≤ 10).

**Spec-editor flags (no bead — non-MUST or spec-side):** R-06-020/021 (§8.6 signature cache described procedurally without RFC-2119 keyword; all stacks deliberately verify per-hop), R-06-011 (spec `.44.7` text vs implemented delegation profile), R-06-019 (stale symbol/line cross-refs, see project-LICHEN-9k53).

## spec/06-security.md (part 2 of 3: §8.8 OSCORE, §8.9 EDHOC, §15.3 OSCORE replay) — coverage (sweep 2026-09-01)

Scope note: the wave prompt named "§5-§7: OSCORE, EDHOC, group keying", but
spec/06-security.md has no §5-§7 (it numbers §8.1-§8.13 + §15.1-§15.6). This
part sweeps the OSCORE/EDHOC material: §8.8, §8.9, and the OSCORE-specific
replay content of §15.3 (lines 1232-1275). §8.1-§8.7 and §8.10-§8.13/§15
belong to parts 1 and 3. **Group keying has no dedicated section in this
spec file** — group OSCORE keying is implemented (test/vectors/group_oscore_key.json,
groups_rekey.json, python/src/lichen/crypto/group_oscore_wrap.py,
python/tests/crypto/test_group_oscore*.py) but its normative source lives in
another spec (gateway coordination / applications), not 06-security.

Req numbering: `R-06-2NN` for part 2, to avoid collision with parts 1/3.

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-06-201 | OSCORE confidentiality: AES-CCM-16-64-128 | implemented+tested | rust/oscore-fork/src/lib.rs:46-47 (`type AesCcm = Ccm<Aes128, U8, U13>`); lichen/subsys/lichen/oscore/oscore_protect.c:311,473 (lichen_aes_ccm_encrypt/decrypt); python/src/lichen/crypto/oscore.py:197 (default AES-CCM-16-64-128). Tests: rust/lichen-oscore/tests/vectors.rs, lichen/tests/oscore, test/vectors/oscore.json | high |
| R-06-202 | OSCORE replay protection via sequence number | implemented+tested | rust/oscore-fork/src/seqnum.rs + ownership fencing; lichen/subsys/lichen/oscore/oscore_replay.c (32-entry window); python sqlite_store.py:262 (restore_replay_window). Tests: test/vectors/oscore_cross_exchange.json, lichen/tests/oscore, rust/lichen-oscore/tests/vectors.rs | high |
| R-06-203 | Key derivation: HKDF from master secret | implemented+tested | lichen/subsys/lichen/oscore/hkdf.c; rust/oscore-fork/src/edhoc/kdf.rs:35 (hkdf_extract); python crypto/oscore.py. Tests: test/vectors/oscore_context_parity.json, python/tests/crypto/test_oscore_context_parity.py, rust/lichen-oscore/tests/context_parity.rs | high |
| R-06-204 | MUST bound OSCORE security contexts to at most 64 entries | divergent | C bound ≤64 ✓ (lichen/subsys/lichen/oscore/Kconfig:20-23, `LICHEN_OSCORE_MAX_CONTEXTS` default 8, range 2-32); Python 2048 ✗ (python/src/lichen/coap/secure/channel.py:144 `_MAX_ACTIVE_PEER_CONTEXTS = 2048`); Rust node unbounded ✗ (rust/lichen-node/src/secure.rs:443 `contexts: HashMap<[u8;8], Context>`, inserts at :565,:579 with no cap found). **human-only (oscore semantics)** | high |
| R-06-205 | MUST evict least-recently-used context on overflow (LRU by last message timestamp) | divergent | C: full table returns OSCORE_ERR_NO_MEMORY, no eviction (lichen/subsys/lichen/oscore/oscore_ctx.c:379-388 "Find free slot ... if (ctx == NULL) return OSCORE_ERR_NO_MEMORY"); Python LRU eviction ✓ but bound 2048 (channel.py:416-435 `_evict_peer_contexts_if_needed`); Rust node: no eviction found (secure.rs:565/579). **human-only (oscore semantics)** | high |
| R-06-206 | OSCORE overhead 8-13 bytes (Partial IV + Tag) | implemented+untested | Consistent across stacks: 8-byte tag (`Ccm<Aes128, U8, U13>`), PIV ≤5 (lichen/subsys/lichen/oscore/include/lichen/oscore.h:96 `OSCORE_PIV_MAX_LEN 5`); descriptive bound, no dedicated test | high |
| R-06-207 | x25519_private = clamp(SHA-512(seed)[0:32]); clamping per RFC 7748 §5 REQUIRED | ambiguous | C ✓ (lichen/subsys/lichen/edhoc/edhoc_crypto.c:287 `x25519_keypair_from_seed`, documented at edhoc_internal.h:89-94; exercised by lichen/tests/edhoc_handshake); Python ✓ (python/src/lichen/crypto/identity.py:98-103 `x25519_private`, :119 `x25519_public`); Rust ✗ static-from-seed derivation not found anywhere in rust/ (fork edhoc uses random ephemeral X25519, initiator.rs:180, and Ed25519 seed only for signatures, sign.rs:77). Unclear whether Rust needs it: METHOD=0 (sig/sig) never uses static DH. **human-only (EDHOC semantics)** | low |
| R-06-208 | EDHOC msg 2/3 authenticate via Ed25519 (or Schnorr variant) signatures from existing link-layer keypairs | implemented+tested | rust/oscore-fork/src/edhoc/initiator.rs:224 (METHOD=0 signature/signature), credential.rs:59-65 (Ed25519 CCS), sign.rs (ed25519_dalek); schnorr48 variant feature exists (rust/oscore-fork/Cargo.toml:59 `edhoc-schnorr48`); Python python/src/lichen/crypto/edhoc.py; C lichen/subsys/lichen/edhoc/. Tests: test/vectors/edhoc.json, lichen/tests/edhoc_handshake, python/tests/crypto/test_edhoc.py | high |
| R-06-209 | OSCORE Master Secret = EDHOC-Exporter(...,16), Master Salt = EDHOC-Exporter(...,8) | implemented+tested | RFC 9528 §7.2 numeric labels 0/1 (spec's text labels are informational): rust/oscore-fork/src/edhoc/kdf.rs:31-135 (LABEL_OSCORE_SECRET=0 → KEY_LEN 16; LABEL_OSCORE_SALT=1 → 8); python/src/lichen/crypto/edhoc.py:571-572,844-845; C lichen/tests/edhoc_export/src/main.c `test_rfc9529_export_chain` (label 7→10→0/1 chain, master_secret+master_salt pinned) | high |
| R-06-210 | When to run EDHOC: lazy on first OSCORE request; explicit POST /.well-known/edhoc; periodic refresh 24 h / seq exhaustion | ambiguous | Python: lazy ✓ (channel.py:126-131 docstring + `_establish` path :1449-1460), explicit endpoint ✓ (python/src/lichen/coap/resources/edhoc.py, registered at site.py:285); 24 h periodic refresh: not found in any stack; Rust node: no EDHOC integration (no edhoc refs in rust/lichen-node/src — edhoc only in lichen-oscore lib/tests); C node: EDHOC lib + tests only (lichen/subsys/lichen/edhoc, lichen/tests/edhoc_handshake). Unclear: whether Rust/C nodes are intended to establish EDHOC at runtime at all (nodes MAY use pre-shared contexts per §8.9) | low |
| R-06-211 | EDHOC cipher suite 0 (AES-CCM-16-64-128 / SHA-256 / X25519 / Ed25519) REQUIRED | implemented+tested | rust/oscore-fork/Cargo.toml:58 (`edhoc` = x25519-dalek + ed25519-dalek), SHA-256 KDF (kdf.rs), AES-CCM (lib.rs:46); python/src/lichen/crypto/edhoc.py:8 ("Why Suite 0 ... Matches link-layer Ed25519"); C lichen/subsys/lichen/edhoc. Tests: test/vectors/edhoc.json, edhoc_export_rfc9529.json | high |
| R-06-212 | Nodes unable to run EDHOC MAY use pre-shared OSCORE contexts provisioned out-of-band | implemented+untested (MAY) | C oscore_ctx_create* (lichen/subsys/lichen/oscore/oscore_ctx.c:311+) fed by provisioning; python crypto/provisioning.py; vector test/vectors/gcp_psk_oscore.json exists as parity input | high |
| R-06-213 | SHOULD rate-limit concurrent EDHOC handshakes to ≤3 per peer IID and 10 globally | divergent (SHOULD) | Python: global-only bound 64, no per-peer cap (python/src/lichen/coap/secure/channel.py:144 `_MAX_CONCURRENT_EDHOC = 64`, check at :1456-1460); no EDHOC handshake limiting found in Rust or C (neither node integrates EDHOC at runtime). SHOULD-gap noted in matrix, no bead filed (hardening recommendation; omission breaks no documented feature) | high |
| R-06-214 | OSCORE sender seq reservations, recipient replay window, response replay state MUST remain valid across restart | implemented+tested | C: NVM callbacks + SSN restore (lichen/subsys/lichen/oscore/oscore.h:272, oscore_persist.c); Rust: `ContextStateStore` compare_exchange + `reserve_sender` durable commit (rust/oscore-fork/src/lib.rs `reserve_sender` "Storage advances before this returns"); Python: sqlite_store.py:262,339-342 (replay window + sender seq persisted). Tests: lichen/tests/oscore_persist, rust/lichen-oscore/tests/key_update.rs | high |
| R-06-215 | Persistent state MUST be a versioned, authenticated record bound to the exact Security Context | divergent | C: versioned ✓, authenticated ✗ — MAGIC + FORMAT_VERSION only, no MAC/checksum over state (lichen/subsys/lichen/oscore/oscore_persist.c:12,170-171,204-205); context binding ✓ (record_id_derive, oscore_ctx.c:368-378); Python: no version or authentication found in sqlite_store.py; Rust: trait contract + app-level store (rust/lichen-gateway/src/gateway.rs:1072 `save_atomic_with_floor`). **human-only (oscore semantics)** | low |
| R-06-216 | Sender reservation committed before nonce use; replay state committed before plaintext/result released | implemented+tested | Rust: reserve→commit ordering (rust/oscore-fork/src/lib.rs reserve_sender doc: "a crash can only skip the reserved sequence"; Conflict for competing contexts); receiver ownership fencing (rust/lichen-oscore/src/ownership.rs:26-31). Tests: rust/lichen-oscore/tests/ownership.rs, key_update.rs; C: lichen/tests/oscore_persist | high |
| R-06-217 | Persistent state MUST be protected by an independent monotonic rollback-and-deletion authority, updated atomically | ambiguous | Rust ✓ (compare_exchange_sender CAS + `save_atomic_with_floor`, rust/lichen-gateway/src/gateway.rs:1072,1185; fork lib.rs KeyUpdateStore/ContextStateStore). C: NVM callback abstraction (oscore_ctx.c:194-205) — whether the registered store provides monotonic rollback authority is store-implementation-defined, unverified. Python: sqlite transactions, floor semantics unverified. Unclear: C/Python store guarantees | low |
| R-06-218 | Missing/corrupt/torn/stale/rolled-back state MUST fail closed | implemented+untested | C: load validates MAGIC+FORMAT_VERSION and rejects otherwise (oscore_persist.c:204-205); Rust: ReservationError::Conflict / fail-closed paths (fork lib.rs; ownership.rs:48 refuses at capacity). Python fail-closed behavior on corrupt rows: unverified | low |
| R-06-219 | If state cannot be restored, MUST NOT reuse the affected context; fresh context with distinct key/nonce material required | ambiguous | C header documents re-create flow on restore failure (lichen/subsys/lichen/oscore/include/lichen/oscore.h:219-220,272); Rust re-key path exists (key_update.rs). Whether every restore-failure path actually refuses the old context (vs. proceeding) is not pinned by a test I found. **human-only (oscore semantics)** | low |
| R-06-220 | Clearing a replay window while retaining the old context MUST NOT re-accept previously accepted messages | implemented+tested | Rust: key updates fence sender sequence without resetting acceptance (rust/lichen-oscore/src/lib.rs:300 "Key updates only fence the sender sequence"); test rust/lichen-oscore/tests/key_update.rs; C oscore_replay.c window persistence | high |
| R-06-221 | Every Observe notification MUST carry a fresh, nonzero Partial IV; receivers MUST reject any notification lacking one (deliberate RFC 8613 §4.1.3.5 deviation) | implemented+tested | Sender: fresh PIV by construction — rust/oscore-fork/src/protect.rs:52-74 `protect_response_with_piv` uses the reserved sender sequence as response PIV (nonce = compute_nonce(sender_id, response_piv), :483); request PIV feeds AAD only (:498). Node test rust/lichen-node/src/secure.rs:3145 `protected_observe_registration_notifications_retries_blocks_and_reset`; rust/lichen-oscore/tests/observe_responses.rs. Receiver-rejection of a missing/fresh-PIV-less notification: no pinning test found; §15.3 claims "interop vectors pin the fresh-PIV requirement" but no observe case exists in test/vectors/oscore*.json. **human-only (oscore semantics)** | low |
| R-06-222 | LICHEN servers (Rust and C) MUST always include a fresh Partial IV per notification | divergent | Rust ✓ (secure.rs:3145 test, protect.rs construction above). C: no OSCORE-protected Observe notification path found — coap_oscore.c has no observe handling; C coap_client.c:175,193 has client-side observe only. C server OSCORE notifications: not-implemented. **human-only (oscore semantics)** | low |

## Histogram

| Status | Count |
|--------|-------|
| implemented+tested | 9 (201,202,203,208,209,211,214,216,220) |
| implemented+untested | 2 (206,212) |
| divergent | 5 (204,205,213,215,222) |
| ambiguous | 6 (207,210,217,218,219,221) |
| not-implemented | 0 |

## Gap beads

Filed 3 (cap 10, overflow 0), all `human-only` per oscore/EDHOC edit bar:
1. R-06-204 + R-06-205 — context bound ≤64 + LRU eviction divergent across stacks (C bound ok but no eviction; Python 2048; Rust unbounded).
2. R-06-215 (+ R-06-218 C/Python residue) — persisted OSCORE state not authenticated (C: magic+version only; Python: no version/auth found).
3. R-06-207 — Rust lacks static X25519-from-Ed25519-seed derivation (C/Python have it; may be unused-by-design under METHOD=0).

SHOULD-divergence noted in matrix only (no bead): R-06-213 rate-limit values.
MAY rows never filed: R-06-212.

## Group keying

Not a section of spec/06-security.md — no R-06 requirements extracted for it.
Implementation evidence exists (test/vectors/group_oscore_key.json, groups_rekey.json,
python/src/lichen/crypto/group_oscore_wrap.py, python/tests/crypto/test_group_oscore*.py,
rust/oscore-fork/src/group.rs, lichen/subsys/lichen/oscore Kconfig `LICHEN_OSCORE_GROUP_MAX`);
its normative source should be swept under whichever spec owns it (gateway coordination /
applications), flagged for the wave runner to assign.



## spec/06-security.md (Part 3 of 3: §8.10–§8.13, §15.1–§15.6) — coverage (sweep 2026-09-01)

**Scope:** spec/06-security.md §8.10 (RPL Security) through §8.13.2 (CA Credentials), plus §15.1–§15.6 (Security Considerations remainder), excluding the OSCORE replay block of §15.3 (lines 1232–1275, already covered by part 2 as R-06-214…218). Requirement IDs use R-06-3NN (part 1 used R-06-0NN, part 2 used R-06-2NN).

**Step 0 — adjudicated decisions:** `spec/decisions.jsonl` contains no decision whose `specs` array includes `06-security.md`; no `verify` gates applied, no spec fixes needed.

**Notes:**
- Three stacks: Rust (`rust/`), C/Zephyr (`lichen/subsys/lichen/`), Python (`python/src/lichen/`).
- Wiring divergences for tunnel-auth (R-06-311/313/317/319/320) and capability announcements (R-06-324/328/329/330) are already tracked by open beads `project-LICHEN-worker6-b7z9.35` / `b7z9.36` (+ children `m9i4`, `utw0`, `7nnl`, `7nnl.3`, `99sg`, `p7mv`; Rust sub-bugs `4j1y`, `tpbv`, `b3le`, `t6sj`). No duplicates filed.
- Node credentials / local facts (§8.13, §8.13.1) are tracked by open epic `project-LICHEN-worker6-bedw` (+ children `.1`–`.10`). No duplicate beads filed.
- §8.13.2 (CA Credentials, portable) is descriptive coexistence text — no normative sentences, no rows.
- §15.1 (Cryptographic Strength), §15.4 (Known Limitations), §15.6 (Recommendations) are informational/advisory — no MUST/SHOULD on implementations; noted below the table.

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-06-301 | All RPL control messages (DIO/DAO/DIS) MUST carry valid Schnorr signatures; receivers MUST reject unsigned RPL frames; no normative permissive mode for production (§8.10, L538) | implemented+tested | Unsigned frames die at link layer before RPL in all stacks: rust/lichen-link/src/link_layer.rs:1092-1096; Rust RPL RX accepts only AuthenticatedFrame rust/lichen-node/src/rpl_stack/receive.rs:190; C lichen/subsys/lichen/link/lichen_link_rx.c:52-78 + authorized-gate lichen/subsys/lichen/rpl/dodag.c:547; Python python/src/lichen/rpl/authenticated_dio.py:291-326. No permissive flag exists in any stack (repo grep). Tests: python/tests/rpl/test_rpl_control_link_signatures.py:249 (test_unsigned_rpl_control_is_rejected_before_routing_mutation), lichen/tests/l2_key_selection/main.c:534, rust/lichen-link/tests/relay_re_sign.rs. Cross-ref part 1 R-06-001/R-06-006 | high |
| R-06-302 | Optional RPL preinstalled mode: CONFIG_LICHEN_RPL_SECURE_MODE=y + CONFIG_LICHEN_RPL_PSK control-plane PSK (MAY, defense-in-depth) (§8.10, L555-582) | not-implemented (MAY — no bead per protocol) | Strings exist only in spec L570-572 and as planned work docs/spec-chapter-breakdowns.md:937. Absent from lichen/subsys/lichen/rpl/Kconfig (read in full), rust/, python/. Note trap: rust/lichen-node/src/rpl_stack/secure.rs is OSCORE/CoAP, not RPL secure mode | high |
| R-06-303 | Root DIOs MAY carry an additional COSE_Sign1 signature (alg -65537, bstr-wrapped protected h'47A1013A00010000', 48-byte Schnorr48) in an RPL DIO option (§8.10.1, L589-613) | implemented+tested (verify path; no TX producer in any stack) | Option type 0x17: rust/lichen-rpl/src/message.rs:85 + RootDioSignature codec :428-450; C LICHEN_RPL_OPT_ROOT_DIO_SIGNATURE lichen/subsys/lichen/rpl/messages.c:176-223 (singleton, min 64/max 255); Python crypto/root_dio_signature.py:186-242 (to/from_cose_sign1) + root-side signer create_root_dio_signature :242. Tests: lichen/tests/rpl_root_sig/main.c (17 tests incl. option-constant sync), rust root_sig.rs:427-705, python/tests/crypto/test_root_dio_signature.py (20); vectors test/vectors/root_dio_signature.json (14 cases, cross-stack). Gap: no stack appends the option to outgoing DIOs; CONFIG_LICHEN_RPL_ROOT_SIG absent — residual bead filed (see R-06-310) | high |
| R-06-304 | Payload CBOR map keys 1-7 (dodag_id, instance, version, rank, expiry, root_seq, mop); sig = Schnorr48(root_privkey, SHA256(CBOR(Sig_structure "Signature1"‖protected‖h''‖payload))) (§8.10.1, L615-655) | implemented+tested | Rust root_sig.rs:146-193 decode, :286-321 canonical payload, :338-347 SHA-256 Sig_structure; C root_dio_sig.c:226-347, :365-392; Python root_dio_signature.py:106-146, :62-81. Byte-parity via test/vectors/root_dio_signature.json (protected hex 47a1013a00010000, vector sig_structure) consumed by all three stacks. Divergence note: duplicate CBOR labels — Rust rejects fail-closed (root_sig.rs:124-131), C/Python last-wins (dict semantics) | high |
| R-06-305 | Receiver validation: kid→IID, alg check, root pubkey from trust store, Sig_structure verify, payload fields match DIO, expiry>now, root_seq>cached keyed (dodag_id,instance), cache, mark root-authenticated (§8.10.1, L659-669) | divergent (Rust wired into RX; C library test-only; Python oracle-only) | Rust: wired in DIO RX rust/lichen-node/src/rpl_stack/receive.rs:522→657-747; trust store = announce TOFU pin table (announce.rs:287); RootSeqCache rust/lichen-rpl/src/root_seq_cache.rs:45-97. C: lichen/subsys/lichen/rpl/root_dio_sig.c:401-445 + root_dio_replay.c — only callers are tests (lichen/tests/rpl_root_sig); dodag.c RX never consumes the option. Python: verify_root_dio_signature root_dio_signature.py:291-377 — no production caller, no keyed cache. Tests: root_sig.rs:458-693 (18 tests), lichen/tests/rpl_root_sig/main.c, python/tests/crypto/test_root_dio_signature.py. Note: spec L661 says kid must match DIO source IID; stacks instead bind kid to the IID derived from the pinned/supplied root pubkey (stronger; Rust pins via Announce). Residual bead filed: project-LICHEN-worker6-b7z9.88 (root-sig cross-stack wiring) | low |
| R-06-306 | Nodes MUST NOT reject DIOs solely due to missing root signature validation capability (graceful degradation; TOFU pin later) (§8.10.1, L673-680) | implemented+tested (Rust; C option-layer) | Rust three-way outcome Verified\|Baseline\|Reject receive.rs:638-648 — no option :681-683, unpinned root :691-695, no wall clock :709-712 all → Baseline (never Reject). C: test_dio_parse_without_option_unaffected lichen/tests/rpl_root_sig/main.c:200-222. Python: link-layer TOFU pinning link_layer.py:199. Note: spec's "cache root IID from DIO for later TOFU pinning" (L676) is not literal — pinning comes from the root's Announce instead (receive.rs:692-694) | high |
| R-06-307 | Expired signature → treat as unsigned; accept with link-layer auth only (§8.10.1, L689) | implemented+untested (Rust RX mapping) | Rust receive.rs:713-715 expired → Baseline (not Reject); C ROOT_SIG_ERR_EXPIRED root_dio_sig.c:425-427 (no policy wiring); Python EXPIRED root_dio_signature.py:350-351 (caller maps). Tests: expiry_boundary_rejects_equal_timestamp root_sig.rs:489 (structural level) — no test asserts Baseline-on-expired in the RX path | low |
| R-06-308 | root_seq MUST NOT wrap (uint64); strict increase; replay/superseded seq rejected (§8.10.1, L687, L667) | implemented+tested | Rust RootSeqCache strict-increase + post-wrap rejection root_seq_cache.rs:36-77, test post_wrap_counter_is_rejected_not_wrapped :131-138; C root_dio_replay.c:31-33 + test main.c:339-340; Python seq<=cached reject root_dio_signature.py:354-355; vector valid_seq_max (u64::MAX) | high |
| R-06-309 | Root re-election: clear cached root_seq; new root starts fresh (§8.10.1, L686) | implemented+untested (transitive via keying) | No explicit cache-clear in any stack. Mitigation: cache keyed (dodag_id, instance) and DODAGID is bound to the key-derived root address (root_sig.rs structural binding :419-423), so a new root ⇒ new dodag_id ⇒ fresh cache key. Same-DODAG explicit clear absent. Flagged for Opus | low |
| R-06-310 | Root SHOULD include the signature option when CONFIG_LICHEN_RPL_ROOT_SIG=y (adversarial environments) (§8.10.1, L693-699) | not-implemented | No Kconfig/cargo feature anywhere; no root-side TX signing in Rust/C (Python has create_root_dio_signature but it is an unwired oracle). Residual bead filed: project-LICHEN-worker6-b7z9.88 (root-sig TX + CONFIG switch + C RX call site + Python integration) | high |
| R-06-311 | Egress MUST verify the tunnel was authorized by the current DODAG root before decapsulating and forwarding to external networks (§8.11, L703-705) | divergent (library complete+tested in all stacks; wired into no live data path) | Rust TunnelAuthorizationTable::authorize_decapsulation rust/lichen-gateway/src/tunnel_auth.rs:376-426 (denial UnauthorizedTunnel :41); C lichen_tunnel_auth_decapsulate lichen/subsys/lichen/gateway/tunnel_auth.c:416-438 (DENIAL_NO_AUTHORIZATION :431); Python gateway/tunnel_auth.py:511-554 + TunnelEgressGateway.forward :563-583 (never external_sends before permit). No production call sites in any stack (gateway decap paths never invoke). Tracked: project-LICHEN-worker6-b7z9.35 (+ children m9i4 Rust dispatch/egress, utw0 C resource); Rust sub-bugs 4j1y (first-match vs longest-prefix), tpbv (missing _purge_expired), b3le (claim-selection divergence), t6sj (test expects Replay gets Revoked) | high |
| R-06-312 | COSE alg Schnorr48-Ed25519 = -65537 used in COSE_Sign1 protected headers; bstr-wrapped {1:-65537} per spec/08 §6.5 (§8.11, L714-722, L732-737) | implemented+tested | Rust COSE_SCHNORR48_ED25519=-65_537 tunnel_auth.rs:11, PROTECTED b"\xa1\x01\x3a\x00\x01\x00\x00" :17 (wire 0x47-wrapped asserted :852); C tunnel_auth.h:17 + protected_header[] tunnel_auth.c:16; Python SCHNORR48_ED25519_ALG :31, canonical protected :288. Decoy alg -65536 rejected in all stacks (Rust :560-565, C decode_sign1 :270-278, Py :239-240). Vector sig_structure bytes consumed cross-stack | high |
| R-06-313 | Delivery: POST coap://[egress]/.well-known/tunnel-auth, Content-Format application/cose cose-sign1, OSCORE-protected (root-egress pairwise); 2.04 Changed / 4.03 Forbidden (§8.11, L725-741, L798-800) | divergent (Python endpoint complete+mounted; Rust/C endpoint absent) | Python coap/resources/tunnel_auth.py:34-83, mounted coap/resources/site.py:260-263, OSCORE-enforced (_request_is_oscore_protected + transport-remote IID :57-59), uniform 4.03 + log :69-74, 2.04 :68. Rust resources.rs dispatch :2270-2290 has no tunnel-auth route; C lichen_tunnel_auth_receive has no production caller. Root-side sender libs exist (Rust build_root_post :142-161, C route_installed :237-261, Py route_installed :307-341) but are unwired. Tests: python/tests/coap/resources/test_tunnel_auth_resource.py (6 incl. non-OSCORE denied :88). Tracked: b7z9.35/utw0/m9i4 | high |
| R-06-314 | Payload CBOR map keys 1-6: target prefix (zero-padded), prefix_len 0-128, route_hash bstr(16), path_seq, expiry (unix), egress_iid bstr(8) (§8.11, L743-756) | implemented+tested | Rust encode/decode_payload tunnel_auth.rs:526-549/581-607 (exact key order, canonical CBOR, prefix_len≤128); C :137-149/151-171; Python TunnelAuthorizationPayload :184-209 (canonical-strict _canonical_loads :117-130). Vectors test/vectors/tunnel_authorization.json payload_hex byte-for-byte: rust/lichen-gateway/tests/tunnel_auth_vectors.rs:124, lichen/tests/tunnel_auth/main.c:139 (test_encoder_exact_vector), python/tests/gateway/test_tunnel_auth_vectors.py | high |
| R-06-315 | route_hash = SHA-256(concat(hop[0..n] IIDs in source-route order))[0:16]; order matches IPv6 Source-Route Header (first hop → egress) (§8.11, L758-767) | implemented+tested | Rust route_hash tunnel_auth.rs:470-480 + validate_route :458-468 (1-8 hops, no dups); C lichen_tunnel_route_hash :190-205 (-ELOOP on loop); Python compute_route_hash :155-163. Vector route_hops_hex pins first→egress order. Note: all three take caller-supplied ordered hops; none extracts hops from an actual SRH at this site (data-path wiring is R-06-311/b7z9.35) | high |
| R-06-316 | Egress validation on tunnel-auth POST: OSCORE verify, kid == current DODAG root IID, alg -65537, Sig_structure verify w/ root pubkey, egress_iid == own IID, expiry > now, path_seq > cached per (target,route_hash), cache keyed (target_prefix,route_hash), 2.04; failure → 4.03 Forbidden, no cache (§8.11, L785-800) | implemented+tested | Rust accept_post tunnel_auth.rs:282-372 in spec order (+fail-closed clock-rollback guard :428-443; uniform 4.03 coap_response_code :47-49); C receive :327-367 (revoked floor :348, history-capacity fail-closed :351); Python receive_post :438-494 (atomic commit :486-493). Vector post_cases (26, response_code 204/403) consumed by all three (Rust :188, C test_complete_shared_decision_corpus, Py :213). Note: "current root" = configured root identity (set_root/change_root); DIO-driven change detection unwired — see R-06-320 | high |
| R-06-317 | Data packet for decapsulation: compute route_hash from SRH, lookup (inner_src.prefix, route_hash); missing/expired → drop + log "unauthorized tunnel"; valid → decapsulate and forward (§8.11, L802-807) | divergent (library implemented+tested; data path unwired; literal "unauthorized tunnel" log absent everywhere) | Rust :390-415 (route_hash match + last-hop==egress_iid + prefix match, expiry purge, LRU touch); C :423-437 (longest-prefix :427-430, link-local/multicast/loopback/0200::/8-dest denials :433-436); Python :526-553 (longest-prefix max(prefixlen) :545, policy scope :549-552). Vector decapsulation_cases (13: wrong_route_hash, expired_at_boundary, root_rotation_clears_table, link_local_source_rejected, looped_route…) driven by Rust :248, C zephyr test_complete_shared_decision_corpus, Py :236. Logging: only Py POST-denial log resources/tunnel_auth.py:69-73. Tracked: b7z9.35 | high |
| R-06-318 | Implementations MUST bound the authorization table; recommended 256 entries with LRU eviction (§8.11, L809-812) | implemented+tested (C bound = 8, max 32 — below recommendation, MUST still met) | Rust DEFAULT_AUTHORIZATION_CAPACITY=256 tunnel_auth.rs:13 + LRU :345-352 (+ replay-floor history 4×N fail-closed Capacity :332-337 — stricter than plain LRU); C gateway/Kconfig:16-19 default 8, range 1-32, LRU by age :357-361, history fail-closed ENOSPC :391; Python MAX_AUTHORIZATIONS=256 :35, LRU popitem(last=False) :492-493, floor fail-closed :484-485. Tests: rust tunnel_auth.rs:996+, C fail_closed_boundaries, Py | high |
| R-06-319 | Root SHOULD send tunnel-auth when new egress route installed (DAO), route path changes, or nearing expiry; SHOULD NOT send for non-egress routes (§8.11, L814-823) | divergent (hook with egress gate exists; no DAO/production wiring in any stack) | C lichen_tunnel_auth_route_installed tunnel_auth.c:237-261 (egress_capable gate :248-250); Python RootTunnelAuthorizer.route_installed :307-341 (gate :326-327); Rust build_root_post :142-161 (no capability gate at all). No caller from DAO processing in any stack. Tracked: b7z9.35 | high |
| R-06-320 | Root re-election: egress MUST clear the authorization table on detecting a new root identity in DIO; no grace period (§8.11, L834-840) | divergent (clear API implemented+tested; DIO-detection wiring absent) | Rust set_root :217-225; C change_root :369-381 (purges entries+history+time); Python change_root :421-436. Tests: rust revocation_root_change :996, vectors old_root_after_rotation / new_root_after_rotation / decap root_rotation_clears_table. Detection of new root from DIO absent in all stacks (pairs with R-06-316 note). Tracked: b7z9.35 | high |
| R-06-321 | Egress restart clears the authorization table; mesh-lifetime state; rebuilds via CoAP as routes re-establish (§8.11, L842-847) | implemented (by design: in-memory only) | All three stacks hold the table in RAM; no persistence code exists for it (verified across tunnel_auth.rs / tunnel_auth.c / tunnel_auth.py) — matches mesh-lifetime semantics | high |
| R-06-322 | Future signed control messages SHOULD use COSE_Sign1 with Schnorr48-Ed25519 (-65537) unless another algorithm is required (§8.11, L849-861) | implemented (satisfied de facto) | Same envelope used by: root DIO signature (R-06-303), capability announcements (R-06-324), GCP slot-claim/handoff (lichen/subsys/lichen/coap/coap_slot_coord.c:78-85), delegation tokens, key-rotation attestation (python/src/lichen/crypto/) | high |
| R-06-323 | Capability bits: 0=egress, 1=prefix-delegation; bits 2-7 reserved and MUST be zero (§8.12, L880-886) | implemented+tested (Rust/Python; C absent) | Rust RESERVED_BITS_MASK=0xFC capability_announcements.rs:25, checked :299-301 → ReservedBitsSet; Python Capability IntFlag :35-40, __post_init__ :103-111, verify RESERVED_BITS_SET :306-308. Vector capability_invalid_reserved_bits consumed by Rust :433-438 and Py test_invalid_reserved_bits:89. C: none (tracked p7mv) | high |
| R-06-324 | Delivery: POST /.well-known/capability-announce, COSE_Sign1 alg -65537 bstr-wrapped protected, kid = announcer IID, OSCORE-protected (§8.12, L888-907) | divergent (Rust endpoint live but under /.well-known/lichen-gw/ prefix; Python resource complete but unmounted; C absent) | Rust resources.rs:1888-1932 handle_post_capability_announce dispatched :2280, path prefix parse gateway.rs:1416 → /.well-known/lichen-gw/capability-announce (spec says /.well-known/capability-announce); OSCORE 401 :1901-1907; alg -65537 :13. Python coap/resources/capability_announce.py:124-181 — NOT mounted in site.py (tests only). C: none (naming comment only in coap_slot_coord.c:777). Tracked: b7z9.36, 7nnl.3 (spec-path dispatch child), p7mv (C). Tests: rust resources.rs:2325, python/tests/coap/test_capability_announce_resource.py (8) | high |
| R-06-325 | Payload CBOR map keys 1-6: capabilities (bitmask), prefix, prefix_len, expiry, seq, announcer_iid (§8.12, L909-931) | implemented+tested (Rust/Python; C absent) | Rust _PAYLOAD_* :17-22, decode :134-188, encode :241-272; Python :44-49, to/from_cbor :139-162. Vectors test/vectors/capability_announcements.json: payload_cbor byte-identity + sig_structure_hash asserted rust :415-429. Divergence: Rust accepts optional CBOR tag-18 wrapper :97-101, Python does not | high |
| R-06-326 | Root validation on capability-announce POST: OSCORE sender auth, kid == OSCORE sender ID, alg -65537, Sig_structure verify, announcer_iid == kid, expiry > now, seq > cached per announcer, reserved bits zero, cache by announcer_iid, 2.04 / failure 4.03 (§8.12, L949-965) | implemented+tested (Rust/Python; C absent) | Rust verify_announcement :291-323 (kid==announcer_iid :190-194; announcer pinned via OSCORE peer pubkey-derived IID :303); Python verify_capability_announcement :279-332 + resource :147-181 (pubkey from OSCORE context :156; 2.04 :179 / 4.03 :158-171 / 5.03 refusal :178). Divergences: (a) Py re-encodes the protected header (:316) instead of hashing received bytes (Rust uses received :307) — equivalent for canonical messages; (b) Py resource calls record() without egress= :172-177 so the 25% egress reserve never engages (MAY-feature omission, see R-06-327) | high |
| R-06-327 | Implementations MUST bound the capability table (recommended 256 LRU; MAY reserve up to 25% for egress-bit entries) (§8.12, L967-984) | implemented+tested (Rust/Python; C absent) | Rust capability.rs: CAPABILITY_TABLE_CAPACITY=256 :10, CAPABILITY_RESERVED=64 (25%) :11, partitioned LRU insert :62-98 / evict_lru(allow_egress) :133-139 (non-egress capped at 192, egress may use whole table; egress flag correctly derived via into_entry().is_egress()); Python CapabilityTable :40-121 (256/64 :34-35, OrderedDict LRU, purge_expired :114-121). Note: Py resource never passes egress= so the MAY reservation is inert there (not beaded — MAY). Tests: rust capability.rs:170-206, python/tests/coap/test_capability_announce_resource.py:141 | high |
| R-06-328 | Nodes SHOULD announce on joining (after EDHOC), on capability change, approaching expiry, and after root re-election (§8.12, L986-992) | not-implemented (no production callers) | Create/sign functions exist: Rust create_announcement :329-353; Python create_capability_announcement :237-276 — no trigger wiring (join/change/expiry) in any stack. Tracked: b7z9.36 | high |
| R-06-329 | Nodes MUST re-announce capabilities to the new root after detecting root change in DIO (§8.12, L1004-1008) | not-implemented | No DIO root-change hook triggers re-announcement in any stack. Tracked: project-LICHEN-worker6-99sg | high |
| R-06-330 | Root consults capability table (egress bit + prefix match) before sending tunnel-auth; no tunnel-auth for non-egress nodes (§8.12, L994-1002) | not-implemented (parameter-only glue) | C route_installed(..., egress_capable, ...) tunnel_auth.c:241 and Py route_installed(..., egress_capable=...) :321,326 require the caller to supply the flag; no stack reads a capability table to decide. Rust build_root_post has no gate. Tracked: b7z9.36 / 7nnl.3 | high |
| R-06-331 | Nodes MAY hold signed credentials: COSE_Sign1 (bstr-wrapped -65537 protected, optional x5chain header 33 per RFC 9360), payload keys 1-5 (subject-iid, claim-type, claim-value, expiry, seq) (§8.13, L1020-1049) | not-implemented (MAY — tracked) | No credential codec or x5chain support in any stack (x5chain appears only in spec text); no node_credentials vectors. Sibling envelopes (tunnel-auth, delegation tokens) share alg/-65537 machinery. Tracked: project-LICHEN-worker6-bedw epic (+ children .1-.10 incl. .8 C struct/CBOR/x5chain, .9 vectors, .10 cross-validation) | high |
| R-06-332 | Claim namespaces lichen:/oidc:/vcard:/jwt:/URI; LICHEN claims lichen:fleet, lichen:gateway_access, lichen:egress, lichen:relay, lichen:emergency, lichen:emergency_callback, lichen:role (§8.13, L1051-1071) | not-implemented | Repo-wide grep: zero code references to any claim string (spec only). Tracked: bedw | high |
| R-06-333 | Credential verification: alg -65537, x5chain → trust anchor, else issuer pubkey by kid, sig per RFC 9052, subject-iid == presenting node, expiry > now, seq > cached (§8.13, L1088-1096) | not-implemented | No credential verifier anywhere; closest analogue is tunnel-auth verify chain (tunnel_auth.c:337-347). No x5chain/chain-validation code. Tracked: bedw | high |
| R-06-334 | Revocation = supersede with higher seq; no explicit revocation; short expiry 7-30 days; CRL caching MAY (§8.13, L1098-1102) | not-implemented | No 7/30-day TTL constants; expiry/seq-floor patterns exist only in sibling mechanisms (delegation tokens, tunnel-auth). Tracked: bedw | high |
| R-06-335 | Presentation: GET /.well-known/auth → 4.01 with required-claim info; POST credential → 2.04, cached for session (§8.13, L1104-1117) | not-implemented | .well-known/auth has zero hits repo-wide (existing endpoints: core, edhoc, tunnel-auth, capability-announce, lichen-gw/*). Tracked: bedw | high |
| R-06-336 | Gateway issues local facts: POST /.well-known/local-fact (OSCORE) with {"request": ...} → 2.01 Created with COSE_Sign1 fact; gateway policy out of band (§8.13.1, L1119-1137) | not-implemented (MAY-level; tracked) | local-fact / local_fact appear only in spec text; no endpoint, no issuance code. Tracked: bedw (credential machinery covers the fact format) | high |
| R-06-337 | Local fact claims: lichen:emergency, lichen:emergency_callback, lichen:relay, lichen:priority, lichen:channel, lichen:quota, lichen:sponsored (§8.13.1, L1139-1149) | not-implemented | Zero code references (repo-wide grep). Tracked: bedw | high |
| R-06-338 | Nodes without the lichen:emergency fact SHOULD warn users that emergency services are unavailable via this mesh (§8.13.1, L1158-1160) | not-implemented (SHOULD; depends on R-06-337) | No warning UI; emergency CoAP resources implement spec-18 SOS relaying only (python/src/lichen/coap/resources/emergency.py) with no lichen:emergency fact check. Not beaded: SHOULD whose prerequisite feature (facts) is already tracked under bedw | high |
| R-06-339 | Federation facts MAY be co-signed by multiple gateways (COSE_Sign with multiple COSE_Signature entries); verifiers accept any trusted federated gateway (§8.13.1, L1166-1178) | not-implemented | All COSE codecs in all stacks are COSE_Sign1-only; no multi-signature support | high |
| R-06-340 | Local facts are mesh-lifetime: gateway restart or root re-election invalidates cached facts; nodes re-request (§8.13.1, L1180-1183) | not-implemented (no fact cache exists) | Follows from R-06-336/337 absence; tracked: bedw | high |
| R-06-341 | Private keys MUST be stored in hardware secure element (preferred) or flash with readout protection; never transmitted over the air (§15.2, L1211-1216) | divergent (never-over-the-air enforced; storage is plain flash; no secure element or readout protection anywhere) | Never-TX: frame builders consume pubkey/epoch/seq only (no privkey TX path found in any stack). Storage: C lichen/subsys/lichen/app_identity/identity_store.c (LIKY record, CRC, mutex) + rollback authority identity_store.h:40-104; Rust rust/lichen-gateway/src/bin/lichend.rs:166-236 (Zeroizing seed, abort on unreadable/persist-fail) + rust/lichen-hal/src/storage.rs NV; Python python/src/lichen/crypto/key_persistence.py (0600 two-slot crash-safe; docstring cites spec 15.2 verbatim). No SE/ATECC/RDP support (grep zero hits). Threat-model tension (project excludes physical attackers) → human-labeled bead filed: project-LICHEN-worker6-b7z9.87 | low |
| R-06-342 | Link replay: receivers track per-sender (epoch,seqnum) with a 32-entry sliding window; epoch persisted to flash; increments on wrap or reboot (§15.3, L1218-1230) | implemented+tested (divergent detail: epochs never wrap — 255 exhausts fail-closed, all stacks) | 32-entry window: C replay.h:59-64 (u32 bitmap) + replay.c:30 + per-peer table fail-closed; Rust replay.rs:39-75 (u32 bitmap, from_persistent_parts) + link_layer.rs:377/:423/:839-844; Python replay.py:40 WINDOW_SIZE=32. Epoch persistence: C replay_persist.c (:464 TX-epoch reserve) + replay_settings.c; Rust lichend.rs:211-236 (cold/uninit→128, 128-254→+1, 255→fail "rotate identity", persist-fail→abort) + hal/storage.rs:317-326; Python epoch_store.py:48 (cold→128, EpochExhaustedError at 255). Tests: lichen/tests/replay/main.c (4) + lichen/tests/replay_persist (wrap 253→255, corruption fail-closed, ztest testcase.yaml), rust shared_vectors.rs test_replay_window_vectors + epoch_serial_vectors.rs + in-file :1584/:1619, python/tests/link/test_replay.py + test_epoch_store.py; vectors replay_window.json, epoch_rollover.json. Spec says "increments on wrap" but all stacks treat 255 as exhausted → fail closed (stricter, consistent) — spec-text bead filed: project-LICHEN-worker6-b7z9.86 | high |
| R-06-343 | No address randomization / privacy extensions (RFC 4941, RFC 7217): IIDs and 02xx addresses stable and key-derived (SHA-512 IID; short address via crc32_ieee(EUI-64, key=LICHEN)) (§15.5, L1284-1306) | implemented+tested | Rust iid_from_pubkey_bytes rust/lichen-core/src/addr.rs:86 (SHA-512[0:8], U/L cleared) + ygg_addr_from_pubkey; short_addr.rs:23 LICHEN_CRC32_INITIAL=0x4348_454e ("LICHEN"), :61 crc32_ieee, :80 short-addr derivation; C identity_addr.c:28 (lichen_key_pubkey_to_iid) + ipv6_addr.c:45 + ipv6_addr.h:111; Python short_addr.py:46/_LICHEN_KEY, :98, :124-127 + crypto/identity.py yggdrasil_address. No rfc4941/RFC 7217/privacy-extension/randomization code anywhere (grep zero hits). Vectors: ipv6-addresses.json, node-addresses.json, link-addressing.json, yggdrasil_address.json + rust/lichen-core/tests/{short_addr_vectors,yggdrasil_addr_vectors,ipv6_address_vectors}.rs | high |

**Not swept as numbered requirements (informational):**
- §15.1 Cryptographic Strength — table of primitive levels; exercised via test/vectors/schnorr48.json, oscore.json, HKDF inside OSCORE context tests. No implementation requirement.
- §15.4 Known Limitations — accurately describes implemented state (static ECDH ⇒ no PFS; 48-byte truncated sigs = 128-bit; no anti-jamming; link headers unencrypted).
- §15.6 Recommendations — deployment guidance (rotate keys, use OSCORE, RPL secure mode in adversarial environments, monitor routing anomalies); no code artifact. Note item 3 ("Enable RPL secure mode") is currently unactionable — see R-06-302.
- §8.13.2 CA Credentials (portable) — descriptive coexistence text; no normative sentences.

**Overflow note:** gap-bead cap not reached — 3 beads filed: project-LICHEN-worker6-b7z9.88 (root-sig residuals), b7z9.86 (spec 15.3 epoch wording), b7z9.87 ([human] spec 15.2 key storage); all other divergences/not-implemented items are already tracked by open beads b7z9.35, b7z9.36 (+children), bedw (+children) and are cited in the rows.
