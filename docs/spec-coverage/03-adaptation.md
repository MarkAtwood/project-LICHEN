## spec/03-adaptation.md — coverage (sweep 2026-09-09)

77 requirements extracted. This matrix supersedes a lost prior 03-adaptation
matrix (only its verification log `03-adaptation-verify.log` survives);
known old→new ID mappings are in `03-adaptation-flagged.md`. Rule Set
Version 3 is the only operational registry; per the settled
`upstream-yggdrasil-addressing` decision, node /128s live in `0200::/8`
(all three stacks and this spec section agree on first-byte `0x02` MSB(8)
for Rules 1/6 today — tension with open bead `project-LICHEN-worker6-0t11`
is flagged, not adjudicated here). Per-stack qualifiers appear in Status;
gap beads: **4 new filed** (`worker6` sweep children), 0 overflow (cap 10);
6 divergent/not-implemented rows resolve to pre-existing open beads
(b7z9.68, z4m8, b7z9.5.2/.2.2, b7z9.5, l1qw.3.7.6.x, b7z9.88, pgsl) — no
duplicates filed. Repo caveat: `rust/lichen-node/src/rpl_stack/` has
unresolved merge conflicts (UU receive.rs/mod.rs etc.) in the working tree;
rpl_stack evidence below cites conflict-free regions. Rust's
adaptation-vector runner skips `port_boundary`/`compressed_size` categories
(adaptation_vectors.rs:1511-1518); equivalent Rust coverage exists inline.

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-03-001 | §5.1: Zephyr integration requires custom L2 driver/network interface; cannot use CONFIG_NET_L2_IEEE802154 | implemented+untested | rg CONFIG_NET_L2_IEEE802154 over lichen/ = 0 matches; custom L2 at lichen/subsys/lichen/l2/ (lichen_l2_*.c behind LICHEN_LORA_L2/LICHEN_L2 Kconfig). L2 exercised by lichen/tests/ping_l2 etc., but no test asserts the 802.15.4 absence (architectural) | high |
| R-03-002 | §5.3: Rule 0 fixed compressed header 23 bytes (uncompressed base 52) | implemented+tested | Byte-exact: Rs vector_coap_linklocal codec.rs:2515 + packet_walkthrough.json schc_layer_matches_rule0 (lichen-node/tests/packet_walkthrough_vectors.rs:182); Py test_round_trip_compresses_real_coap_packet (test_headers.py:79); C tests/schc + schc_rule*_compress; vectors schc_compression.json | high |
| R-03-003 | §5.3: Rule 1 fixed compressed header 37 bytes (uncompressed base 52) | implemented+tested | Rs vector_coap_global codec.rs:2528 (residue 36+1); Py test_rule1 vectors coap_global (test_vectors.py:742); C schc_rule1_compress; schc_compression.json | high |
| R-03-004 | §5.3: Rule 3 DIO fixed compressed header 40 bytes (base 68) | implemented+tested | Rs vector_rpl_dio codec.rs:2553 (residue 312 bits=39+1); Py test_rule3.py:85/125; C schc rule3 gate 40u+tail (schc_compress.c:308); schc_compression.json rpl_dio | high |
| R-03-005 | §5.3: Rule 4 DAO-with-DODAGID fixed compressed header 37 bytes (base 64) | implemented+tested | Rs vector_rpl_dao codec.rs:2593 (288 bits=36+1); Py test_rule4.py:80/116; C 37u+tail (schc_compress.c:359); schc_compression.json rpl_dao | high |
| R-03-006 | §5.5: Rule 2 ICMPv6 Echo 23-byte fixed compressed header | implemented+tested | Rs vector_icmpv6_echo codec.rs:2541 (RESIDUE_BITS=176, codec.rs:881); Py Icmpv6EchoProfile headers.py:852-905 + test_rule2.py:83; C 23u+tail (schc_compress.c:257); vectors | high |
| R-03-007 | §5.5: Rule 7 compressed size 21 B (two link-local) / 37 B (full addresses) incl. Rule ID + padding, excl. payload | implemented+tested | Rs mqtt_sn_round_trip_linklocal codec.rs:2605 / mqtt_sn_global_addresses codec.rs:3360; Py MqttSnProfile residue 20/36 B (headers.py:66-67) + test_headers.py:118/133; C compress_mqtt_sn residue_len 20/36 (schc_compress.c:418-420) + tests/schc_mqtt_sn; schc_compression.json mqtt_sn_* (14) | high |
| R-03-008 | §5.5: Rules 0-6+255 in generic descriptor registries; Rule 7 specialized, only codec/headers, absent from registries yet canonical wire ID | implemented+tested | Rs RULE_SET_V3 order [5,6,0,1,2,3,4,255] rules.rs:467-478 + doc "intentionally absent" rules.rs:464-466 + compress_mqtt_sn/decompress_mqtt_sn codec.rs:1049/1448; version_three_registry_has_canonical_membership_and_order rules.rs:557. Py _RULES_V3 rules.py:503-513 + MqttSnProfile. C rule table schc.c:470-526 incl. SCHC_RULE_MQTT_SN | high |
| R-03-009 | §5.5: Rules 64/65/66 are reusable descriptor building blocks only, not wire Rule IDs, not default-context selectable; MUST be rejected as unknown if received as packet Rule ID | implemented+tested | Py COAP_RULE(64)/UDP_PORT_RULE(65)/ICMPV6_ECHO_RULE(66) rules.py:272-304, excluded from _RULES_V3, unknown-ID raise context.py:129-131 + headers.py:1007; test_rule_id_format.py:60-79. Rs registry-exclusion assert rules.rs:564-566 + UnknownRuleId fallthrough codec.rs:2411 + adaptation rejection vectors 0xFE/0x10/0x77 (adaptation_vectors.rs:602). C table has no 64-66. Hygiene: no direct wire vector for 0x40-0x42 (path-equivalent coverage only) | high |
| R-03-010 | §5.5: Rule 255 uncompressed fallback — MUST implement | implemented+tested | Rs encode_rule255/decode_rule255 codec.rs:1532/1556; Py encode_rule255/decode_rule255 headers.py:260/284; C schc.c Rule 255 path + tests/schc negative suite; 12 fallback vectors in schc_compression.json consumed by all three | high |
| R-03-011 | §5.5: Rule 0 matches fe80::/64 MSB and sends two 64-bit LSB IIDs | implemented+tested | Rs FD_IPV6_SRC/DST_LL TV 0xfe80:: MSB(64)/LSB(64) rules.rs:106,154-171; Py _LINK_LOCAL_PREFIX_TV rules.py:326 mo_arg 64; C is_canonical_link_local gate schc_compress.c:170-171; vectors | high |
| R-03-012 | §5.5: Rule 1 matches 0200::/8 MSB and sends two 120-bit LSBs | implemented+tested | Rs GLOBAL_PREFIX_TV=0x0200<<120 MSB(8) rules.rs:107,174-191 + codec first-byte 0x02 gate codec.rs:782 + YGGDRASIL_PREFIX decompress codec.rs:1176; Py _GLOBAL_PREFIX_TV=0x0200<<112 rules.py:327 + profile gates headers.py:551/596; C src[0]!=0x02 gate schc_compress.c:96-100 + schc_decompress.c:32-37. All three + spec agree 0200::/8 today; see flagged F1 (bead 0t11 tension with /7 aggregate reading) | low — flagged |
| R-03-013 | §5.5: Rules 5/6 are OSCORE-protected CoAP variants | implemented+tested | Rs LINK_LOCAL/GLOBAL_OSCORE_RULE rules.rs:459-461, selection by OSCORE option codec.rs:2303-2331, cross-check codec.rs:1230-1243; rule5/6_compression+decompression.rs, oscore_roundtrip.rs:190 vs oscore_schc_roundtrip.json. Py rules.py:488-495 + test_rule5/6*. Py/C per-rule tests + C schc_rule5/6 tests | high |
| R-03-014 | §5.5: Port compression MSB(12)/LSB(4) covers 5680-5695 in Rules 0/1; Hop Limit is value-sent | implemented+tested | Rs FD_UDP_*_PORT TV 5683 Msb(12) rules.rs:214-217 + expected_msb check codec.rs:799-808; boundary tests rule5_decompression.rs:58-60 + port_dispatch.rs:286-292; Py _udp_fields rules.py:363-366 + port_boundary vectors (Py-run; Rust skips category, inline coverage instead); C gate (5683u>>4) schc_compress.c:97-98/172-173. Hop limit value-sent: Rs FD_IPV6_HOP_LIMIT rules.rs:143-151 + write codec.rs:824 | high |
| R-03-015 | §5.5: Rules 3/4 match fe80::/64 src+dst carrying two 64-bit IIDs; multicast DIOs (ff02::1a) MUST use fully validated Rule 255; decoder MUST NOT reinterpret multicast as fe80::1a | implemented+tested | Rs link-local-both gate codec.rs:2354; decoder always rebuilds fe80::/64 codec.rs:1350-1351; multicast DIO via Rule 255 stack.rs:502-522; authenticated_dio_vectors.rs:165/206/331 (ff02::1a canonical). Py profile link-local matches headers.py (RplDio/DaoProfile) + admission tests. C is_canonical_link_local gates. Gap: no explicit ff02::1a-destination DIO compression vector (agent-audited) | high |
| R-03-016 | §5.5: Rule 4 does not match a ULA, Yggdrasil, or other routable source address | implemented+tested | Rs authoritative codec enforces link-local-both codec.rs:2339; Py RplDaoProfile requires link-local headers.py:819-825; C gate. Soft spots (non-authoritative): Rs headers.rs:893 RplDaoProfile::matches uses is_routable (context-selection only); Py _is_ula/_is_routable helpers headers.py:254-259 uncalled/untested. Prior Opus verify F8: hygiene, not correctness | high |
| R-03-017 | §5.5: kd_flags byte bit7=K, bit6=D; Rule 4 = DAO with D=1 + DODAGID present; DAOs without DODAGID fall back to Rule 255 | implemented+tested | Rs D-bit gate kd_flags&0x40 codec.rs:996/2362-2372 + decompress re-check codec.rs:1402-1406 + full kd_flags byte VALUE_SENT (FD_RPL_KD_FLAGS rules.rs:302); Py headers.py:819-838; vector rpl_dao_no_dodagid_nonmatch (category fallback) all three consumers; C D-bit path | high |
| R-03-018 | §5.5 Rule 3/4 table: ICMPv6.type TV 155 equal/not-sent; checksum ignore/compute | implemented+tested | Rs FD_ICMPV6_RPL_TYPE TV 155 rules.rs:269-270 + dispatch codec.rs:2354 + compute-on-decode codec.rs:1376-1382; FD_ICMPV6_CHECKSUM rules.rs:240-248; Py same; C schc_decompress computes; schc_compression.json rpl_dio/dao round-trips | high |
| R-03-019 | §5.5: every RPL option octet after DIO/DAO base appended verbatim; v3 applies no MATCH_MAPPING or other compression to RPL options (PIO type 8, Target, Transit intact) | implemented+tested | Rs tail verbatim codec.rs:935/976/1001/1040 + no MatchMapping in any descriptor; Py _DIO_OPTION_FIELDS doc-only rules.py:431-436 + test_dio_options_travel_as_tail (test_headers_rules.py:155); C copies tail; C schc_helpers validate walks don't consume options | high |
| R-03-020 | §5.5 Rules 1/6: both addresses MSB(8)/LSB(120) vs 0200::/8; remaining 120 bits in residue; no provisioned ULA or arbitrary mesh /64 context | implemented+tested | Same sites as R-03-012; Py docstring "no provisioned ULA" headers.py:22-23 + test_rule1_nonmatches_use_validated_rule255 (test_rule1.py:141) + test_03xx_is_not_a_native_profile_address (test_headers.py:379); Rs rule6_requires_both_current_yggdrasil_prefixes (rule6_compression.rs:142). Flagged F1 applies | low — flagged |
| R-03-021 | §5.5 Rule 7: matches UDP with ≥1 endpoint port 10883; compressor MUST carry the other port; MUST NOT assume both are 10883 | implemented+tested | Rs port check codec.rs:1077-1080 + direction/other_port codec.rs:1095-1101; Py headers.py:607-618; C schc_compress.c:397-399 + 439-444; vectors both-ports byte-exact (Rs inline codec.rs:2661; Py test; C schc_mqtt_sn); port_10883_mqttsn_exact vector (Py-run) | high |
| R-03-022 | §5.5 Rule 7: before selecting, compressor MUST verify all elided/recomputed fields (ver 6, TC 0, FL 0, NH 17, exact payload len, ≥1 port 10883, UDP len ≥8 & exact, UDP checksum nonzero + valid) | implemented+tested | Rs ensure_ipv6 + checks codec.rs:743-748, 1055-1092; test mqtt_sn_rejects_invalid_elided_ipv6_udp_fields codec.rs:3416 (6 mutations) + mqtt_sn_non_udp codec.rs:3448. Py compress_if_matching chain headers.py:620-662 + test_mqtt_sn_rule7_rejects_invalid_elided_fields (test_headers.py:141). C checks schc_compress.c:385-411 + tests | high |
| R-03-023 | §5.5 Rule 7: addresses MUST satisfy exact address policy (source usable unicast — no ::/::1/ff00::/8/v4-mapped; dest no ::/::1/v4-mapped, multicast scope 2-14 only; canonical fe80::/64 + AddressMode; zone ID never serialized) | implemented+tested | Rs validate_address_policy codec.rs:241-263 applied TX codec.rs:1067; adaptation category rule7_address_policy (14 vectors, error-token asserts adaptation_vectors.rs:994-1109). Py validate_rule7_addresses headers.py:94-113 applied headers.py:626 + tests. C validate_rule7_addresses schc_helpers.c:49 + TX call schc_compress.c:400 + RX re-check schc_decompress.c:489 | high |
| R-03-024 | §5.5 Rule 7: structural/checksum failure MUST be dropped; well-formed packet failing only Rule 7 matching MUST use another rule/255; MUST NOT normalize into a different packet | implemented+tested | Rs: NoMatchingRule falls through to 255 (codec.rs:2296-2300, 2377) while InvalidPacket propagates (buffer-atomic, mqtt_sn_compress_error_leaves_buffer_untouched codec.rs:2811); Py: same split (headers.py:980-1007). Tension: address-policy reject returns error not fallback — open bead pgsl ("un-sendable instead of degrading to Rule 255"); note Rule 255 emission policy (R-03-064) would reject the same packets, so drop may be the only conformant outcome — flagged F2 for Opus | low — flagged |
| R-03-025 | §5.5 Rule 7 residue: HopLimit 8b, AddressMode 1b, addrs 64/128b, PortDirection 1b, OtherPort 16b, MSB-first; AddressMode=0 iff both in fe80::/64 else 1 with full 128-bit addresses (no prefix compression) | implemented+tested | Rs BitWriter layout codec.rs:1113-1166 + mqtt_sn_fe80_slash10_outside_slash64_uses_full_addresses codec.rs:3398 (AddressMode bit asserted); Py layout headers.py:644-658 + tests 118/133; C layout 413-437; shared byte-exact vectors | high |
| R-03-026 | §5.5: both ports 10883 → canonical encoding PortDirection=0 + OtherPort=10883 | implemented+tested | Rs codec.rs:1095-1101 + inline byte-exact codec.rs:2661-2663; Py decoder rejects direction=1+other=10883 headers.py:694-695; C decompress reject schc_decompress.c:501-503; vector port_10883_mqttsn_exact (schc_adaptation.json, Py-consumed) | high |
| R-03-027 | §5.5: decoder MUST reject residue with policy-violating decoded address, AddressMode=1 with both decoded in fe80::/64, PortDirection=1+OtherPort=10883, or nonzero final padding bits | implemented+tested | Rs decompress_mqtt_sn codec.rs:1470-1489 (all four) + test mqtt_sn_rejects_noncanonical_residues codec.rs:3539; Py headers.py:676-695 + test_mqtt_sn_rule7_rejects_noncanonical_residue_forms (test_headers.py:163); C schc_decompress.c:489-512 | high |
| R-03-028 | §5.5: decoder MUST validate IPv6/UDP lengths, recompute UDP checksum and install before delivery; computed zero serialized 0xffff, 0x0000 never canonical; compressor validates checksum before eliding | implemented+tested | Rs UDP length derive codec.rs:1499-1503, checksum recompute+install :1504-1516, ones_complement_sum 0→0xFFFF codec.rs:623-634 + test mqtt_sn_checksum_zero_is_serialized_as_ffff codec.rs:3585; Py UdpDatagram recompute headers.py:703-704 + zero→0xFFFF ipv6/udp.py:76-77 + test (test_headers.py:191); C recomputes in decompress (all rules) — 0xffff serialization not separately evidenced in C | high |
| R-03-029 | §5.6: compression sublayer MUST zero-pad compressed header through next octet boundary before byte-aligned payload | implemented+tested | Rs BitWriter div_ceil + zero-fill codec.rs:143-168 + debug_asserts + RX padding_is_zero enforcement codec.rs:205-212 (call sites :1204/1281/1342/1408/1485); Py codec.py:45-52 + test_decompress_rejects_nonzero_padding (test_codec.py:41); C bitstream.c memset + per-rule padding checks schc_decompress.c; padding vectors + rule1/rule5/rule6 nonzero-padding tests | high |
| R-03-030 | §5.6: fragment header RuleID 8b / W 1b / FCN 6b; Rule IDs 0x78 A→B, 0x79 B→A; W absolute 0/1 no wrapping; FCN regular 62..0, 63 = All-1 | implemented+tested | Rs RULE_ID_A_TO_B/B_TO_A fragment.rs:41-42, FRAGMENT_M/N/T=1/6/0 fragment.rs:13-15, W∈{0,1} fragment.rs:1471-1473, FCN map fragment.rs:2033; Py fragment.py:26-29,327-355; C SCHC_FRAGMENT_RULE_A_TO_B/B_TO_A (schc.h:123-124), All-1 0x3f (schc.h:128); vectors: schc_adaptation.json fragmentation_endpoint_direction ×6 (Rs adaptation_vectors.rs:292 + Py test_fragment.py:2243/3019), schc_fragmentation.json (C-generated headers + Rs/Py) | high |
| R-03-031 | §5.6: WINDOW_SIZE 63 tiles, ≤126 tiles/two windows; tile 179 B except shorter non-empty final; RCS CRC-32/ISO-HDLC before final tile in All-1; packing MSB-first bit-contiguous, zero padding only at message end | implemented+tested | Rs WINDOW_SIZE=63 fragment.rs:30, MAX_PACKET_SIZE=2*63*179 fragment.rs:36, TILE_SIZE=179, compute_mic CRC-32 fragment.rs:1436-1449, write_to/from_bytes NonZeroPadding fragment.rs:1469-1540; fragment_generation.rs:18/51/127 + schc_tile_sizing.json + schc_fragmentation.json. Py fragment.py:27-39, compute_mic zlib.crc32(payload+b"\0") fragment.py:280-282 + test_fragment.py:376/390/554. C crc32_iso_hdlc poly 0xEDB88320 + write_be32 (subsys/schc/schc.c:171-204) + C tests generate from schc_fragmentation.json | high |
| R-03-032 | §5.6: endpoint A = lexicographically smaller canonical 32-byte link-signing pubkey (unsigned octets); equal keys MUST be rejected; direction MUST derive+validate from authenticated full signer identities (EUI-64/untrusted/caller default insufficient) | implemented+tested | Rs canonical_fragmentation_rule fragment.rs:134-152 (Equal → InvalidPeerEvidence) + PeerContextAuthority permits fragment.rs:416-458; test full_signer_order_derives_both_directions_and_rejects_self_sessions fragment.rs:3391. Py fragmentation_rule_for_sender fragment.py:64-77 (equal rejected) + test_fragment.py:416-422 + link-derives-direction test_fragment.py:2243. C lichen/schc_session.c:26-86 + tests/schc_session (A/B roles, equal-key reject) | high |
| R-03-033 | §5.6: no ACK for All-0; MUST respond to All-1/ACK REQ with C=1 after successful whole-packet verification or C=0 + RFC 8724 compressed bitmap (1=received, 0=missing) | implemented+tested | Rs regular → no response fragment.rs:3103-3113; All-1 finalize C=1/C=0+bitmap fragment.rs:3174-3220; reassembly_state_machine.rs:44/120/139. Py reassembly.py:166-215, 240-292 + test_reassembly.py:54/92/127 + ack_bitmap vectors. C receiver MIC handling subsys/schc/schc.c:933-953 + tests/schc_reassembly | high |
| R-03-034 | §5.6: two-octet ACK REQ can be bit-identical to compressed C=0 ACK; MUST classify from authenticated sender role + directional Rule ID before invoking ACK decoder or mutating sender state | implemented+tested | Rs is_sender_facing_control fragment.rs:1764-1780 + ingress classify before decode fragment.rs:3030-3040, 2109-2151; tests fragmentation.rs:283/320, fragment.rs:4058. Py fragmentation_message_is_response fragment.py:80-106 applied session_manager.py:665-684 + schc_handler.py:520-534; test_fragment.py:2986/2961/2929. C: engine keeps separate sender/receiver contexts + ACK canonical re-encode check schc.c:538-543; bit-identical-ambiguity classification is a property of the unwired shared ingress — C evidence engine-level only | high |
| R-03-035 | §5.6: MAX_ACK_REQUESTS=4 — initial All-1 + at most three later All-1/ACK REQ emissions | implemented+tested | Rs attempts latch fragment.rs:1966, abort at ≥4 fragment.rs:2277-2300; test authenticated_fixed_retransmission_timer_is_exact_and_bounded fragment.rs:3886 (10s..40s deadlines) + fragmentation_vectors.rs:454. Py MAX_ACK_REQUESTS=4 fragment.py:42 + budget exhaustion tests test_fragment.py:742/759/1003. C SCHC_FRAGMENT_MAX_ATTEMPTS 4 (schc.h:133) enforced 715/791/808 | high |
| R-03-036 | §5.6: receiver emits at most four ACK responses per session; fifth otherwise-valid All-1/ACK REQ → Receiver-Abort + terminal hold-down | implemented+tested | Rs respond aborts at 4 fragment.rs:3222-3225 (+ tombstone via release); fragmentation_vectors.rs:503-516 (4 OK, 5th → abort). Py _respond reassembly.py:166-175 → _abort + tombstone reassembly.py:693-701; test_reassembly.py:136. C receiver_queue_ack aborts at 866-869 (attempts++ :1205) | high |
| R-03-037 | §5.6: receivers MUST support 1281-byte SCHC Packets (1280 IPv6 + Rule ID); larger optional up to 22,554 ceiling | implemented+tested | Rs MAX_PACKET_SIZE=22554 fragment.rs:32-36 + receiver clamp fragment.rs:2968; test receiver_capacity_bounds_are_enforced_during_reassembly reassembly_state_machine.rs:179 (explicit 1281 boundary) + fragmentation_vectors.rs:536. Py MAX_SCHC_PACKET=1281 / DEFAULT_RECEIVER_LIMIT fragment.py:40-41 + _validate_max_size reassembly.py:45-53 + test_reassembly.py:70/228/254. C tile math + capacity tests | high |
| R-03-038 | §5.6: T=0 (DTag absent, only one possible value); sender MUST NOT have >1 fragmented SCHC Packet active per (authenticated link identities, directional Rule ID) | implemented+tested | Rs FRAGMENT_T=0 fragment.rs:15 + no DTag slot in layout fragment.rs:1494 + single slot per (signer,rule) → SessionBusy fragment.rs:615-619 + hold-down :627-630; tests fragment_generation.rs:18, fragment.rs:3513/3539. Py test_profile_has_no_dtag_field test_ack.py:157 + single_active vector + registry fail-closed test_fragment.py:1053. C engine single session per context + static asserts schc.c:1240-1244 | high |
| R-03-039 | §5.6: reassembly context key = (local_identity, remote_signer_identity, remote_key_generation, fragmentation_rule_id); live local-key replacement MUST additionally bind opaque local_key_generation; fixed-local-key owner MAY persist under full local key | implemented+tested | Rs binds local signer (PeerContextAuthority::new) + key_generation/durable_key_generation per entry + per-direction slots fragment.rs:407-413, 506-535; std path additionally binds ReceivingLinkIdentity instance token fragment.rs:237,374-384 (retired on reinstall — test same_public_key_reinstall_gets_distinct_fragmentation_owner fragment.rs:4367). Py contexts keyed by remote signer+generation reassembly.py + persistence export/restore. C session_table keyed (local, remote, generation, rule_id) schc_session_table.c. Nuance: spec's conditional local_key_generation binding is realized as an instance/retirement token in Rs and is thin in Py — flagged F3 | low — flagged |
| R-03-040 | §5.6: fragment MUST be authenticated + pass link replay before it can allocate/reset/abort/mutate a fragmentation context; receiver MUST revalidate every applicable generation token before every context mutation or control response | implemented+tested | Rs receive_frame/receive_link_verified gate before receive_verified fragment.rs:2812-2908 + constructors check accepts_current :2758-2765; tests authenticated_receiver_rejects_replay_before_state_mutation fragment.rs:3979, 3746, 3791. Py reassembly reachable only via link-verified RxFrame receipts schc_handler.py:484-497 + node.py:970-984. C: identity/replay/generation wiring is the open epic l1qw.3.7.6 (session authority exists, no production caller) | high |
| R-03-041 | §5.6: MUST bound contexts per remote signer ≤4 and global ≤64; on authenticated exhaustion send one Receiver-Abort and MUST NOT evict/mutate active contexts; unauthenticated fragment silently dropped, no control response | implemented+tested | Rs 4/signer (2 sender + 2 receiver slots fragment.rs:194-195), node global FragmentationPolicy<8> ⇒ 32 ≤ 64 stack.rs:234; PolicyFull no-evict fragment.rs:477-479; ReceiverAllocationRejected{ReceiverAbort} :1123-1132; unauth → InvalidPeerEvidence, no ReceiverResult; test fragment.rs:3955 + link_layer_integration.rs:688. Py DEFAULT_MAX_CONTEXTS=4 global / 2 per-signer (stricter, conformant) reassembly.py:39-40 + fail-closed no-evict tests test_reassembly.py:184, test_fragment.py:3136; unauth silent via verified-receipt gate. C session_table exact 4/64 fail-closed schc_session_table.c:66-99 + tests/schc_session_table | high |
| R-03-042 | §5.6: unsigned 24-bit link replay counter (Epoch+SeqNum); context tracks session-wide greatest accepted counter, ordinary unsigned ordering, no wrap | implemented+tested | Rs (epoch<<16)\|seqnum fragment.rs:208,2168,2700,2819 + high-water max/reject fragment.rs:794,841-843 + link logical_counter "0xFFFFFF last valid tuple" lichen-link/src/seqnum.rs:121-126 + TX exhaust latch stack.rs:362-377 (test :1047/1125, sfn_wrap_vectors). Py MAX_LINK_REPLAY_COUNTER=0xFF_FFFF fragment.py:43 + baseline check session_manager.py:512-514 + test_fragment.py:1039. **C: not-implemented** — high_water is uint32_t, no 24-bit mask, no counter in reassembler (open bead l1qw.3.7.6.4) | not-implemented (C; Rs/Py tested) |
| R-03-043 | §5.6: duplicate tile coordinate + identical bytes → idempotent discard without clearing/replacing/resetting stored tile, MUST still advance session-wide high-water; same coordinate different bytes MUST fail closed, MUST NOT reset context | implemented+tested | Rs identical → early return keeping tile, counter recorded fragment.rs:3105-3109, 2887; conflicting → abort :3106-3108; tests reassembly_state_machine.rs:44/74, fragmentation.rs:79. Py reassembly.py:258-263 + high_water advance :685; test_fragment.py:2519, test_reassembly.py:147. C duplicate identical → ignore / conflicting → Receiver-Abort schc.c:1156-1164. Thin: Rs high-water-advance-on-duplicate has code, no isolated test | high |
| R-03-044 | §5.6: after completion or authenticated abort, receiver MUST retain terminal tombstone (high-water, outcome, final ACK); 60 s hold-down — repeated authenticated All-1/ACK REQ with strictly newer counter gets same terminal response idempotently, never starts a session; every authenticated late message advances tombstone high-water | implemented+tested | Rs FRAGMENT_TOMBSTONE_MILLIS=60_000 fragment.rs:40 + replay_receiver_terminal fragment.rs:827-866 (idempotent + high-water bump :860-864); tests fragment.rs:4058 (tombstones once, 2→3→4), 3659, 4206. Py SESSION_HOLD_DOWN_SECONDS=60 fragment.py:45 + receiver tombstones reassembly.py:402-408,503-524 + session_manager hold-down. **C: not-implemented** — no 60 s hold-down enforcement (SCHC_INACTIVITY_TIMEOUT_S defined but dead, lichen/schc.h:109); tombstone table exists unwired (open bead l1qw.3.7.6.5; related open bead bsul: Py malformed-first-fragment edge) | not-implemented (C; Rs/Py tested) |
| R-03-045 | §5.6: after hold-down, generation-scoped high-water remains admission floor; only canonical first Regular Fragment (W=0, FCN=62) with strictly greater counter starts next session; All-1 never opens (one-tile packet MUST be sent unfragmented); opening counter immutable; every later fragment/control strictly greater; ACK REQ/aborts/All-1/other FCN never create session; expiry without terminal → hold-down with high-water but no cached success ACK; late fragments MUST NOT create/contaminate next session | implemented+tested | Rs is_receiver_session_opener fragment.rs:1563-1571 enforced fragment.rs:2830-2862; floor from tombstone :663-679 immutable via counter<=high; expire_receiver_session → ReceiverAbort tombstone, no success ACK :1200-1218; tests fragment.rs:4206 (opener counter 6 > floor accepted; All-1 aborted), 3659, 3539. Py _is_session_opener reassembly.py:486-497 (W=0 FCN=62 only) + floors :456,517-524 + schc_session_security.json all1_may_open=false asserted test_fragment.py:2578. **C: not-implemented** — floor storage only, never consulted in any RX path (l1qw.3.7.6). Sub-point "one-tile packet sent without fragmentation": no direct sender-side evidence in any stack (Rs TX fragmentation itself unwired, b7z9.5) — flagged F4 | not-implemented (C; Rs/Py tested) |
| R-03-046 | §5.6: tombstones/floors/hold-down records are bounded state persisted with the authenticated trust/replay transaction; link-key rotation invalidates all such state for the old generation; counter 0xffffff cannot be reused or wrapped under the same key | implemented+tested | Rs persist_receiver_floor/restore fragment.rs:874-993 (signed by local identity, revision anti-rollback, durable-generation check) persisted in same transaction as link.persist_peer_trust_state (test :4529-4543); rotation rejects restored floor (test :4690-4707); 0xFFFFFF exhaust stack.rs:362-377. Py export/restore persistence with replay consistency session_manager.py:577-655 + reassembly.py:768-919; exhaustion test_fragment.py:1039; floor round-trip :2742. C: authority generation invalidation exists (schc_authority.c + tests/schc_authority) but persistence/wiring absent (l1qw.3.7.6) | high |
| R-03-047 | §5.6: tombstone and admission-floor tables MUST be bounded ≤256 entries per direction; on overflow MUST evict oldest tombstone by terminal-time | divergent | C session_table: 256 tombstones with oldest-terminal_time eviction + 256 floors with lowest-high_water eviction schc_session_table.c:102-235 (tests/schc_session_table). Rs: per-signer [Option<TerminalTombstone>;2]/direction fragment.rs:196-197 — bounded far below 256 but no 256-entry table and no evict-oldest (time expiry + PolicyFull instead). Py: MAX_SENDER_SESSION_RECORDS=64 etc. fail-closed, expiry-deleted only fragment.py:46-48 + session_manager.py:394-418. Prior verify F2 confirmed. **New bead filed** (G3) | high |
| R-03-048 | §5.6: fragmentation is hop-by-hop; routers reassemble and decompress before IPv6 forwarding | not-implemented | Py node.py:936-1014 reassembles via link.accept_authenticated_schc_fragment before router.route (:1069) and FORWARD re-compress (:1074-1103) — implemented+tested (test_node.py:1788, test_fragment.py:2276). **Rs node: RX seam unwired** — Stack::receive goes link-verify → decompress only, fragment wires rejected (link.rs:199-203); open beads b7z9.5.2 + child b7z9.5.2.2; TX never invokes fragmentation (b7z9.5, gateway b7z9.5.5). **C: engine complete, no production caller** (l1qw.3.7.6) | not-implemented (Rs node, C; Py tested) |
| R-03-049 | §5.6: "Larger buffers optional up to the 22,554-byte encoded SCHC Packet profile ceiling… compressible raw IPv6 packet may be larger when its final encoded SCHC Packet remains ≤22,554" | divergent | Contradicts §5.7 raw-bound ceiling (R-03-067): all three implementations enforce the 5.7 reading (Rs compress raw-gate before dispatch codec.rs:2263-2275; Py headers.py:937-940; C per-rule raw gates schc_compress.c:119/189/257/308/359/416). Spec-internal contradiction; open bead **b7z9.68** (rewrite 5.6 sentence to raw-bound reading). Referenced, not re-beaded | high |
| R-03-050 | §5.7: rule set version is 8-bit; table: 0 reserved, 1 legacy, 2 RFC 8724 profile, 3 canonical Rule 7 residue; v3 the only operational registry | implemented+tested | Rs RULE_SET_VERSION=3 + history doc rules.rs:19-26 + only_version_three_has_a_local_registry rules.rs:624; Py rules.py:189; C LICHEN_SCHC_RULE_SET_VERSION 3 (rpl_messages.h:132); rule_versioning.json registry vectors (Rs rule_versioning_vectors.rs:336 + Py test_vectors.py:839) | high |
| R-03-051 | §5.7: DIO Rule Version Option Type 0x13 is project-local reserved; implementations MUST NOT use 0x13 for another RPL option | implemented+tested | Rs SCHC_RULE_VERSION_TYPE=0x13 rules.rs:30; Py rules.py:193; C LICHEN_RPL_OPT_SCHC_RULE_VERSION 0x13 (rpl_messages.h:69); singleton enforcement all three (Rs codec.rs:1985-1989; Py messages.py:334-335; C validate_dio_options messages.c:194-201 + dodag.c:682) | high |
| R-03-052 | §5.7: every node emitting a parent-selection-usable DIO MUST advertise the DODAG's rule set version; root originates; non-root propagates the same value | implemented+tested | Rs root mints DodagVersionAuthorization (fails closed if root pubkey ≠ DODAGID) router.rs:959-981; non-root propagates stored root-minted version filtered to authorization.version==dodag.version :976-978; cross-language vectors every_dio_matches_committed_cross_language_vectors lichen-rpl/src/message.rs:956 (modes insert_current/propagate_root/explicit). Py DIO.to_bytes always carries option (see R-03-054 caveat). C root inserts rpl_root.c:198-210. C production RX call-site absence tracked by b7z9.88 | high |
| R-03-053 | §5.7: serializer MUST emit exactly one canonical Rule Version Option; root MAY insert current immutable version when caller supplies none; caller-supplied option preserved (mismatch detectable); MUST reject duplicates | implemented+tested | Rs SchcRuleVersionOption rules.rs:570-641 + duplicate reject codec.rs:1985-1989 + authenticated_dio corpus duplicate_rule_version (authenticated_dio_vectors.rs:49). Py serialize-time duplicate reject messages.py:334-335 + explicit-version preserved test_messages.py:125-133 + test_vectors.py:1068. C lichen_rpl_schc_rule_version_write messages.c:849-866 + duplicate → BAD_OPT + tests/rpl_messages/main.c:30-56 | high |
| R-03-054 | §5.7: non-root serializer MUST require the authenticated root-originated DODAG version as input and preserve byte-for-byte; MUST NOT silently substitute its local version when input absent | divergent | Rs: satisfied at node level — non-root never substitutes (router.rs:959-981 propagates root-minted version), though the 0x13 serializer primitive itself has no role parameter (None → local current). **Py: divergent** — DIO.to_bytes auto-inserts local RULE_SET_VERSION when none supplied messages.py:338-339 (explicitly supplied non-current version IS preserved; enforcement only at RX admission gate dodag.py:593-608). **C: not-implemented** — no non-root serializer path (only root originates). Prior Opus verify F4 escalated for maintainer context. **New bead filed** (G2) | high |
| R-03-055 | §5.7: option is exactly three octets; a parser that does not return a consumed length MUST reject trailing bytes | implemented+tested | Rs from_bytes len==3 rules.rs:81-92 + rejects_trailing_bytes test rules.rs:619 + DIO option-loop bounds codec.rs:1962-1991; Py from_bytes len!=3 → ValueError rules.py:254-255 + test_context.py:260-326; C parse <1 TOO_SHORT / >1 BAD_OPT messages.c:830-847 (header docstring drift noted — claims 3-byte input incl. Type; implementation takes 1-byte value) + tests/rpl_messages | high |
| R-03-056 | §5.7: node MUST process the option only after the DIO's link signature and replay counter have been verified | implemented+tested | Rs LinkLayer verify first (rpl_stack/receive.rs:190,64) → decompress_authenticated_frame :222-227 → option parse from retained link_evidence codec.rs:2016-2038, 1977-1991. Py transact_authenticated_schc_dio operates only on AuthenticatedDio from link-verified RxFrame (dio_handler.py:73-119; link_layer.py:864-1012, option parse inside security lock :931-942). C process_dio_inplace returns unchanged when !authenticated dodag.c:404-406 + tests/rpl_dodag/main.c:847-860 ("unauthenticated DIO ignored") | high |
| R-03-057 | §5.7: node MUST NOT join a DODAG when option absent/malformed/reserved/unsupported/differs from local immutable registry; applies equally to root and non-root candidate-parent DIOs (optionless non-root DIO not a valid fallback) | implemented+tested | Rs MissingRuleVersion codec.rs:1993-1995 + malformed :1978-1984 + duplicate :1985-1989 + allows_dodag_join v3-only codec.rs:2099-2102 (0/1/2 rejected) applied to both roles router.rs:405-430 + dodag.rs:546-553; tests rule_versioning_vectors.rs:432, rpl_stack/tests.rs:1147 (bad shapes → RplRejected, stays joined — UU-merge file). Py dodag.py:593-608 (absent → ValueError; malformed; incompatible → admission denied) + commit re-check :613-617 + vector-driven test_protocol_vector_security.py:940. C lichen_rpl_dodag_process_dio_bytes_authorized dodag.c:659-693 (absent/malformed/mismatched → BAD_OPT before join :695) + tests. Note: C production RX path does not call process_dio_bytes yet (test-only; wiring family tracked via b7z9.88) | high |
| R-03-058 | §5.7: all implementations MUST support Rule 255 syntax regardless of version, but policy MUST still reject its use inside an incompatible DODAG | implemented+tested | Rs version-mismatch peer restricted to validated single-frame Rule 255 (AuthenticatedPeerSchcContext codec.rs:2104-2134) + test authenticated_peer_version_mismatch_allows_only_single_frame_rule255 codec.rs:3636 + rule_versioning.json "255-mismatch-inside-DODAG rejected" vector. Py peer fail-closed test_context.py:377 + version-mismatch-blocks-fragmentation test_vectors.py:1111. C admission-level gate (join refused ⇒ no v3 context) — rule_versioning vectors consumed only by stale standalone C test | high |
| R-03-059 | §5.7 Rule 255: payload MUST be complete structurally valid IPv6 — version 6, exact Payload Length, consistent next-header chain walked only through HBH (0), Routing (43), Destination Options (60) | implemented+tested | Rs validate_full_ipv6_structure codec.rs:271-375; Py ipv6/packet.py:84-85,223-248 + headers.py:116-149; C validate_ipv6_header_chain schc_helpers.c:406-460 + validate_ipv6_transport_lengths :490-565; malformed vectors: 19 in schc_compression.json + rule255_malformed adaptation vectors (Rs adaptation_vectors.rs:1111 + Py) + C tests/schc negative suite | high |
| R-03-060 | §5.7: Routing header (43) MUST be uncompressed RPL RH3 (RFC 6554): type 3, CmprI/CmprE/Pad octet + Reserved octet all zero, Hdr Ext Len = 8+16n (n≥1), Segments Left ≤ n; type 0 (RFC 5095) and every other type or non-canonical encoding malformed | implemented+tested | Rs codec.rs:312-338 (ext_len≥24, type 3, zero octets, SL≤n, (ext_len−8)%16==0); Py _validate_routing_headers headers.py:116-149; C schc_helpers.c:429-447 (IPV6_ROUTING_TYPE_RPL_SRH 3); tests routing_header_policy_matches_rfc6554_and_rfc2460 codec.rs:3481 + Py test_headers.py:555-571 + C tests/schc (non-RH3 reject); vectors rh3_*/routing_type0_malformed | high |
| R-03-061 | §5.7: an IPv6 Fragment header (44) is malformed | implemented+tested | Rs codec.rs:346-350 + fragment_header_is_unsupported codec.rs:3521; Py packet.py:250-251 + test_headers.py:566; C schc_helpers.c:454-456 + tests/schc; vector ipv6_fragment_header_malformed | high |
| R-03-062 | §5.7: walk terminates at first header outside {0,43,60} — encapsulated IPv6 (41), AH (51), ESP (50) are opaque payload (decapsulation/IPsec are the receiver's role, not the codec's) | implemented+untested | Rs catch-all `_ => break` codec.rs:351-353; Py NH 41/51/50 end walk, surface as upper layer packet.py:249-254; C chain-walk default. No test in any stack exercises NH 41/51/50 termination explicitly — flagged F5 | low — flagged |
| R-03-063 | §5.7: UDP payload MUST have exact UDP Length + nonzero valid checksum per RFC 2460 §8.1 over addresses present (last source-routing address while SL≠0, else destination) | implemented+tested | Rs codec.rs:355-373 + upper_layer_destination codec.rs:297,339-341; Py udp.py:97-102 + headers.py:147-165; C schc_helpers.c:542-562; tests: checksum-over-outer-dst rejected codec.rs:3512 + Py test_headers.py:571 + srh3_in_transit_udp_fallback vector + rule255_malformed bad-checksum vector | high |
| R-03-064 | §5.7: TX endpoint policy is an EMISSION constraint on every transmitted packet incl. Rule 255: sender MUST NOT originate unspecified/loopback/multicast/v4-mapped source or unspecified/loopback/v4-mapped/multicast-scope-outside-2-14 destination; Rule 255 encoder + fallback path MUST reject | implemented+tested | Rs validate_full_ipv6 → validate_address_policy applied in compress (codec.rs:2276) and encode_rule255 (codec.rs:1532); tests rule255_emission_rejects_policy_invalid_endpoints codec.rs:3148 + rule255_accepts_policy_valid_multicast_scope_5 codec.rs:3225 + adaptation rule255_endpoint_policy (11 vectors). Py _validate_rule255_emission_endpoints headers.py:223-249 applied :260-261, 941-942 + tests test_headers.py:276/295. C validate_ipv6_address_policy schc_helpers.c:462-488 enforced schc.c:563-571 | high |
| R-03-065 | §5.7: Rule 255 DECODING is byte-preserving (rule255-rx-decode decision, 2026-08-31, FINAL) — decoder MUST validate structure and checksums only, MUST NOT apply any endpoint address policy on receipt | divergent | Rs decode_rule255 structure-only codec.rs:1556-1584 + decision citation codec.rs:282-294 + test rule255_decode_is_byte_preserving_despite_emission_policy codec.rs:3203 (::1 source decodes byte-exact). Py decode_rule255 structure-only headers.py:284 + docstrings :170-200 + test (test_headers.py:306). **C: divergent** — RX path still applies structural address checks (unspecified/multicast source, unspecified destination → reject) schc_helpers.c:513-525, installed as validate_payload (schc.c:525); C comments self-contradict (schc.c:522-525 "deliberately absent" vs schc_helpers.c:513-518 "BOTH directions"); the 3 rule255_rx_structural_reject vectors are TX-reject/RX-accept in Rs/Py but would RX-reject in C. Prior verify F1 confirmed. Decision verify grep on Py headers.py passes. **New bead filed** (G1). Note: open bead iard (Python RX rejection) appears resolved by current code — candidate for closure check, not actioned here. Related: xl9j (RX re-screening) | high |
| R-03-066 | §5.7: on origination/forwarding paths, structural/checksum failures report before endpoint-shape; validation reports are local diagnostics only, MUST NOT be transmitted as protocol errors | implemented+tested | Rs validate_full_ipv6 order structure→policy codec.rs:383-388; source-before-destination asserted via adaptation vector message checks (adaptation_vectors.rs:1157-1168); error paths buffer-atomic/local-only (compress_error_leaves_output_untouched codec.rs:2716 + 5 more; stack maps to TxError::SchcCompress without transmit stack.rs:495). Py test_validate_full_ipv6_structure_precedes_address_checks (test_headers.py:319); C order structural→policy schc.c:563-571 | high |
| R-03-067 | §5.7: SCHC_FRAGMENT_MAX_PACKET_SIZE (126×179=22554) bounds the RAW uncompressed IPv6 packet in BOTH directions, every rule incl. 255; sender MUST NOT compress raw > ceiling; receiver reassembly buffer same bound; at-ceiling MUST compress; one octet above MUST be rejected before rule dispatch; MUST NOT substitute encoded size for raw | implemented+tested | Rs SCHC_FRAG_MAX_PACKET_SIZE=22554 constants.rs:28; TX raw-gate pre-dispatch codec.rs:2263-2275; RX encoded-gate :2396-2400 + enforce_encoded_profile_limit :2415-2423; boundary tests mqtt_sn/coap/icmpv6/rule255 profile_size_boundary codec.rs:2751/2910/2989/3249 (raw+1 terminal reject; raw 22581 rejected though encoded would fit — :2849 comment cites spec) + size_boundary vectors. Py raw gate headers.py:937-940 + test_compressed_packet_ceiling_applies_after_encoding (test_headers_rules.py:96) + per-rule max tests. C TX/RX gates (lichen/schc/schc.c:542-544,574-584; subsys/schc/schc.c:254-263,286-293; per-rule decompressor gates) + tests/schc main.c:720,756 | high |
| R-03-068 | §5.7: Rule 255 packet = 1-byte Rule ID + full IPv6 packet (encoded ≤ 1+raw) | implemented+tested | Rs needed=packet.len()+1 codec.rs:1533 + rule255_profile_size_boundary codec.rs:3249 (raw 22553 → encoded 22554 OK; raw 22554 → terminal); Py headers.py:262-263 + test_rule255_maximum_is_encoded_schc_size (test_headers_rules.py:74); C ceiling tests | high |
| R-03-069 | §5.7: sender may select Rule 255 when: no matching rule; version mismatch outside incompatible DODAG (authenticated peer registry known, fits one link frame); debugging; configured legacy peers outside operational v3 DODAG | implemented+tested | No-match fallback + version-mismatch single-frame Rule 255 evidenced (R-03-058 sites); legacy/retained-registry isolation R-03-075. Residual: oversized Rule 255 with allow_fragmentation=true fails BufferTooSmall instead of returning fragmentable packet (open bead **z4m8**, Rust link.rs) — referenced, not re-beaded | high |
| R-03-070 | §5.7: Rule 255 provides no fragmentation compatibility; if Rule 255 SCHC Packet exceeds one link frame, both peers MUST support the same fragmentation Rule Set Version or the packet cannot be sent | divergent | Spec logic implemented (version-mismatch peers get Rule-255-only contexts, R-03-058), but the "cannot be sent"/fragmentable boundary is buggy in Rs: open bead **z4m8** (compress_schc_for_peer ignores allow_fragmentation on version-mismatch Rule 255 — oversized fails BufferTooSmall instead of fragmentable SCHC packet). Py: single-frame enforcement consistent; C: n/a production. Referenced, not re-beaded | high |
| R-03-071 | §5.7 failure table: unknown Rule ID → drop+log; decompression → invalid IPv6 → drop+log; repeated failures from same source → assume version mismatch, notify operator | implemented+tested | Realized by the tracker (R-03-072) + drop paths: Rs UnknownRuleId/InvalidPacket propagate, notify_operator single-shot; Py _record_schc_failure on the error classes node.py:985-1040; C schc_failure_tracker + lichen_link_rx.c:369/396 | high |
| R-03-072 | §5.7: production decompression ingress MUST maintain bounded consecutive-failure tracker keyed by authenticated link signer; unknown rules/truncated or non-canonical residues/invalid decompressed increment; output-buffer or transport failure does not; success clears only that signer; threshold emits exactly one notification per run; full tracker → fail closed for untracked signer, MUST NOT evict | implemented+tested | Rs RuleVersionFailureTracker<MAX_SOURCES> context.rs:43-108 (notify-once, saturate, capacity_events, no eviction) wired via decompress_authenticated_frame_tracked codec.rs:1588-1618 (BufferTooSmall exempt :1609; InvalidPeerEvidence exempt :2220) + stack.rs:228/267/320-337; tests rule_versioning_vectors.rs:229-260 + rule_versioning.json notify/capacity vectors. Py RuleVersionFailureTracker context.py:214-264 (max_sources 16, threshold 3) + node tests test_node.py:1019/1106/1137/2036. C schc_failure_tracker.c:18-86 (16 sources, fail-closed no-evict) + twister test. Thin: direct increment tests only drive UnknownRuleId (Rs); churn-slot note = open bead 80x5 | high |
| R-03-073 | §5.7: explicit version negotiation NOT required (DIO passive discovery); mismatched nodes cannot join same DODAG (DIO filter), can communicate validated single-frame Rule 255 outside the DODAG, should be upgraded | implemented+tested | Join gate v3-only (R-03-057); version-mismatch peer Rule-255-only compression codec.rs:2104-2134 + link_layer_integration.rs:345 (version-1 DIO ⇒ Rule-255-only) + Py test_vectors.py:1111/1102. "Should be upgraded" is ops guidance, no code expected | high |
| R-03-074 | §5.7: receiver MUST drop an unknown Rule ID; MUST NOT reinterpret the unknown residue as IPv6 or wrap it in Rule 255 after decompression fails | implemented+tested | Rs dispatch has no fallback — id => Err(UnknownRuleId) codec.rs:2411-2412 (error type itself proves no wrap) + unknown_rule_id_errors codec.rs:3626 + adaptation rejection vectors 0xFE/0x10/0x77; Py context.py:129-131 + headers.py:980-1007 (only rule_id==255 routes to decode) + test_context.py:182, test_headers.py:398; C find_rule miss → SCHC_ERR_UNKNOWN_RULE_ID (subsys/schc/schc.c:325-328); rule_versioning.json rejection vectors (rules 8/63/67/119/254). Related open bead 3jj2 (draft wording) | high |
| R-03-075 | §5.7 backward compat: new rules SHOULD use new Rule IDs (no reuse); MAY retain old registry for offline/legacy but retention MUST NOT make it operational, satisfy admission, enable fragmentation, or permit compressed traffic under a v3 peer context | implemented+tested | Rs only_version_three_has_a_local_registry rules.rs:624 + version-1 DIO ⇒ Rule-255-only compression link_layer_integration.rs:345 + mismatched DIO revokes peer router.rs:420-430; Py peer fail-closed test_context.py:377 + test_authenticated_dio_security.py:814/858/1040 (rejected DIO keeps no policy/state). SHOULD is process guidance; retention-MUST satisfied by single-v3 registries | high |
| R-03-076 | §5.7: version number MUST increment on any rule change | implemented+untested | Process-level: Rs rules.rs:19-26 history + rule_set_v3_descriptor_hash rules.rs:518 (registry fingerprint detects drift — rule_versioning.json registry vector) but no mechanism can enforce that the version was bumped alongside a change; Py RULE_SET_VERSION=3 + same vector. Prior verify F5: recommend CI fingerprint-vs-version check. Flagged F6 | low — flagged |
| R-03-077 | §5.7: every parent-selection-usable DIO (root or non-root) carries exactly one authenticated Rule Version Option; non-root MUST propagate root-originated version unchanged; MUST NOT replace unsupported value with local version or omit option for legacy fallback; DIOs not usable for parent selection MAY be retained for diagnostics but MUST NOT create/refresh an operational SCHC context | divergent | Rs: admission-gated context issuance (rejected DIO revokes peer router.rs:420-430; compress/decompress fall back to 255 when !allows_dodag_join codec.rs:2104-2134); production node has no AuthenticatedSchcPolicy install site yet (test-only type) — guards cover the intent. Py: no-policy-before-parent-selection explicit (prepare callback None → return before context issue, dodag.py:610-611 + link_layer.py:967-988; test_authenticated_dio_security.py:874/321/1169) — but the serializer auto-insert (R-03-054) undermines the propagate-unchanged clause at TX. C: gate exists test-only; no production RX. Python serializer divergence → same **new bead G2** | high |

### Histogram (rows)

- implemented+tested: 64 (R-03-002..026 except those listed below, 029-041, 043, 046, 050-053, 055-061, 063, 064, 066-075; 4 of these are low-confidence: R-03-012, 020, 024, 039)
- implemented+untested: 3 (R-03-001, R-03-062, R-03-076)
- divergent: 6 (R-03-047, 049, 054, 065, 070, 077)
- not-implemented: 4 (R-03-042, 044, 045, 048 — all C-side production wiring; Rs node reassembly seam for R-03-048)

### Gap beads

New this sweep (parent `project-LICHEN-worker6-b7z9`, label `spec-gap`):

1. **G1** — C Rule 255 RX path applies structural address checks contrary to FINAL `rule255-rx-decode` decision (R-03-065) — divergent.
2. **G2** — Python DIO serializer auto-inserts local rule-set version; non-root MUST require/preserve root-originated version (R-03-054/077); C has no non-root path — divergent + not-implemented.
3. **G3** — Rust/Python tombstone/admission-floor overflow is fail-closed/time-expiry, not evict-oldest-by-terminal-time (R-03-047) — divergent.
4. **G4** — dangling CMake source `schc_session_authority.c` (lichen/subsys/lichen/schc/CMakeLists.txt:18) breaks Zephyr builds of the SCHC subsystem — build defect blocking the l1qw.3.7.6 fixes (filed as bug; not a spec MUST row).

Overflow: 0 (4 < cap 10). Pre-existing open beads covering the remaining divergent/not-implemented rows: b7z9.68 (R-03-049), z4m8 (R-03-070, R-03-069 residual), b7z9.5 / b7z9.5.2 / b7z9.5.2.2 / b7z9.5.5 (R-03-048 Rs), l1qw.3.7.6 + .4 + .5 (R-03-042/044/045/048 C), b7z9.88 (C DIO RX production wiring), pgsl (R-03-024 address-policy fallback tension), iard (appears resolved — Python now byte-preserving), bsul, 80x5, 3jj2, 61ju, fvyf, h65m (adjacent).
