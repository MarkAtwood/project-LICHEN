<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- SPDX-FileCopyrightText: The contributors to the LICHEN project -->

# spec/02b-ccp-receiver-aware.md — flagged set (sweep 2026-09-09)

Every requirement classified **divergent**, or with **low confidence**, or
**ambiguous**. (02b is not 06-security and contains no oscore/EDHOC semantics,
so clause (c) of the protocol does not add rows.) Companion matrix:
`02b-ccp-receiver-aware.md`. Gap beads: `project-LICHEN-kudc.8`–`.17` under
epic `project-LICHEN-kudc`; reference-integrity bead `project-LICHEN-kudc.18`
(human decision required).

| Req | Classification | Evidence (summary) | Question for Opus |
|-----|----------------|--------------------|-------------------|
| R-02b-001 | divergent | Superseded density→SF (+2, floor 11) and density→CH0 fallback live in all stacks: py ccp.py:105,352,378; rust rf_health.rs:385-395,662,695; C gradient.c:325,361, link_ctx.c:716. Tests (py test_ccp.py:107-113, C routing_dispatch:1116-1248, rust ccp15_vectors.rs) pin the superseded behavior. No receiver-aware path exists to violate | CCP2-001 conformance target: is the density→SF/CH0 code expected to be **removed or policy-gated now**, or does 02b defer conformance until wire activation (CCP2-002) so legacy code is merely "retained legacy"? This decides whether kudc.9 is a code change now or a gating task. Also confirm the >8 residues (duty_cycle.py:55, channel_plan.py:110, ccp.py:316, rust rf_health.rs:384, C link.h:647, channel_plan_selection.schema.json:33) are stale-comment-only against the density-high decision |
| R-02b-003 | divergent, low | `schedule_generation`/PHY-profile/plan-version namespaces absent everywhere; ccp16-desync.json is a bare array with no `format_version`; channel_plan_selection.json format_version=1 schema-pins legacy `density > 8` | Is the bare-array ccp16-desync.json a sanctioned legacy exception (document it) or a fixture-namespace regression to repair (add format_version)? And should format_version=1 of channel_plan_selection.json serve as the CCP2-025 "legacy" marker mechanism? |
| R-02b-005 | divergent | C frequency lookup = operating-class base + index·200 kHz (lora_l2_tx.c:395) — the exact forbidden pattern; Rust single 3-entry table serves as plan and operating class (lichen-hal lib.rs:228-266); no stack supports 1..255 plan entries; Python plan-table lookup conforms (channel_plan.py, ccp4 vectors) | Confirm C should move to identified-plan lookup (and whether that lands with the Rev2 plan model or earlier), and whether the 1..255 entry-count requirement is a wire-revision-gated representation or a present-day code obligation |
| R-02b-006 | divergent (arguably not-implemented) | u32 BE bit0=CH0 beacon mask in all stacks (py tdma_beacon.py:119,140; rust tdma_beacon.rs:107,135 + "wider than 32 channels cannot be expressed" :191-193; C beacon.c:53-96); ccp4 pins u64 masks; ceil(n/8) byte-string codec exists nowhere | Is "divergent" the right classification when the conforming encoding simply doesn't exist yet and the existing one is explicitly legacy (02b:117-119 says the old mask cannot represent the contract)? If not-implemented is preferred, kudc.11 keeps the same scope |
| R-02b-007/008 | divergent / not-implemented, low (008) | `schedule_generation`: 0 hits in all stacks; legacy preimages hash the link epoch (py ccp.py:108-115, rust lib.rs:165-199, C link_ctx.c:721-733, ccp_select_channel_endianness.json); rx_channel limited 0-7; no suppression-on-unknown-plan path | Confirm CCP2-007/008 belong entirely to the post-CCP2-002 wire-encoding workstream (kudc.12) rather than having any present-day code obligation (e.g., should legacy epoch-hash selection be annotated as legacy now?) |
| R-02b-014 | divergent | C production RX feeds every signature-verified frame as a valid beacon (lichen_l2_rx.c:256-264, CONFIG_LICHEN_TDMA=y); `>3` in secondary FSMs (C tdma.c:219, py tdma_fsm.py:34, rust gateway tdma_fsm.rs:47) vs mandated `>=3`; epoch-floor adoption gate unwired (C time_sync.c:564-582 no caller; rust tdma_beacon.rs:425-429 defers) | Confirm (a) the l2 data-as-beacon feed was an intentional 02a design (d7hg) that 02b now overrides, and gating it to beacon-type frames is the correct remediation; (b) whether `>=3` binds only the canonical DesyncFSM or also the secondary tdma_fsm/CCP FSM tables (the 02b sentence says "this revision uses >=3 missed beacons"); (c) whether adopting a verified non-beacon frame also currently bypasses "authorized time provenance" in practice |
| R-02b-015 | implemented+tested, low | SF10/125k/CR4-5/8-preamble/explicit/CRC/LDRO-off are the universal defaults (py airtime.py:13-16, rust constants.rs:4-9 + airtime.rs:195-199 "CCP profile 0x01 fixes DE off", C airtime tests 2295808U); but no `0x01` profile identifier exists, no generation container, C production slot is 250 ms (bead b7z9.17) | Does CCP2-015 require a named profile-0x01 constant + C slot-constant correction as present-day obligations, or are both subsumed by the CCP2-002 wire work and b7z9.17? |
| R-02b-016/017 | divergent | Density/loss escalate SF toward 12 (py ccp.py:352-365, rust rf_health.rs:617-629, C gradient.c:325); estimators add +2/+1 synthetic neighbors from loss/RSSI (rust rf_health.rs:124-139; py ccp.py:220-228 pinned by tests); no RF-evidence staleness/direction model | Confirm the remediation in kudc.9: density inputs restricted to airtime-budget tiers only, synthetic-neighbor terms removed, and ccp16.json legacy vectors left intact as legacy (per CCP2-025) rather than regenerated |
| R-02b-018/019 | divergent | Rust production TX = CCA-then-transmit, no duty/legal check (stack.rs:458-470; DutyCycleTracker zero production callers); accounting erased on restart in all stacks (C hal_duty.c:23-27 memset, rust heapless RAM, py in-memory); C charges success-only non-SF-aware durations (lora_l2_tx.c:631, :508); C rejects backwards time (conformant) | Confirm (a) Rust TX-path duty enforcement is an in-scope present-day fix (kudc.16) given the policy library already exists and is tested; (b) the restart-persistence mechanism preference: persist an accounting ledger vs fail-closed until window expiry, given Zephyr flash/RAM budgets; (c) whether the 0.4 s/20 s occupancy ledger (CCP2-022) must exist before any US915 fixed-channel operation can be enabled, or only before Rev2 schedules activate |
| R-02b-020/021 | implemented+tested, low | Queues/priority/back-pressure/CSMA bounds tested all stacks; CCA-clear never treated as delivery; congestion = 5.03+shed (py coap/params.py tiers, rust duty_cycle.rs:280-293, C duty_response.c). Coalescing evidence conflict: earlier 07-transport-app sweep recorded C-only position/status coalescing; this sweep found none in C (only DIS coalescing, rpl_dis.h:49,85) | Resolve the coalescing evidence conflict for CCP2-020's SHOULD: does C coalesce position/status updates (coap_status.c/coap_location.c) or not? And does CCP2-021's "recheck remaining operation time after deferral" require an operation-time model now, or is it deferred with the CCP2-012 fit-check work (kudc.15)? |
| R-02b-023 | divergent | No plan representation supports 1..255 entries or a 128-entry grid (py US915=64, C Kconfig 1-16, rust 3 classes); 902300000 only as legacy 64-entry fixture value | Confirm the classification: not enabling the candidate 128-grid is conformant (CCP2-023 says qualification-gated), so the only present-day gap is representational capacity — is widening the plan representation required before the wire revision, or with it? |
| R-02b-024 | not-implemented (conformant absence), low | No experimental-profile machinery; nothing enabled; amateur mode absent so the OSCORE MUST is vacuous; C Kconfig LICHEN_LR_FHSS_ENABLED is an unwired knob; lr_fhss.json:33 anchor off-by-one (1026047 vs 1026048), zero consumers | Confirm "conformant absence" is the intended treatment for CCP2-024 (no gap bead filed), and whether the unguarded LICHEN_LR_FHSS_ENABLED Kconfig should carry a dependency/notice that it must not silently replace baseline PHY/security per CCP2-024 |
| R-02b-025 | divergent | No legacy markers on any fixture; vectors "regenerated to match real behavior" (test_ccp_sync_vector_consumers.py:25-45); rust ccp_slot_map_validation.rs duplicates the algorithm while production parse_slot_map has no fixture consumer; py test_vectors.py inline recomputation (:1862,:1897,:5219-5239,:5285, docstring contradiction :1935); ccp4 oracle field self-referential; zero-consumer fixtures (ccp5, density_scaling, ccp9); lr_fhss.json 1026047 off-by-one; ccp16-desync.json no format_version | Confirm the tracking split for kudc.17 vs existing worker6-cmj5 / b7z9.106 / b7z9.163 (avoid duplicate or conflicting remediation), and whether "regenerated to match real behavior" fixtures should be re-derivable at all under CCP2-025 or grandfathered as legacy with explicit markers |
| R-02b-026 | implemented+untested, low | docs/ccp-conformance.md:13-59 defines evidence classes and per-requirement cases; nothing enforces them; no capacity-report implementation | Does CCP2-026 need enforcement tooling (CI/report template), or is the documented contract sufficient conformance for a policy-only requirement? |
| Step-0 (section-level) | reference gap | `ccp-receiver-aware` decision absent from decisions.jsonl though 02b:9 claims it; docs/ccp-conformance.md:9 cites nonexistent epic project-LICHEN-worker6-x7hh; no verify checks were runnable for 02b | Bead kudc.18 (human-flagged via note): Mark to record the ccp-receiver-aware decision (with verify checks) or fix the spec citation, and to create or de-reference the x7hh epic. No sweep output depends on the missing record; density-high (>10) is textually consistent with 02b |
