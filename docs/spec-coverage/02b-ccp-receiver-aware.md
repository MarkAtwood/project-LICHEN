<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- SPDX-FileCopyrightText: The contributors to the LICHEN project -->

## spec/02b-ccp-receiver-aware.md — coverage (sweep 2026-09-09)

Requirement IDs `R-02b-NNN` map 1:1 to the spec's stable conformance
identifiers `CCP2-001`…`CCP2-026`. Several CCP2 requirements bundle multiple
MUSTs; where sub-MUSTs split between conforming and gapped code, the row shows
a compound status and the histogram counts the primary (weakest) one.

**Step 0 (decisions.jsonl):** no decision's `specs` array includes
`02b-ccp-receiver-aware.md`, so no verify checks were applicable. Two
reference-integrity findings, NOT adjudications:

1. `spec/02b-ccp-receiver-aware.md:9` states "The `ccp-receiver-aware` decision
   in decisions.jsonl supersedes…", but **no such decision exists** in
   `spec/decisions.jsonl` (19 lines, none named `ccp-receiver-aware`). The
   decision that owns this section's authority cannot be verified. Filed as a
   human-flagged reference bead; per protocol the missing decision was NOT
   invented or re-adjudicated.
2. `docs/ccp-conformance.md:9` names epic `project-LICHEN-worker6-x7hh` as the
   conformance tracker; no such bead exists. Spec 02b:290-291's `project-LICHEN-kudc`
   does exist (open) and is used as the parent for gap beads here.

Cross-checked decisions that 02b cites: `density-high` (>10) is consistent with
CCP2-001's "remains applicable" text; no conflict found between 02b and any
recorded decision.

**Scope note:** 02b itself declares (lines 6-7, 291-292) that wire activation is
gated and implementation conformance is not claimed. Accordingly, the Rev2
receiver-aware machinery (rows 007-010, 013, 022) is expected-absent; the
material findings are the production-behavior divergences (014, 016-019) and
the fixture/oracle discipline (025). "Vacuous" below = the required mechanism
does not exist, so the prohibition cannot currently be violated.

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-02b-001 (CCP2-001) | Decisions MUST stay separate; density/load/queue/utilization MUST NOT select a remote receiver's channel or cause an SF increase; CH0-fallback and SF-escalation consequences superseded; control-traffic tiers unchanged | divergent | No receiver-aware path exists to violate (density is not an input to any receiver-side selection: py link/channel.py:86-158 has no density param). But the superseded consequences are the live policy in all three stacks: py ccp.py:352,378 (density>10→SF+2, floor 11), rust rf_health.rs:662,695, C gradient.c:325,361; density→CH0 fallback py ccp.py:105, rust rf_health.rs:385-395, C link_ctx.c:716. Tests pin the superseded behavior (py test_ccp.py:107-113, rust ccp15_vectors.rs, C routing_dispatch main.c:1116-1248). Control-traffic tiers: test/vectors/density_scaling.json has zero consumers in any stack | high |
| R-02b-002 (CCP2-002) | Rev2 MUST NOT be enabled on air until exact versioned encoding + independent byte vectors specified and reviewed | implemented+untested (vacuous) | No Rev2 wire encoding exists anywhere — zero hits for `schedule_generation`, permitted-set, or rendezvous-encoding symbols in python/src, rust/, lichen/ (agent sweeps); nothing can activate. No test asserts non-activation. Gap bead NOT filed (absence is the conformant state; wire spec is the CCP2-002 follow-up itself) | high |
| R-02b-003 (CCP2-003) | Policy revision, PHY profile ID, plan ID/version, schedule generation, SFN, SCHC Rule Set Version, sig-domain version, JSON fixture `format_version` MUST stay distinct namespaces; no repurposing of RPL options/SCHC Rule IDs/spare bits | divergent | Most Rev2 namespaces do not exist (`schedule_generation`: 0 hits all stacks). Concrete regressions in the fixture namespace: test/vectors/ccp16-desync.json is a bare array with no `format_version`; test/vectors/channel_plan_selection.json format_version=1 pins the legacy `density > 8` formula as a schema `const` (channel_plan_selection.schema.json:33) while code uses >10. No evidence of RPL-option/SCHC-rule repurposing (vacuous). SCHC Rule Set Version / signature-domain version models absent | low |
| R-02b-004 (CCP2-004) | Unversioned/incompatible advertisement MUST NOT activate a Rev2 data schedule; CH0 contact bypasses nothing; legacy interop not assumed | implemented+untested (vacuous) | No Rev2 advertisement format exists; beacon accept gates only on signature+mask intersection (rust tdma_beacon.rs:452-472, C beacon.c, py tdma_beacon.py verify_gate) with no revision concept, so nothing can be activated. No mixed-version rejection test (impossible pre-wire) | high |
| R-02b-005 (CCP2-005) | Plans MUST be ordered 1-255 entries, index 0=CH0; plan IDs and operating-class IDs MUST NOT be interchanged; frequency lookup MUST use the identified plan, not base+offset | divergent | Python conforms on lookup: 7 plans by plan_id 0x01-0x07 with integer-Hz tables (channel_plan.py:122-234), fail-closed UnknownPlanError :239-260, tested via test/vectors/ccp4_regional_channel_plans.json (test_ccp4_regional_channel_plans.py:36-122); operating-class IDs separate (link/op_class.py:18-23). C violates the lookup MUST: lora_l2_tx.c:395 `ch_freq = plan_freq + effective_channel * 200000U` is exactly the forbidden index-dependent offset from an operating-class base; counts capped 1-16 (link/Kconfig). Rust has no plan model — a 3-entry operating-class table serves both roles (lichen-hal/src/lib.rs:228-266). No stack supports 1..255 entries | high |
| R-02b-006 (CCP2-006) | Permitted sets keep global plan indices; canonical bitset = ceil(count/8) bytes, bit 7-(i mod 8) of byte floor(i/8); MUST NOT truncate to u32/u64; 128 entries = 16 bytes | divergent | Existing representation is exactly the forbidden one: u32 BE beacon channel_mask, bit0=CH0 (py tdma_beacon.py:20,119,140; rust tdma_beacon.rs:15,107,135; C beacon.h:24, beacon.c:53-96); py intersect normalizes to 0xFFFFFFFF (channel_plan.py:89-99); rust documents plans >32 channels cannot be expressed (tdma_beacon.rs:191-193); C slot bitmap u64 (coap_slot_coord.c:624-649); ccp4 fixture pins u64 masks (US915 0xffffffffffffffff). Byte-string encoding and 7-(i mod 8) bit order exist nowhere. Tests pin the u32 form (py test_ccp4…:102-122, rust tdma_beacon.rs:479-502, C tests/tdma_beacon) | high |
| R-02b-007 (CCP2-007) | Receiver's fresh authenticated rendezvous authoritative; home = eligible[fnv1a32(receiver EUI-64 ‖ schedule_generation LE u32) mod len]; sender validates selected channel; MUST NOT rehash over a local subset | not-implemented | `schedule_generation`: 0 hits in python/, rust/, lichen/ (dedicated greps). No home-channel derivation in any stack; legacy preimages use the link replay epoch (py ccp.py:108-115, ccp_select_channel_endianness.json, C link_ctx.c:721-733) — the exact namespace confusion 02b forbids. Announce rx_channel is 0-7 only (py messages.py:74-75; C announce.c:123,290; rust announce.rs:111-114), which 02b:117-119 itself names as unrepresentative | high |
| R-02b-008 (CCP2-008) | Home channel stable for its generation; CCA failure/congestion MUST NOT trigger per-packet retuning; unknown plan ⇒ suppress TX, not local rehash | not-implemented | No home-channel or per-packet retune machinery exists (rust node tunes once at init, stack.rs:258; C derives per-TX from base+offset lora_l2_tx.c:379-404). Unknown-plan fail-closed exists (py channel_plan.py:239-260; rust lookup None) which partially matches "suppress"; the required control/discovery path is absent. No gap bead (folded into R-02b-007 bead) | low |
| R-02b-009 (CCP2-009) | Schedules MUST provide periodic common CH0 control/discovery windows and explicit data opportunities; nodes MUST NOT claim continuous CH0 reception; geometry fits exchanges/tuning/clock/legal budgets | not-implemented | No control-window model in any stack: no schedule structure with control windows, no tuning/settling budget, no clock-uncertainty term. Rust node uses one fixed channel (stack.rs:258,461-468); C data channels via base+offset (lora_l2_tx.c:374-404); py channel selection is the legacy CCP-9 priority chain (link/channel.py:86-158). Bead filed | high |
| R-02b-010 (CCP2-010) | Transmission eligible only with sender TX opportunity AND next-hop receiver compatible RX opportunity on same channel/profile; no overlapping RX/TX on half-duplex; queued TX MUST NOT preempt committed RX; grant identifies owner; anonymous slot_map is not an assignment | not-implemented | No receiver commitments or grants exist in any stack (grep `commit*`/grant in receiver context: zero). `slot_map` is the anonymous CBOR index list (rust tdma_beacon.rs:199-290; C beacon.c:150-226; fixture ccp_slot_map_validation.json) — the exact object 02b disclaims as ownership. Rust slot-map test duplicates the algorithm locally instead of consuming production `parse_slot_map` (tests/ccp_slot_map_validation.rs:14-110; production at lichen-core/src/tdma_beacon.rs:208). No RX/TX overlap checker. Bead filed | high |
| R-02b-011 (CCP2-011) | Candidate = ((FNV1a32(eui64)+u32(SFN)) mod 2^32) mod num_slots retained; num_slots MUST be positive; candidate is not an exclusive grant; equal residues at 8 slots collide; explicit assignments/repair MUST resolve conflicts | implemented+tested / not-implemented (repair) | Formula implemented+tested in all three stacks: py timing/sfn.py:28-46 (u32 carry, num_slots<=0 ValueError) + test_ccp_sync_vector_consumers.py:510-524 (ccp_slot_hash_carry.json, discriminating test); rust tdma_beacon.rs:31-37 + sfn_wrap_vectors.rs (ccp_sfn_wrap_slot_hash.json), triplicated rf_health.rs:376-382 / tdma_scheduler.rs:18-24 (u16); C tdma.c:51-55 + tests/link_crypto main.c:606-613. num_slots: py ValueError, rust None/ParseError::NumSlotsZero (:94-96), **C silently coerces 0→8** (tdma.c:53, link.h:88) instead of validating. Collision repair/assignments for equal residues: none in any stack; C production treats the hash candidate as the TX slot (lichen_link_tx.c:110, inert now_ms=0 FIXME :99-109). Bead filed | high |
| R-02b-012 (CCP2-012) | Admission MUST establish the complete operation (setup/retune/CCA + full frame + response + single trailing guard) fits before the boundary; profile 0x01 keeps 50 ms guard and 2346 ms minimum; no new universal link ACK | implemented+tested / not-implemented (fit) | Guard+slot constants and window gate implemented+tested: py sfn.py:12-13 + sim/tdma.py:94-117 (tests test_ccp_sync_vector_consumers.py:482-485); rust constants.rs:95,97 + tdma_clock.rs tx_allowed/in_guard (ccp_tdma.json via tdma_clock_vectors.rs); C guard=50 link.h:87 (tests link_crypto:641-694). 255-byte SF10 airtime 2295808 µs pinned (py test_protocol_vector_security.py:738 via independent oracle; rust airtime_vectors.rs:60-85; C tests/airtime:30). Whole-operation fit check (setup/retune/CCA/turnaround terms): none in any stack. Universal link ACK: absent everywhere (conformant). C production LICHEN_TDMA_SLOT_MS=250 contradicts the 2346 geometry (existing bead b7z9.17). Bead filed for the fit check | high |
| R-02b-013 (CCP2-013) | Hardware capability MUST describe actual simultaneous RX/TX; channel count alone MUST NOT be a throughput multiplier; passband/demodulator/half-duplex limits respected | not-implemented | Beacon carries `rx_chains` u8, parsed unchecked and unused (C beacon.c:47; rust tdma_beacon.rs:103); no capability model, no parallel-RX or passband/demod logic anywhere (agent sweeps all three stacks). Bead filed (with R-02b-012 fit-check bead) | high |
| R-02b-014 (CCP2-014) | Only a validated TDMA sync object maintains/recovers the schedule; ordinary authenticated data MUST NOT count as a beacon; epoch-floor/current-generation checks precede adoption; quality ordering per 09 §14.6; `>=3` missed beacons (not `>3`); 3 consecutive valid beacons recover | divergent | (a) **C production RX feeds every signature-verified frame to the desync FSM as a valid beacon**: lichen_l2_rx.c:256-264 comment "a verified frame counts as a beacon result… Drives DESYNCED -> SYNCED recovery" — direct violation. (b) FSM operator split: canonical `>=3` implemented+tested (C tdma.c:142-143,165-172 + link.h:92; py sfn.py:166,173; rust desync.rs:186-194; ccp16-desync.json consumers all three) vs conflicting `>3` in the secondary FSMs (C tdma.c:219 `missed > LICHEN_TDMA_MISSED_BEACON_THRESHOLD`; py tdma_fsm.py:34 `missed > 3`; rust gateway tdma_fsm.rs:47 `n > 3`, test pins MissedBeacons(3) stays Synced) — 02b mandates `>=3`. (c) Epoch-floor/current-generation adoption gate exists but unwired: C lichen_epoch_floor_accepts time_sync.c:564-582 has no production caller (l2 rx comment defers to it); rust tdma_beacon.rs:425-429 explicitly defers freshness to C. Stratum ordering per 14.6 implemented (C root_selection.c:34-35, rust lichen-rpl time_stratum.rs + multi_instance.rs:883-919, py timing/stratum.py:303-325). Recovery = 3 consecutive valid beacons implemented+tested all stacks. Bead filed | high |
| R-02b-015 (CCP2-015) | Profile 0x01 stays SF10/125 kHz/CR4/5/8-preamble/explicit/CRC/LDRO-off; parameters MUST NOT change inside a generation; earlier hysteresis decision not permission to retune this profile | implemented+tested (partial) | Parameters exist as universal defaults with LDRO explicitly off: py timing/airtime.py:13-16 (+ test_airtime.py Semtech cross-check 369.664 ms SF9); rust constants.rs:4-9 + airtime.rs:195-199 (comment "CCP profile 0x01 fixes DE off") pinned by airtime_vectors.rs (2295808 µs); C tests/airtime asserts 2295808U. But no `0x01` profile identifier exists in any stack (grep zero), no generation container exists to be stable within, and C production slot geometry is 250 ms (link.h:88, bead b7z9.17). Adaptive-profile gating unimplementable until generations exist | low |
| R-02b-016 (CCP2-016) | Queue load/density/CCA exhaustion/unattributed loss MUST NOT be treated as proof of insufficient sensitivity; denying admission MUST NOT implicitly select SF12; RF evidence retains receiver/direction/channel/profile/time/coverage/validity; stale evidence stays unknown | divergent | Density directly escalates SF toward 12 in all stacks: py ccp.py:352-353 `min(12, sf+2)` on density>10, threshold floor :378-379 sf=max(11,sf); rust rf_health.rs:617-629 (density>20→12, >10→11), full select :636-702; C gradient.c:325,361. Loss feeds SF too (py :361 ema_loss>0.25→+1). No RF-evidence record with direction/staleness/validity fields exists in any stack. Admission denial path (py utilization>200→SF12+tx off :363-365; rust rf_health.rs:678-681) literally returns SF12. Bead filed (with R-02b-001) | high |
| R-02b-017 (CCP2-017) | Density MUST count observed distinct immediate radio peers over the window; relayed Announce origins are not peers; weak RSSI and loss MUST NOT add synthetic neighbors; metrics stay separate; RX-driven estimators must not silently freeze | divergent | All three density estimators ADD synthetic neighbors from loss and RSSI: rust rf_health.rs:124-139 `estimate_density` (+2 loss>threshold, +1 RSSI<threshold), py ccp.py:220-228 identical formula (pinned by test_density_matches_formula_vectors test_ccp.py:550-559 and ccp16.json), C lora_data.density feeds the same tiers (lora_l2_tx.c:507). Distinct-peer tracking exists (rust PeerDensityTracker rf_health.rs:291-367; py PeerDensityTracker) but the estimator output is the forbidden hybrid. Metrics separation partial (BusyPercent/PER separate). No estimator-freeze modeling. Bead filed (with R-02b-001) | high |
| R-02b-018 (CCP2-018) | Every transmission MUST satisfy legal constraints AND the adaptive airtime ceiling; tiers/accounting are upper bounds; remote ads MAY narrow only; unsupported profiles fail closed | divergent | C conforms in production: duty gate before every TX (lora_l2_tx.c:500-513,631) + 400 ms dwell (hal_duty.c:201,308-312) + region from op class. Python enforces only in the simulator medium (sim/node_server.py:322,350-361); the policy wrappers (RegionalDutyCycleEnforcer, adaptive_duty_permille) have no production callers. **Rust production TX performs no duty/legal/admission check at all** — stack.rs:458-470 is CCA-then-transmit with zero callers of DutyCycleTracker/adaptive ceilings (lichen-tui uses them demo-only). Fail-closed unknown plan/class conforms (py channel_plan.py:239-260; rust lichen-hal lib.rs:268-272). Ceilings+dwell themselves implemented+tested (duty_cycle_calculation.json consumers all three). Bead filed | high |
| R-02b-019 (CCP2-019) | Accounting MUST include complete frames, control, forwarding, retries, parity; aliased indices/SF/retune/plan changes MUST NOT reset or duplicate allowance; monotonic time; clock correction or restart MUST NOT erase an active restriction | divergent | 1-hour rolling window + monotonic axis implemented+tested in all three (rust duty_cycle.rs:45,320-354,631-643 + duty_cycle_vectors.rs; py DutyCycleTracker sim medium + timing/duty_cycle.py:32; C hal_duty.c:113-159, rejects backwards time :40-49, tests/regional_duty_cycle). Gaps: **restart erases accounting everywhere** (C ctx memset hal_duty.c:23-27; rust heapless::Deque in RAM; py in-memory list) violating "restore valid accounting or wait out the window"; C records only successful TX (`if (ret >= 0)` lora_l2_tx.c:631) with a non-SF-aware duration `80 + pop_len*6` ms (:508) — retries/backoff/forwarding/parity never charged; rust/py would count whatever the (unwired) caller passes. No retune/alias reset exists (nothing retunes) — vacuous. Bead filed | high |
| R-02b-020 (CCP2-020) | Priority MUST NOT bypass receiver commitments, frame-fit, legal limits, or adaptive ceilings; keep bounded queues/deadlines/priorities/back-pressure; SHOULD coalesce replaceable position/status updates; custody unchanged | implemented+tested (commitments vacuous) | Bounded queues, deadlines, priority classes, back-pressure implemented+tested all three: py timing/tx_queue.py:440-465 + tx_queue_bounded.json vectors (47 cases); rust tx_queue.rs:269-287,493-501 + bufferbloat_vectors.rs; C tx_queue.h:60-66. Congestion gating by priority (rust check_congestion_allows duty_cycle.rs:280-293; py coap/params.py:118-292 tiers 500/800/950‰; C duty_response.c:30-55). No priority bypass path exists (commitments absent). Coalescing (SHOULD): conflicting evidence — an earlier sweep recorded C-only position/status coalescing (docs/spec-coverage/07-transport-app.md:60) but this sweep found none in C either (only DIS coalescing rpl_dis.h:49,85) | low |
| R-02b-021 (CCP2-021) | CCA-clear is an access check, not delivery; preserve bounded retries; recheck remaining operation time and budgets after deferral; congestion response SHOULD reduce low-priority traffic to actual service, not enlarge queues or raise SF | implemented+tested (partial) | Bounded CSMA implemented+tested all three: py csma.py:14 RETRY_LIMIT 3, backoff cap 5 (tests test_csma.py:242-244); rust constants.rs:13-19 + CsmaState/CcaState fail-closed; C link.h:116-119 + csma.c:39-77,182-189 (tests csma, csma_constants). CCA-clear never surfaced as delivery in any stack (py CsmaResult.TX_SUCCESS is access-only; C TX_ALLOWED access-only). Congestion response reduces/sheds, never enlarges queues (bounded queues + 5.03/Retry-After all three). NOT done: recheck of remaining operation time/budgets after deferral (no operation-time model; see R-02b-012) | low |
| R-02b-022 (CCP2-022) | Deployment profile MUST identify operating authority and document measured bandwidth/spacing/hopping/occupancy/power conditions; fixed receiver-home or fixed CH0 MUST NOT be enabled merely because the radio tunes there | not-implemented | No operating-authority concept anywhere (0 hits, all stacks); no FHSS 0.4 s-per-frequency/20 s occupancy bookkeeping (the 400 ms constant is a per-TX dwell ceiling: rust duty_cycle.rs:50-55, C duty_cycle.h:19, py duty_cycle.py:44 — an aggregate-occupancy ledger like the conformance doc's two-emission case does not exist); no 250/500 kHz measured-bandwidth rules (those numbers appear only as SX127x modem encodings rust airtime.rs:22, py airtime.py:26-27); no deployment-profile authority document. Bead filed (regulatory enforcement bead) | high |
| R-02b-023 (CCP2-023) | Channel-plan representation MUST NOT impose a lower-half-US915 restriction; 128-entry candidate grid `902300000+200000*i` (i=0..127) is unqualified and must receive a distinct plan/version only after qualification | divergent | Representations cap below the required index space: py US915 table = 64 entries (channel_plan.py:139-150) with u64 scalar masks in ccp4; C Kconfig num_channels 1-16 + base+offset lookup (lora_l2_tx.c:395); rust 3 operating classes only. No encoding can express 128+ entries or the candidate grid; `902300000` exists only as the legacy 64-entry fixture value (ccp4…json:28,498); 927700000 nowhere. Not enabling the grid conforms to 02b; the representation being unable to express it is the divergence (same root as R-02b-005/006 bead) | high |
| R-02b-024 (CCP2-024) | Narrow-band hopping/LR-FHSS/LICHEN-2 PHY are separate experimental profiles, none enabled by Rev2; experiments MUST count preamble/LDRO/framing/reacquisition/parity costs; amateur operation MUST NOT silently disable production OSCORE/security | not-implemented (conformant absence) | No experimental-profile machinery exists in any stack; nothing is enabled, so "None is enabled" conforms and the amateur/OSCORE MUST is vacuous (no amateur mode exists). C Kconfig `LICHEN_LR_FHSS_ENABLED` (l2/Kconfig:295-313) is an un wired PHY knob. lr_fhss.json exists but carries an off-by-one anchor (1026047 µs vs the 1.026048 s independent anchor — noted in the CCP2-025 bead); no consumer in any stack | low |
| R-02b-025 (CCP2-025) | Legacy CCP fixtures MUST remain identified as legacy; outputs MUST NOT be rewritten from the revised implementation; new fixtures need independent oracles; production TX/RX paths in Python, Rust and C must consume them; algorithm-duplicating tests are insufficient | divergent | (a) No fixture carries a legacy marker/classification (only narrow prose in py test_channel_plan.py:280-283,340-343), and test_ccp_sync_vector_consumers.py:25-45 documents vectors "regenerated to match real behavior" — implementation-derived, against the independent-oracle rule (ccp-conformance.md:63-67). (b) Duplication: rust tests/ccp_slot_map_validation.rs:14-110 reimplements slot-map validation with zero production imports while production `parse_slot_map` (lichen-core tdma_beacon.rs:208) has no fixture consumer; rust ema_vectors.rs:16-29 local reimpl; py test_vectors.py:1862,1897,5219-5239,5285-5291 inline SF/channel recomputation (subset tracked as worker6-cmj5; docstring contradiction :1935). (c) ccp4 fixture's own `oracle` field names production `lichen.channel_plan` (self-referential). (d) Zero-consumer fixtures: ccp5, density_scaling, ccp9 (generator-equality only); ccp9*/ccp16-hop unconsumed by Rust/C (b7z9.163). (e) lr_fhss.json:33 pins 1026047 µs — off-by-one vs the 1026048 independent anchor. (f) ccp16-desync.json lacks format_version (R-02b-003). Production consumption exists (C runtime JSON reads tdma_beacon/desync_fsm/rf_health_vectors; py production-module consumers; rust crate tests) but no Integrated-class radio TX/RX path in any stack — matching 02b:291-292's declared blockers. Bead filed | high |
| R-02b-026 (CCP2-026) | Claims MUST distinguish policy-tested / wire-tested / integrated / RF-measured / authorization-reviewed; missing evidence is not assessed, not passed; capacity results must report workload/topology/limits including failures | implemented+untested | The evidence-class contract exists as documentation: docs/ccp-conformance.md:13-28 (classes, per-result recording fields, "missing… are not passes") and :30-59 per-requirement case matrix; 02b:288-292 links it and names the blockers. Nothing enforces it (no report tooling/CI gate); no capacity-report artifact exists to check the failure-reporting clause | low |

### Classification histogram

| Status | Count |
|--------|-------|
| implemented+tested | 5 |
| implemented+untested | 3 |
| divergent | 11 |
| not-implemented | 7 |
| ambiguous | 0 |
| **total** | **26** |

### Gap beads filed (cap 10)

10 gap beads filed under epic `project-LICHEN-kudc` (label `ccp`, `spec-gap`),
consolidating 18 requirement-IDs with gap content; no MUST-gap was dropped —
the overflow (8 requirement-IDs beyond 10) is folded into the 10 beads by shared
implementation site, not dropped. Plus 1 reference-integrity bead (missing
`ccp-receiver-aware` decision + missing `x7hh` epic), a Step-0 finding, not a
MUST-gap. IDs listed in `02b-ccp-receiver-aware-flagged.md` and the session report.
