<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- SPDX-FileCopyrightText: The contributors to the LICHEN project -->

## spec/appendix-bufferbloat.md — coverage (sweep 2026-09-09)

Scope note: the Problem section (lines 6–44) is background; the latency table
and LoRa-factor prose carry no keywords except where noted (R-BB-021,
R-BB-022 are extracted from keyword-less but behavior-defining statements
there). Requirement IDs use `R-BB-NNN` (BB = appendix-bufferbloat). Several
rows are keyword-less imperatives from "Design Principles" and
"Implementation Guidelines"; these are numbered because they define behavior.

Step 0: `spec/decisions.jsonl` has exactly one decision listing
`appendix-bufferbloat.md`: **`custody-best-effort`** (2026-09-08). It has no
`verify` grep. Spec lines 89–90 ("Custody remains best effort, subject to the
existing storage, eviction, TTL, and retry policies; acceptance does not
guarantee delivery") already conform to the decision — no regression, no spec
edit. R-BB-006 below is read through that decision.

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-BB-001 | Every queue has an explicit, small bound; when full, new packets are rejected rather than silently queued beyond it; local admission returns an error (backpressure) (Design Principle 1, lines 52–55) | implemented+tested | Rust: `TX_QUEUE_CAPACITY=4` (rust/lichen-core/src/tx_queue.rs:240), `TxQueueError::QueueFull` (:219), preemption :494-541; Python: capacity 4 (python/src/lichen/link/tx_queue.py:33,256), `QueueFullError` (:76), `ensure_can_push` :448-463; C: `TX_QUEUE_SIZE=LICHEN_FRAME_POOL_CAPACITY=4` (lichen/subsys/lichen/link/include/lichen/frame_pool.h:21 → tx_queue.h:49), `-ENOBUFS` (lichen/subsys/lichen/link/tx_queue.c:361). Tests: rust lichen-core `capacity_is_four` (tx_queue.rs:1334), `tx_queue_capacity_matches_bounded_vector` (tests/bufferbloat_vectors.rs:32 ← test/vectors/tx_queue_bounded.json); python tests/link/test_tx_queue.py + tests/timing/test_tx_queue_vectors.py `test_capacity_default_4`; C lichen/tests/tx_queue (gen_vectors.py) | high |
| R-BB-002 | Concrete bounds: TX queue 4 packets max; forwarding 2 packets per source max; BLE GATT 8 messages max (returns QueueFull) (lines 58–61) | implemented+tested | TX-4 and forwarding-2 evidence under R-BB-001/R-BB-016. BLE GATT: rust/lichen-meshtastic/src/gatt.rs:39 `MAX_QUEUE_DEPTH=8`, :379 returns `GattError::QueueFull`, test gatt.rs:587 `assert_eq!(result, Err(GattError::QueueFull))`; bridge queue also 8 (bridge.rs:24 `MAX_BRIDGE_QUEUE=8`). Note: 8-bound verified only in the Rust meshtastic bridge; C ble_ipsp_transport returns `-ENOBUFS` (lichen/subsys/lichen/transport/ble_ipsp_transport.c:442,479) with the 8 bound not pinned; no Python BLE GATT queue found | medium |
| R-BB-003 | Queued packets older than their deadline are silently dropped (Time-Based Expiry, lines 68–69) | implemented+tested | C: `deadline_expired` + purge (tx_queue.c:136-174, `packets_dropped_deadline` :174); Python: expiry sweep + stats (link/tx_queue.py:324-338); Rust: pop-time expiry, `expired_capacity_is_reclaimed_by_observers` (tx_queue.rs:1818). Tests: test/vectors/tx_queue_expiry.json via rust tests/bufferbloat_vectors.rs:47 `deadline_constants_match_expiry_vector`, python tests/timing/test_tx_queue_vectors.py, C lichen/tests/tx_queue | high |
| R-BB-004 | Datagram-service deadlines: routing control 5 s; ACK/NACK 10 s; application data configurable, default 60 s (lines 76–78) | divergent | C: `TX_DEADLINE_ROUTING_MS=5000`, `TX_DEADLINE_ACK_MS=10000`, `TX_DEADLINE_NORMAL_MS=60000` (lichen/subsys/lichen/link/include/lichen/tx_queue.h:71-76); Rust: `DEADLINE_ROUTING_MS=5_000`, `DEADLINE_ACK_MS=10_000`, `DEADLINE_NORMAL_MS=60_000` (rust/lichen-core/src/tx_queue.rs:243-262); vector oracle pins ACK=10000 (test/vectors/tx_queue_expiry.json constants, test/vectors/bufferbloat_congestion.json `ack_deadline_ms`). DIVERGENT: Python runtime `lichen/link/tx_queue.py:39 DEADLINE_ACK_MS = 5000  # alias for ROUTING` — 5 s vs spec 10 s; parallel module `lichen/timing/tx_queue.py:28` has the correct 10000 and is the one driven by vectors (tests/timing/test_tx_queue_vectors.py), while runtime code (node.py:72) uses link/. Gap bead filed | high |
| R-BB-005 | Message service: custody messages carry absolute TTL (hours–days, 05 §9.8); TX queue deadline 120 s (time to reach first custodian) (lines 80–82) | ambiguous | Absolute TTL exists: `LICHEN_DTN_DEFAULT_TTL_SEC` (lichen/subsys/lichen/coap/coap_dtn.c:245-247), rust/lichen-node/src/routing/dtn.rs, python/src/lichen/routing/dtn_option.py. But no code maps custody/message-service traffic to a 120 s TX-queue deadline: P2 default is 30 s in all three stacks (`TX_DEADLINE_URGENT_MS=30000` tx_queue.h:74, `DEADLINE_URGENT_MS=30_000` tx_queue.rs:256, `DEADLINE_URGENT_MS=30000` link/tx_queue.py:40); 120000 exists only as the P4 BULK deadline. No custody-labeled queueing path found (`rg -i custod` over src = empty). Unclear: should originator custody packets be queued at 120 s, or does P2/30 s + DTN absolute TTL satisfy the intent? Flagged for Opus | low |
| R-BB-006 | Datagram-service packets dropped when stale; once a custody-capable node accepts, the TX-queue deadline no longer applies; custody remains best effort, subject to storage/eviction/TTL/retry policy (lines 85–90) | ambiguous | DTN store path stores with absolute expiry and clockless fail-open (coap_dtn.c:233-261, R-05-080 comment :557); rust routing/dtn.rs + python routing/dtn_option.py cover the S-flag/expiry side. No explicit "custody acceptance ends TX-queue deadline" transition is implemented or tested (no custody vocabulary in src). The best-effort clause conforms to decision `custody-best-effort` (no coercive mechanism added anywhere). Flagged: what code object would demonstrate the acceptance transition? | low |
| R-BB-007 | Waiting for an eligible radio opportunity MUST NOT extend a packet's deadline (lines 94–95) | ambiguous | Python: requeue-on-TX-failure preserves original deadline (link/tx_queue.py:637-644) with tests `test_complete_failure_requeues_entry` (:858), `test_complete_failure_preserves_original_deadline` (:873), retry scenario (:978) in tests/link/test_tx_queue.py. Rust/C: no requeue path found — `tx_queue_pop` removes the entry (tx_queue.c:463) and C L2 runs "synchronous mode - push then immediately" pop (lora_l2_tx.c:475-489); no Rust caller re-queues after TX failure, so "no extension" holds only vacuously and failure-retry deadline semantics are untested. Flagged: is a Rust/C requeue-preserving-deadline path required, or is pop-once semantics conformant? | low |
| R-BB-008 | The node MAY coalesce unsent, replaceable state updates (e.g. current position) under the owning resource's semantics (07 §10.3); it MUST NOT coalesce arbitrary messages, commands, receipts, or custody records (lines 95–97) | not-implemented | No coalescing of position/state updates anywhere (`rg coalesce` finds only RPL DIS solicitation coalescing, lichen/subsys/lichen/rpl/rpl_dis.c:113 — unrelated). The MUST-NOT clause holds vacuously (nothing coalesces arbitrary messages either). MAY → no gap bead per protocol. Flagged only to confirm vacuous-satisfaction reading | low |
| R-BB-009 | Higher-priority packets are selected ahead of lower-priority eligible TX (priority table 0–4 mapped to delivery services, lines 104–112) | implemented+tested | Rust: priority pop (tx_queue.rs:639), `pop_if_allowed_preserves_fifo_within_priority` (:1703), `pop_skips_expired_higher_priority_and_preserves_live_order` (:1767); Python: sorted insert + priority pop (link/tx_queue.py:440-465); C: selection loop (tx_queue.c:165-270). Tests: test/vectors/tx_queue_priority.json (rust bufferbloat_vectors.rs:68, python timing vectors, C gen_vectors.py); `priority_preemption` scenario in test/vectors/bufferbloat_congestion.json pinned by rust tests/bufferbloat_vectors.rs:122 and python tests/test_bufferbloat_congestion_vectors.py:46. Service mapping itself is 07 §10.2 territory (cross-ref that sweep) | high |
| R-BB-010 | Queue eviction is not radio preemption: priority MUST NOT interrupt committed RX, exceed legal/adaptive airtime limits, or start TX unless the full radio operation plus guard fits the available opportunity (lines 113–116) | implemented+tested | Rust: `pop_if_allowed(now, CongestionLevel)` duty-cycle gating (tx_queue.rs:675, tests :1504-1598 normal/elevated/critical tiers) + `pop_after_csma` (:736); C: CCP-13 adaptive duty budget (lichen/subsys/lichen/l2/lora_l2_tx.c:502), CCP-15 CSMA/CA+CAD backoff (:553); tests lichen/tests/{csma,regional_duty_cycle,tdma_guard_budget,ccp_slot_map}; Python: TxReservation commit/cancel machinery (link/tx_queue.py:1180-1326 tests) models slot commitment. Committed-RX protection is receiver-aware CCP (02b) — cross-ref that sweep's rows | medium |
| R-BB-011 | When a queue is full use existing signaling: `ENOBUFS`/`QueueFull` for immediate sends; existing protocol failure response for mesh-forwarded packets only where that protocol permits it and the response is eligible for TX (lines 122–125) | implemented+tested | Local: C `-ENOBUFS` (tx_queue.c:361), Python `QueueFullError` (tx_queue.py:462), Rust `TxError::QueueFull` (stack.rs:134) — all tested (R-BB-001). Mesh-forwarded: Python returns `ForwardingResult.BACKPRESSURE` (routing/router.py:78,215) and has a tested ICMPv6 `make_resource_exhausted` helper (python/src/lichen/ipv6/icmpv6.py:407; component-tested in tests/routing/test_no_silent_drops_vectors.py) but the helper is never called in runtime src; Rust/C return error codes with doc comments saying "SHOULD send NACK upstream" (stack.rs:737) and no emission path. Conditional clause — absence is conformant, but see bead on doc/vector divergence (R-BB-012) | medium |
| R-BB-012 | Without a response mechanism, record the forwarding drop locally rather than inventing a MAC ACK/NACK or unsolicited mesh error (lines 127–128) | implemented+tested | Behavior conforms: forwarding drops are recorded locally (Python `buf.stats.packets_backpressure` asserted in tests/routing/test_no_silent_drops_vectors.py:47; Rust `per_source_limit_enforced` forward_buffer.rs:319 + `ForwardStats`; C router.c per-source counters) and no MAC ACK/NACK mechanism exists anywhere (`rg NACK` rust/ → doc comments only). DIVERGENT DOC/VECTORS: forward_buffer.rs:5-6,29-30,106-107 and stack.rs:95,738 say "a NACK should/SHOULD be sent upstream"; test/vectors/no_silent_drops.json requirement B.2.5.2 "NACK to mesh source (if routable)" (ICMPv6 ADMIN_PROHIBITED); test/vectors/bufferbloat_congestion.json fairness `expected: nack_when_source_full`. Gap bead filed (doc/vector vs runtime alignment) | low |
| R-BB-013 | Senders receiving backpressure must back off within existing retry and deadline policies, or drop the packet and notify the application (lines 129–130) | implemented+tested | Sender-side handling lives in 07 §10.2.4 (cross-ref that sweep): Python congestion 5.03 + Retry-After service (python/src/lichen/coap/params.py) with vectors (coap_transport.json congestion tiers); Rust `check_congestion_allows` + `pop_if_allowed` tier gating (rust/lichen-core/src/duty_cycle.rs, tx_queue.rs:675); C coap_backoff test (lichen/tests/coap_backoff). App-notification: `TxError`/`QueueFullError` surface to callers | medium |
| R-BB-014 | Congestion visibility: return error to local sender; mesh failure response only where existing protocol supports it (route to source alone not sufficient); log queue-full events for diagnostics (lines 134–139) | implemented+untested | Error-to-sender: R-BB-011. Logging: Python `ensure_can_push` logs WARNING on queue-full (link/tx_queue.py:459-462), asserted in `test_eviction_logged_at_warning` (tests/routing/test_no_silent_drops_vectors.py:49); C `LOG_WRN` on push failure (lora_l2_tx.c:482); Rust returns `QueueFull` — no log statement found at the queue site. Mesh-failure-response clause: same open question as R-BB-011/012 | medium |
| R-BB-015 | TX-queue admission/eviction: expire oldest deadline first; if full and new packet higher priority, evict lowest queued priority; if full and same/lower priority, return -ENOBUFS (lines 157–161) | implemented+tested | C: tx_queue.h:158-170 doc + preemption loop tx_queue.c:165-270, `-ENOBUFS` :361, `packets_dropped_full` :360; Python: preemption (`packets_dropped_preempt` link/tx_queue.py:429) + `ensure_can_push` reject :448-463; Rust: `try_preempt` tx_queue.rs:494-541. Tests: tx_queue_priority vectors (all three stacks), python tests/link/test_tx_queue.py preemption/rejection cases, C lichen/tests/tx_queue, rust tx_queue.rs unit tests | high |
| R-BB-016 | Forwarding buffer: `MAX_FORWARDING_SOURCES 8`, `MAX_PACKETS_PER_SOURCE 2`; reject forwarding admission when source at limit; total 16 packets max (lines 170–175) | implemented+tested | Rust: constants (forward_buffer.rs:20-23), per-source reject :121-123, tests `per_source_limit_enforced` (:319) + distinct-source fill (:366-374); oracle test/vectors/forwarding_buffer.json pinned (tests/bufferbloat_vectors.rs:95 `forwarding_buffer_oracle_matches_spec_b2`). Python: link/forwarding_buffer.py:32-33 + routing/router.py:33-34,207; tests/link/test_forwarding_buffer.py + test_no_silent_drops_vectors.py `test_backpressure_per_source` (:38). C: CONFIG defaults (routing/include/lichen/routing/router.h:73-78) + reject (router.c:1504); test lichen/tests/routing_fwd_buffer. Note: at 8 distinct sources Rust evicts the oldest source's packet (forward_buffer.rs:126-131) and C recycles the oldest source slot (router.c:1411-1444) rather than rejecting — total bound maintained by eviction; spec example doesn't specify this case | high |
| R-BB-017 | Report forwarding admission failure only through existing protocol mechanisms (per Explicit Backpressure); the example adds no wire response (lines 177–178) | implemented+untested | No new wire response exists: C router.c:1504 returns error only; Rust maps `ForwardError::QueueFull → TxError::QueueFull` (stack.rs:131-137) with no emission; Python returns `BACKPRESSURE` without calling the ICMPv6 helper. "No invented wire response" verified by absence (`rg NACK`/`resource_exhausted` rust/ + lichen/ = no runtime callers). Untested as a negative property | medium |
| R-BB-018 | Track time-in-queue, not just queue depth: packets_queued, packets_dropped_deadline, packets_dropped_full, max_latency_ms, avg_latency_ms (lines 185–192) | implemented+untested | C: `struct tx_queue_stats` (tx_queue.h:108-115) + EWMA alpha-1/8 update (tx_queue.c:509-540) + `tx_queue_stats_get` (:609); Python: `QueueStats` (link/tx_queue.py:204-224) + EMA 0.1 (:498-510) + `node.get_queue_stats` (python/src/lichen/node.py:1676-1694); Rust: `TxQueueStats` with all five fields incl. max/avg latency (rust/lichen-core/src/tx_queue.rs:406-425). No test found asserting latency-stats values (python no_silent_drops vectors assert backpressure/eviction counters only) | high |
| R-BB-019 | Expose (queue stats) via `/status/queues` CoAP resource for diagnostics (line 194) | not-implemented | Endpoint absent in all three stacks: Python resources are /status, /status/neighbors, /status/routes, /config (python/src/lichen/coap/resources/node_resources.py:3-16, resources/__init__.py:5-6); C coap_server registers /status, /config, /neighbors (lichen/subsys/lichen/coap/coap_server.c:12, Kconfig:149-151 — no queues); Rust gateway resources have no "queues" hits. Stats plumbing is ready and waiting: C `lichen_lora_l2_queue_stats_get` documented "Exposed for the /status/queues CoAP endpoint" (lora_l2.h:276, lora_l2_tx.c:693), Python node.get_queue_stats, Rust TxQueueStats. Gap bead filed | high |
| R-BB-020 | Bufferbloat avoidance must be tested under congestion: queue-full (ENOBUFS), deadline expiry, priority eligibility (no preemption of committed RX / airtime / guard), multi-hop latency (expire stale rather than promising an e2e bound), fairness (lines 218–226) | implemented+tested | Oracle: test/vectors/bufferbloat_congestion.json (5 scenarios, "Literals from spec/appendix-bufferbloat.md Testing, not from queue implementations") + tx_queue_{bounded,expiry,priority,implementation}.json + forwarding_buffer.json + no_silent_drops.json. Consumers: rust/lichen-core/tests/bufferbloat_vectors.rs, python tests/timing/test_tx_queue_vectors.py + tests/test_bufferbloat_vectors.py + tests/test_bufferbloat_congestion_vectors.py + tests/routing/test_no_silent_drops_vectors.py, C lichen/tests/tx_queue (gen_vectors.py — does not consume the congestion file). CONFLICT: vector `multihop_latency` pins `expected: "bounded_e2e_delay"`, contradicting spec line 224-225 ("rather than promising an end-to-end delivery bound"); pinned by rust bufferbloat_vectors.rs:126 and python test_bufferbloat_congestion_vectors.py:47. Gap bead filed (report conflicting fixture — do not weaken tests) | high |
| R-BB-021 | Duty cycle: applicable regional rules determine the accounting group and window, dwell accounting per physical frequency where required; channel count or retuning does not create a new budget; queued packets wait for both legal and adaptive airtime eligibility (lines 26–32) | implemented+tested | C: regional duty-cycle test (lichen/tests/regional_duty_cycle), CCP-13 adaptive budget (lora_l2_tx.c:502), tx_queue header doc "Explicit backpressure… time-based expiry… duty-cycle" (tx_queue.h:6-13) and tx_queue.c:10-12; Rust: congestion-tier gating `pop_if_allowed` (tx_queue.rs:675) + `estimated_drain_time_ms` (:823); Python: CCP-16 TDMA awareness (link/tx_queue.py:3) + reservations. Cross-ref: 02a-coordinated-capacity and 02-physical-link sweeps own the regional-rule details | medium |
| R-BB-022 | Half-duplex: channel access requires carrier sense and backoff; receiver-aware CCP reserves committed RX opportunities; queued TX cannot take them over merely because no frame has arrived yet (lines 34–37) | implemented+tested | C: CCP-15 CSMA/CA (lora_l2_tx.c:553) + tests lichen/tests/{csma,ccp_slot_map,tdma_guard_budget}; Python: TX reservation commit/cancel with slot semantics (link/tx_queue.py + tests :1180-1326); Rust: `pop_after_csma` (tx_queue.rs:736). Receiver-aware CCP specifics (committed RX, receiver eligibility from peer contract) are spec 02b-ccp-receiver-aware.md — cross-ref that sweep | medium |
| R-BB-023 | New wire behavior requires exact versioned encodings and independent conformance oracles; more channels do not grant more airtime; experiments do not establish production readiness (lines 229–231) | implemented+untested | Process-governance row. Vector governance observed: `format_version: 2` + `$schema` on bufferbloat_congestion.json / no_silent_drops.json; generators derive literals from spec not code (test/vectors/generate_forwarding_buffer.py; bufferbloat_congestion.json description). "More channels ≠ more airtime" lives in CCP budget code (R-BB-021). No test directly asserts the governance clauses; unclear what an oracle for them would look like. Flagged | low |

### Notes

- Stale self-references found during the sweep: code/vector files cite
  "appendix-bufferbloat.md B.2 / B.2.2 / B.2.5" section numbers that do not
  exist in the current spec (lichen/subsys/lichen/link/include/lichen/tx_queue.h:70,
  python/src/lichen/timing/tx_queue.py docstring, test/vectors/no_silent_drops.json
  `spec_section: B.2.5`, rust tests/bufferbloat_vectors.rs:4). Covered by gap
  beads.
- Python has two parallel TX-queue modules with divergent constants:
  runtime `lichen/link/tx_queue.py` (ACK=5000) vs vector-driven
  `lichen/timing/tx_queue.py` (ACK=10000). Bead filed under R-BB-004.
- Gap beads filed: **4** (R-BB-004, R-BB-012+R-BB-011, R-BB-019, R-BB-020).
  No overflow. Flagged for Opus: **7**
  (appendix-bufferbloat-flagged.md).
