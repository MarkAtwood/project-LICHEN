<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- SPDX-FileCopyrightText: The contributors to the LICHEN project -->

## spec/appendix-loadng.md — coverage (sweep 2026-09-09)

Scope note: RREQ/RREP/RERR codec details and per-message handling are normative
in spec/05-routing.md §10 (own sweep); rows here cover the appendix's
configuration parameters, the B2.2 evidence paragraph, and appendix-specific
behavior. No row in this section touches oscore/EDHOC internals.

Gap beads filed under epic `project-LICHEN-worker6-b7z9` (ids .193–.200);
overflow count: 0 (8 MUST/behavior gaps ≤ cap of 10).

| Req | Spec text (trimmed) | Status | Evidence | Confidence |
|-----|---------------------|--------|----------|------------|
| R-LOADNG-001 | Intro: LOADng only for mesh-internal p2p with recent authenticated evidence; MUST NOT probe an arbitrary 0200::/8 destination | divergent | Gate implemented + vector-tested in py only (`EvidenceTable` python/src/lichen/rpl/evidence.py:14-159; `AddressClassificationTable` python/src/lichen/rpl/address_classification.py:43-97; tests python/tests/rpl/test_local_evidence_vectors.py:84-94, test_address_classification_vectors.py) but **no production routing path consults it**: py initiates discovery on any gradient miss python/src/lichen/routing/router.py:557-567 (and queues LOADng for EXTERNAL dst when DODAG stale router.py:574-597); C router.c:158-160; rust hybrid.rs:276. Intra-spec conflict: 05-routing §10.2 pseudocode has no gate | low |
| R-LOADNG-002 | B2.1: RREQ_WAIT_TIME = 5000 ms | implemented+tested (C) / partial (py) / not-implemented (rust) | C `LICHEN_LOADNG_RREQ_WAIT_TIME_MS 5000` lichen/subsys/lichen/routing/include/lichen/routing/loadng.h:80, used loadng.c:564,605; py only `pending_timeout_ms=5000` python/src/lichen/node.py:242 (queue expiry, docstring cites RREQ_WAIT_TIME); rust has no wait constant — caller-driven timing hybrid.rs:413 | high (C) / low (cross-stack) |
| R-LOADNG-003 | B2.1: RREQ_RETRIES = 3 (max RREQ attempts) | implemented+tested (rust, as ring count) / untested (C) / not-implemented (py) | Rust 3 rings then `Failed`: `advance_ring` rust/lichen-core/src/loadng.rs:349-359, test `route_discovery_expanding_ring_typestate` loadng.rs:823-844; C ring count 3 (loadng.c:589-607) but `LICHEN_LOADNG_RREQ_RETRIES 3U` loadng.h:83 is defined-never-used; py has no retry/ring driver (bead b7z9.195) | medium |
| R-LOADNG-004 | B2.1: RREQ_RATELIMIT = 10/min originated | not-implemented | No originate-side limiter in any stack (rg ratelimit/rate_limit across python/src/lichen/node.py+loadng, rust/lichen-node, lichen/subsys/lichen/routing). Only C receive-side admission limiter (10/min per source + 30/min global per 05-routing §10.3) loadng.c:41-56,453-492, tested `test_rreq_rate_limit_per_source/global/window_expiry` lichen/tests/loadng/main.c:575-630 — different requirement | high (absence) — bead b7z9.199 |
| R-LOADNG-005 | B2.1: MAX_HOP_LIMIT = 15 | implemented+tested | C loadng.h:59 + `test_constants` lichen/tests/loadng/main.c:524; rust `MAX_HOP_LIMIT=15` loadng.rs:38 + reject tests `rreq_hop_limit_above_max_rejected`/`rrep_hop_count_above_max_rejected` loadng.rs:704,738; py messages.py:32 + `_validate_hop` messages.py:39-42; vectors loadng_messages.json | high |
| R-LOADNG-006 | B2.1: INITIAL_HOP_LIMIT = 4 (expanding ring start) | implemented+tested | C loadng.h:56 + test_constants; rust loadng.rs:35; py messages.py:28, `originate_rreq` default discovery.py:134; C `test_expanding_ring_constants` main.c:538 | high |
| R-LOADNG-007 | B2.2: ROUTE_CACHE_SIZE = 32 | implemented+tested (py, C) / not-implemented (rust) | py `ROUTE_CACHE_SIZE=32` python/src/lichen/loadng/cache.py:20 + `test_lru_eviction` test_cache.py:63; C `LICHEN_LOADNG_ROUTE_CACHE_SIZE 32` loadng.h:68 (CONFIG-overridable loadng.c:13-15); rust has no LOADng route cache — installs into gradient table only (hybrid.rs:428-438) | high |
| R-LOADNG-008 | B2.2: ROUTE_TIMEOUT = 300 sec (no traffic) | implemented+tested (py, C) / not-implemented (rust) | py `ROUTE_TIMEOUT_MS=300_000` cache.py:21 + expiry tests test_cache.py:42; C `LICHEN_LOADNG_ROUTE_TIMEOUT_MS 300000U` loadng.h:71, applied loadng.c:354,666,714 + wrap-safe expiry loadng.c:362-380; rust n/a | high |
| R-LOADNG-009 | B2.2: ROUTE_REFRESH = 60 sec (refresh if used within) | divergent | Constants present (C loadng.h:74; py cache.py:22) and refresh mechanisms unit-tested (`lichen_loadng_cache_refresh` loadng.c:340-360; `RouteCache.refresh` cache.py:119-130, test_refresh_extends_validity test_cache.py:51), but **no production caller refreshes on route use** anywhere (only .refresh( caller in python/src is the evidence tracker address_classification.py:74) | high (absence) — bead b7z9.200 |
| R-LOADNG-010 | B2.2: SEQNUM_LIFETIME = 600 sec | not-implemented | rg `seqnum_lifetime` matches only spec/appendix-loadng.md:30. Note py `GRADIENT_TIMEOUT_MS=600_000` python/src/lichen/gradient.py:28 is the §11 gradient expiry — equal value, different parameter | high (absence) — bead b7z9.198 |
| R-LOADNG-011 | B2.2: LOCAL_EVIDENCE_LIFETIME = 1200 sec | implemented+tested (py) / not-implemented (rust, c) | py `LOCAL_EVIDENCE_LIFETIME_SECONDS=1200` python/src/lichen/rpl/evidence.py:14, pinned by test_local_evidence_vectors.py:93 (`== 1200`) against test/vectors/local_evidence.json; no evidence code in rust/lichen-node or lichen/subsys/lichen/routing | high (py) |
| R-LOADNG-012 | B2.2: each authenticated Announce/RREP/RPL/DATA observation refreshes evidence exactly LOCAL_EVIDENCE_LIFETIME; valid while now < observed_at+L, expired at equality | implemented+tested (py) / not-implemented (rust, c) | py `AUTHENTICATED_SOURCES={"announce","rrep","rpl","data"}` evidence.py:18; `refresh()` fixed 1200 s evidence.py:86-137; expiry-at-equality `expires_at_s <= now_s` evidence.py:145; vector scenarios test/vectors/local_evidence.json (lifetime/refresh/expiry) | high (py) |
| R-LOADNG-013 | B2.2: MUST use monotonic clock for the lifetime; MUST NOT extend evidence after clock regression or timestamp overflow | implemented+tested (py) / not-implemented (rust, c) | py `_validate_now` regression rejection evidence.py:64-74 (EvidenceTimeError on `now_s < _last_now_s`), overflow guard evidence.py:103-104 (`now_s > MAX_MONOTONIC_SECONDS - LIFETIME`), u64 range check :67; vectors include monotonic/overflow scenarios | high (py) |
| R-LOADNG-014 | B2.2: MUST bound peers retained; MUST NOT silently evict live evidence to admit a new peer | implemented+tested (py) / not-implemented (rust, c) | py `MAX_LOCAL_EVIDENCE_PEERS=32` evidence.py:15; `refresh()` raises `EvidenceCapacityError` instead of evicting (evidence.py:118-122, docstring "live peers are never silently evicted"); vectors bounded-capacity scenarios | high (py) |
| R-LOADNG-015 | B2.3: hop count is the default metric | implemented+tested | C `metric = hop_count` loadng.c:663-664,711-712; py `RouteEntry.metric=hop_count` discovery.py:170,346 + cache metric tests test_cache.py:133-149; rust gradient `hop_count` hybrid.rs:429-438 | high |
| R-LOADNG-016 | B2.3 (optional): ETX-weighted metric `sum(ETX)*100`, lower preferred, max 65535 | not-implemented (optional — no bead per protocol) | No ETX-weighted LOADng metric in any stack; rust `etx` exists only as RPL DIO signal input (rust/lichen-node/src/node.rs:480-509) | high |
| R-LOADNG-017 | B2.4: LOADng messages use ICMPv6 type 158; codes 0=RREQ, 1=RREP, 2=RERR, 3=RACK | implemented+tested (codes 0-2; code 3 reserved, no handler in any stack) | All three codecs: rust loadng.rs:32,69-84 (rejects code 3 as UnknownCode); py messages.py:27,63-69 (`LoadngCode.RACK=3` enum, no `_CLASS_BY_CODE` entry → "unsupported code"); C loadng.h:47-53 (`RACK 3U /* Reserved */`); vectors loadng_messages.json consumed by py python/tests/test_vectors.py:3177, rust rust/lichen-core/tests/loadng_vectors.rs, C lichen/tests/loadng_vectors/main.c + `test_constants` | high |
| R-LOADNG-018 | B2.5: expanding ring after evidence gate — attempts at hop limits 4, 8, 15; each attempt waits RREQ_WAIT_TIME | implemented+tested (rust) / implemented+untested (C) / not-implemented (py) + interop divergence | Rust `EXPANDING_RING=[4,8,15]` loadng.rs:55 + `advance_ring` tests loadng.rs:823-844; C `expanding_ring[]` loadng.c:22-26 + discovery_advance loadng.c:589-607 (state machine untested; only constants tested main.c:538); py has no ring progression (bead b7z9.195) and additionally **receivers drop hop_limit>4 RREQs** discovery.py:149-156, killing rings 8/15 (bead b7z9.194); wait-per-attempt enforced only in C (timeout loadng.c:564,605) | low |
| R-LOADNG-019 | B2.6: RREQ_ID = hash(originator‖destination‖seq); suppress if seen within 10 s | implemented+tested (py) / implemented+untested (C) / not-implemented (rust) | py `SUPPRESS_WINDOW_MS=10_000` discovery.py:60, seen store + RFC1982-stale logic discovery.py:369-425, tests `test_duplicate_rreq_suppressed`/`test_suppression_expires_after_window` test_discovery.py:53,62 + wrap tests; C seen table loadng.c:402-536, window `LICHEN_LOADNG_SUPPRESS_WINDOW_MS 10000U` loadng.h:77 — **no unit test for seen table** (lichen/tests/loadng/main.c RUN list has none); rust: no RREQ receive path (bead b7z9.197). Note: all implementations key on the (originator, destination, seq) tuple rather than hashing it — functionally equivalent identity; the hash is an implementation detail | medium |
| R-LOADNG-020 | B2.7: RREQ flooding subject to §6.3.3 broadcast rate limits; RREQ_RATELIMIT (10/min) is an additional per-node limit on originated RREQs | not-implemented | No originate-side limiter in any stack (see R-LOADNG-004); C admission limiter is the 05-routing §10.3 receive-side rule (loadng.c:41-56, tested main.c:575-630); §6.3.3 broadcast budget never applied to RREQ origination because no stack drives origination (beads b7z9.195/.196/.197) | high (absence) — bead b7z9.199 |

Classification histogram: implemented+tested 6 (R-002 C-only, R-005, R-006,
R-007/008 py+C, R-011/012/013/014 py, R-015, R-017); implemented+untested 3
(R-003 C, R-018 C, R-019 C); divergent 2 (R-001, R-009, plus the R-018 py
receiver divergence); not-implemented 4 (R-004/020, R-010, plus rust/py legs of
R-007/008/011-014/018/019); optional-MAY unimplemented 1 (R-016).

Should-gaps noted (not filed — omission does not break a documented feature):
none beyond the beads above. MAYs: R-016 ETX metric (never filed).