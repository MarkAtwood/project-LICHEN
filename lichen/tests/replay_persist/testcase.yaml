common:
  # R-APPC-009 (spec/appendix-c-safety.md): ASan+UBSan on native_sim builds.
  extra_conf_files:
    - ../../tests/sanitizers.conf
tests:
  lichen.link.replay_persist:
    platform_allow:
      - qemu_x86
    tags:
      - lichen
      - link
      - replay
      - security
