common:
  # R-APPC-009 (spec/appendix-c-safety.md): ASan+UBSan on native_sim builds.
  extra_conf_files:
    - ../../tests/sanitizers.conf
tests:
  lichen.oscore.persist:
    platform_allow:
      - qemu_x86
    tags:
      - lichen
      - oscore
      - settings
      - security
