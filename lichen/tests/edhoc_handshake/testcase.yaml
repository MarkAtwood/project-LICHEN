common:
  # R-APPC-009 (spec/appendix-c-safety.md): ASan+UBSan on native_sim builds.
  extra_conf_files:
    - ../../tests/sanitizers.conf
tests:
  lichen.edhoc.handshake:
    tags: edhoc crypto security
    platform_allow: qemu_x86 native_sim
    integration_platforms:
      - qemu_x86
