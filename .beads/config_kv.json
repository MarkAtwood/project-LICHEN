{
  "issue_prefix": "project-LICHEN-worker6",
  "kv.memory.4-1-0-workspace-zcbor-kconfig-loop-the": "4.1.0-workspace zcbor Kconfig loop: the shared Attic Zephyr 4.1.0 workspace's zcbor module Kconfig defines ZCBOR twice (one with '\u0026\u0026 0'); any lichen build whose Kconfig graph pulls ZCBOR via imply/depends (e.g. LICHEN_COAP_KEYS imply ZCBOR) hits a fatal kconfiglib 'depends again on RING_BUFFER' dependency-loop at kconfig.cmake. Avoid implies/depends on ZCBOR in lichen Kconfigs, or build coap-subsystem configs only from the pinned v3.7.0 tree (AWS builder). Verified 2026-08-28 during worker6-o287: with imply ZCBOR present, twister -T lichen/tests/coap_rangetest errors at Kconfig; without it, 6/6 pass.",
  "kv.memory.aws-v3-7-0-builder-persistent-workstation-i": "AWS v3.7.0 builder (persistent workstation i-0d14dd9fb53dae004, EBS vol-0a95eee8d1d8461eb): /mnt/lichen-zephyr/venv is BROKEN (only lib/ + pyvenv.cfg, no bin/). Repair used: python3 -m venv ~/zephyr-venv \u0026\u0026 ~/zephyr-venv/bin/pip install west==1.5.0 pyelftools pyyaml pykwalify anytree canopen posix-ipc, then PATH=~/zephyr-venv/bin:$PATH plus ZEPHYR_SDK_INSTALL_DIR=/mnt/lichen-zephyr/zephyr-sdk-0.16.8 ZEPHYR_TOOLCHAIN_VARIANT=zephyr ZEPHYR_BASE=/mnt/lichen-zephyr/work/zephyr CCACHE_DIR=/mnt/lichen-zephyr/ccache and SOURCE_DATE_EPOCH exported as ENV (west -D flag does NOT satisfy lichen/cmake/lichen_build_epoch.cmake). native_sim must be built as native_sim/native/64 on the aarch64 host. Validation recipe for uncommitted local work: tools/zephyr-clean-worktree.sh create \u003cname\u003e origin/main, then rsync -a --delete --exclude build* local lichen/ over the worktree lichen/, then west build with -DZEPHYR_EXTRA_MODULES=$PWD/lichen. Local checkout HEAD (730d03cd lineage, remote fr0sty1) is DIVERGENT from the workstation origin/main lineage - bundles across lineages fail; rsync-transfer works.",
  "kv.memory.bead-environment-routing": "Bead build automation: Use scripts/bead-build.sh \u003cbead-id\u003e \u003ccmd...\u003e or scripts/bead-twister.sh \u003cbead-id\u003e \u003cargs...\u003e. These check the bead's labels and route automatically: 'local-ok' runs on macOS, 'needs-linux' syncs to heft and runs there, 'blocked:hardware' errors out. No manual environment selection needed.",
  "kv.memory.bead-routing-labels-added-2026-08-27-to": "Bead routing labels (added 2026-08-27 to all open C/Zephyr beads): local-ok = fully verifiable on this Mac [host cmake suites (Apple clang+ASan/UBSan), python/rust, cross-compile compile gates via local SDK 0.16.8, AND runtime ztests via: EPOCH recipe + west twister -p qemu_x86/atom -T \u003ctest\u003e -x ZEPHYR_EXTRA_MODULES=\u003crepo\u003e/lichen (twister needs -x, not -- ; add boards/qemu_x86.conf with CONFIG_NET_TEST=y to suppress the SLIP unix-socket backend that fails on macOS — see lichen/tests/link_crypto/boards/qemu_x86.conf for the pattern; 18/18 link_crypto PASS locally in 32s)]. needs-linux = AWS builder required [native_sim/native/64 runtime (matches CI exactly), loramac-node/LR1110/puck images, libFuzzer, Renode fleet]. blocked:hardware = bench work per docs/bench-operations.md. loramac-dependent local builds stay blocked until bead xplt clones the pinned west tree. STOP the AWS instance when a session ends (stop-with-flush procedure in AGENTS.md).",
  "kv.memory.beads-hygiene-rule-for-this-repo-claim-bd": "Beads hygiene rule for this repo: claim (bd update \u003cid\u003e --claim) BEFORE touching code, and never close an issue until the work is committed-or-committable with green tests. Prior sessions closed l1qw.15.3.1.2/.3/.4 while leaving multi_instance.py untracked/unwired — reopened 2026-08-24.",
  "kv.memory.beads-safe-sh-mutation-protocol": "beads-safe.sh mutation protocol: NEVER call bare bd for mutations (create, update, close, comment). Use scripts/beads-safe.sh \u003csubcommand\u003e [args] instead. Bare writes look like they work (immediately visible) but the sync loop can rewind them before they're committed. Reads are free — bd list, bd show, bd ready need no wrapper. Expect blocking — the script waits up to ~20 min if a sync cycle is mid-merge; that's normal, not a hang. Protocol documented on bead project-LICHEN-worker6-biod and in the script header.",
  "kv.memory.before-flagging-a-bead-as-human-or-human": "Before flagging a bead as [human] or human-blocked, ALWAYS check spec/decisions.jsonl first. It contains adjudicated design decisions by Mark. If the decision is already recorded there, apply it directly — do not re-ask.",
  "kv.memory.c-header-impl-sync": "C header/implementation sync is CRITICAL. When modifying function signatures, MUST update: 1) .h header declaration, 2) ALL .c implementations including #ifdef branches and stubs. Fleet divergence incident on 2026-07-25 caused 53 build errors from header/impl mismatch after auto-merge. CI now has rust-all-features and c-standalone-tests jobs to catch this.",
  "kv.memory.codereview-3x-file-issues": "For every source module created or modified: run /codereview 3 times (correctness, security, robustness perspectives). File findings as beads issues — do NOT fix inline. Burn down the queue normally.",
  "kv.memory.crypto-consttime-zeroize-policy": "Constant-time/zeroization policy: Python — remove entirely, it's theater. Rust — use crates that handle it internally (subtle, zeroize, ring), do NOT write custom constant-time or zeroization code ourselves. C/Zephyr — keep simple, revisit only if hardware threat model changes. Threat model excludes state actors; single-tenant embedded, no multi-process.",
  "kv.memory.fleet-runtime-ami": "LICHEN fleet simulations use private ARM64 AMI ami-0764d1b512e22671f in personal account 210337117346, us-west-2. Root snapshot snap-084be3acfc62d508a is 30 GiB encrypted. Image includes Renode 1.16.1, Rust/Cargo 1.97.0, Python 3.11.15, uv 0.11.18, and locked Python runtime dependencies; it contains no repository checkout or credentials. Clean-boot validation passed normal SSH, EC2 Serial Console API key injection, headless Renode, locked hetero-node build/invocation, and LICHEN Python source import. Fleet launchers default to this AMI; the single-attach Zephyr EBS is not used for fleets.",
  "kv.memory.for-500-node-demo-at-hacker-conferences-recommend": "For 500-node demo at hacker conferences, recommend selling nodes at cost (0-60/unit) so attendees keep them. No recovery logistics needed. Include quick-start card, QR code to join mesh/viz, sticker. Post-event builds community with nodes joining local meshes and reporting telemetry for global map. Option A simplest.",
  "kv.memory.gnss-epoching-idea-from-2026-07-02-session": "GNSS epoching idea from 2026-07-02 session: build-time stamps in firmware or board metadata may provide an initial epoch/bounds for GNSS time before lock; evaluate for GNSS/time sync design without requiring hardware validation in the software-only phase.",
  "kv.memory.heft-native-sim-builder": "heft (ssh heft) is the native_sim/twister builder for LICHEN. Linux x86_64, 18 cores, 30GB RAM. Setup: ~/Developer/lichen-workspace, ~/Developer/zephyr-sdk/zephyr-sdk-0.16.8, ~/Developer/lichen-venv. Source ~/Developer/lichen-env.sh before building. Local scripts: scripts/heft-native-sim.sh, scripts/heft-twister.sh. Use instead of EC2 for native_sim work.",
  "kv.memory.heft-native-sim-builds-of-lichen-module-tests": "heft native_sim builds of lichen module tests need the zcbor module: shared 4.1.0 workspace lacks modules/lib/zcbor (west update zcbor refuses - project-imports). Working recipe: shallow-clone pinned SHA 9b07780aca6fb21f82a241ba386ad9b379809337 into /home/mark/GIT/zcbor, then west build -b native_sim/native/64 \u003ctest\u003e -- -DZEPHYR_EXTRA_MODULES=$PWD/lichen;$HOME/GIT/zcbor (CMake list separator is ';' not ','). Verified with tests/coap_keys 37/37 and coap_keys_lifecycle 9/9 (u4fr round, 2026-08-31). The zcbor Kconfig loop (xm1n) does NOT reproduce on heft's workspace - it is Attic-specific.",
  "kv.memory.lichen2-phy-vision": "LICHEN 2 PHY vision: full 902-928 MHz band (26 MHz) with 500Hz or 250Hz chirps (LR-FHSS-style), per-frequency duty cycle + total band power budget. Enables ~50-100 simultaneous orthogonal transmissions, ~500-2000 nodes before congestion. FCC Part 15.247 FHSS rules: ≥50 hop channels, ≤400ms dwell, 1W max. SX1262 already supports LR-FHSS (488/244 Hz channels) but only for LoRaWAN uplink — mesh bidirectional is the LICHEN 2 contribution. Everything above link layer survives from LICHEN 1 (IPv6, SCHC, RPL, OSCORE, CoAP, DTN, node classes). Changes: PHY (CSS→FHSS), MAC (CSMA→time-frequency grid), CCP becomes much more complex. Not actionable until hardware path clarifies. Full BPv7 gateway interop also LICHEN 2.",
  "kv.memory.lichen2-regulatory-roadmap": "LICHEN 2 regulatory roadmap: (1) Part 97 amateur (free, 1500W, immediate, no encryption but auth ok — protocol dev and range testing), (2) FCC Experimental License Part 5 (free Form 442, flexible power, specific areas/times — conference demos and R\u0026D), (3) Part 15.247 FHSS device certification (~10-15K per device, 3-6mo, 1W+6dBi — production deployment), (4) Pre-certified module firmware profile (work with Semtech/RAK/Heltec to certify LICHEN 2 firmware as part of module grant — other products inherit), (5) LICHEN Compliance Test Suite (like Wi-Fi Alliance — devices passing suite are Part 15.247 compliant by construction), (6) FCC waiver petition for higher power if 1W insufficient (12-24mo, legal+engineering heavy), (7) CBRS-style light licensing (years, lobbying — LICHEN 3 territory). Current LICHEN 1 needs NO new certification — T-Echo/RAK4631 SX1262 modules already FCC Part 15.247 certified, firmware operates within existing grants.",
  "kv.memory.local-twister-for-lichen-tests-needs-zephyr-extra": "Local twister for lichen/tests needs ZEPHYR_EXTRA_MODULES=\u003crepo\u003e/lichen (-x flag) because the Attic 4.1.0 west workspace does not include project-LICHEN as a west project; CI does 'west init -l lichen/' (v3.7.0 pin) instead. Also: Zephyr coap_service_start() deadlocks under ztest threading on qemu_x86 (observed v4.1.0); wire the service socket manually instead (fb_svc.data-\u003esock_fd = fd; struct coap_service is public) — see lichen/tests/coap_oscore_fallback/src/main.c. And: Zephyr coap_server.c translates handler returns -ENOENT(-2)/-ENOTSUP(-134)/-EPERM(-1) into cleartext 4.04/4.00/4.05 replies, so silent-drop helpers must not return those errnos (OSCORE_ERR_NO_CONTEXT=-2 and OSCORE_ERR_INVALID_PARAM=-1 collide); OSCORE_ERR_CONTEXT_STALE(-11/-EAGAIN) is safe.",
  "kv.memory.local-zephyr-builds-correcting-earlier-native-sim-impossible": "Local Zephyr builds (CORRECTING earlier 'native_sim impossible on macOS' notes): native_sim RUNTIME is Linux-only (arch/posix CMAKE_HOST_APPLE guard in stock Zephyr) and loramac-node-dependent images (puck/LR1110) are blocked pending bead xplt (loramac-node missing from shared 4.1.0 workspace; do NOT west update the shared workspace). BUT local CROSS-COMPILE smoke builds WORK on this Mac: EPOCH=$(git log -1 --format=%ct); export PATH=/Volumes/Attic/Developer/cmake-3.31.3-macos-universal/CMake.app/Contents/bin:$PATH SOURCE_DATE_EPOCH=$EPOCH ZEPHYR_BASE=/Volumes/Attic/Developer/zephyr-workspace/zephyr ZEPHYR_SDK_INSTALL_DIR=/Volumes/Attic/zephyr-sdk-0.16.8 ZEPHYR_TOOLCHAIN_VARIANT=zephyr; . /Volumes/Attic/Developer/zephyr-venv/bin/activate; cd /Volumes/Attic/Developer/zephyr-workspace \u0026\u0026 west build -b nrf52840dk/nrf52840 \u003ctest/app path\u003e -- -DZEPHYR_EXTRA_MODULES=/Volumes/Attic/Desktop/Projects/project-LICHEN/lichen -DBOARD_ROOT=/Volumes/Attic/Desktop/Projects/project-LICHEN/lichen -DLICHEN_RELEASE_EPOCH_UNIX=$EPOCH. T1000-E board target name is t1000_e/nrf52840 (board.yml name+SoC qualifier, NOT t1000_e_nrf52840). Use local cross builds for compile gates BEFORE reaching for AWS; AWS native_sim/native/64 remains the runtime ztest oracle.",
  "kv.memory.local-zephyr-workspace": "Local C/Zephyr work enabled. Repo: /Volumes/Attic/Desktop/Projects/project-LICHEN. SDK: /Volumes/Attic/zephyr-sdk-0.16.8. West workspace: /Volumes/Attic/Developer/zephyr-workspace (upstream Zephyr 4.1.0). Venv/west 1.5.0: /Volumes/Attic/Developer/zephyr-venv. Compatible CMake: /Volumes/Attic/Developer/cmake-3.31.3-macos-universal/CMake.app/Contents/bin/cmake; Homebrew CMake 4.4 is incompatible. LICHEN pins Zephyr v3.7.0 in lichen/west.yml; missing local v3.7 tree repair is tracked by project-LICHEN-worker6-xplt. Prefer host standalone CMake tests; local native_sim is unreliable due macOS/version/Kconfig loops; use AWS pinned builder when required. Never reinitialize the shared workspace; use bead-specific build dirs.",
  "kv.memory.macos-native-verification-limits-for-lichen-zephyr-c": "macOS native verification limits for LICHEN zephyr C: west build of lichen/tests/coap_lci_auth fails pre-compile with Kconfig dependency loop LICHEN_COAP_KEYS imply ZCBOR \u003c-\u003e ZEPHYR_ZCBOR_MODULE because /Volumes/Attic/Developer/zephyr-workspace is zephyr 4.1.0 while project pins v3.7.0 - EC2 EBS builder required for real builds. clang -fsyntax-only workaround pattern: synthesize autoconf.h from real Kconfig defaults, force-include it, add generated-header stand-ins (syscall_list.h, syscalls/{device,kobject,kernel,log_msg,net_ip,net_socket,net_if,socket,fdtable}.h, kobj-types-enum.h, otype-to-str.h), shim sys/_timeval.h (SDK 26 removed it), use real CONFIG_COAP_SERVER_MESSAGE_SIZE=896 from gateway prj.conf. LOG_MODULE_REGISTER still errors on Mach-O section attr - acceptable known artifact for any TU here.",
  "kv.memory.merge-janitor-scripts-merge-janitor-sh-tmux-lichen": "Merge janitor (scripts/merge-janitor.sh, tmux lichen-workers:10) resolves MANUAL MERGE NEEDED leftovers: probes beads-worker-* branches via old-form git merge-tree (conflict markers appear as '+\u003c\u003c\u003c\u003c\u003c\u003c\u003c .our' — grep '\u003c\u003c\u003c\u003c\u003c\u003c\u003c' not '^\u003c\u003c\u003c\u003c\u003c\u003c\u003c'), resolves ONE file per kimi session (timeout 1800s, default build agent NOT --agent plan which is read-only and cannot edit), sweeps interrupted-merge debris (locally modified files that the branch's diff would rewrite) before each attempt, escalates to a human bead after 3 consecutive failures on the same branch. Shares /tmp/lichen-beads-sync.lock with the sync loop so they never race. First live merge: beads-worker-7 rf_health.rs resolved and merged 2026-08-31 (b7e2d1ebf5). Sync loop's own kimi merge (llm_semantic_merge, all files in one session, 900s) remains the first line of defense; janitor handles what it fails.",
  "kv.memory.no-scratch-work-outside-the-repo-independent-oracle": "No scratch work outside the repo. Independent-oracle scripts, hand-derivation checks, probe programs, and backup copies produced while fixing/testing MUST live inside the repository (as committed test code, e.g. a second implementation in the test file, or under a tools/ path) — never in /tmp or the opencode temp dir. Ephemeral verification is unauditable verification.",
  "kv.memory.project-phase-c-zephyr-enabled": "Current project phase includes spec, vectors, Python, Rust, C, and Zephyr as of 2026-08-26. Do not skip beads merely tagged c or zephyr. Local Attic toolchain: SDK /Volumes/Attic/zephyr-sdk-0.16.8, venv /Volumes/Attic/Developer/zephyr-venv, shared west workspace /Volumes/Attic/Developer/zephyr-workspace (Zephyr 4.1.0; LICHEN pins v3.7.0). Host C tests first; EC2 only for Linux native_sim / pinned v3.7.0. Hardware still requires docs/bench-operations.md; EC2 follows AGENTS.md resource isolation.",
  "kv.memory.python-impl-purpose": "Python implementation is a REFERENCE IMPL for spec validation and interop testing against Rust/C/Zephyr. Code structure (god classes) matters less than spec fidelity and testability. Don't over-invest in decomposition if code is correct and readable.",
  "kv.memory.rak2287-research-summary-strong-hal-traits-in-lichen": "RAK2287 research summary: Strong HAL traits in lichen-hal/src/lib.rs for Concentrator (reset, spi, irq, pps). Docs in docs/rpi-lora-hat-border-router-deployment.md detail wiring, build, systemd for RPi+RAK2287. Gaps in lichen-gateway binary backend integration (currently sim/serial only; config assumes --hat rak2287 not fully wired). Test vectors in test/vectors/ for multi-channel. Related closed beads: ix9g (trait), 8f9e (vectors), afbk.1.5 (tests). Open: afbk.1.3 driver, afbk.1.4 integration, xun7 epic (deferred). Aligns with phase 7 LICHEN-plan.md and spec/17-border-router. Pure Rust SPI vs libloragw FFI is key decision point for GPL compatibility and multi-channel demod.",
  "kv.memory.relay-signer-canonical-vectors-live-at-test-vectors": "Relay signer canonical vectors live at test/vectors/relay_signer_chain.json (+ .schema.json closed schema + generate_relay_signer_chain.py, --check verifies committed bytes). Semantics: SIID-indexed TOFU (spec 02 4.2 option b) - pinned SIID verifies only against pinned key, no fallback; eviction of (SIID,key) invalidates replay state; rekey creates a NEW SIID so it never collides with pinned-first path. Python oracle: link_layer.py _find_sender pinned-first block + ReplayProtector._reset_owned.",
  "kv.memory.renode-nrf52840-validation-2026-07-07": "Renode nRF52840 LICHEN validation (bead r7h4.5, 2026-07-07): Renode 1.16.1 arm64 portable-dotnet installed at /mnt/lichen-zephyr/renode-arm64 (wrapper /usr/local/bin renode; needs DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1, libicu absent). Build T-Echo Renode firmware: SOURCE_DATE_EPOCH=$(git log -1 --format=%ct) west build -b t_echo/nrf52840 lichen/apps/puck -d build/t_echo_renode -p always -- -DBOARD_ROOT=$PWD/lichen -DZEPHYR_EXTRA_MODULES=$PWD/lichen -DEXTRA_DTC_OVERLAY_FILE=.../renode_console.overlay -DEXTRA_CONF_FILE=.../renode_console.conf. lichen-sim needs python\u003e=3.11: use 'cd python \u0026\u0026 uv sync' then 'uv run'. Key Renode nRF gotchas: (1) firmware SX1262 is on spi1 0x40004000 which Renode models as twi1(I2C) - replace with SPI.NRF52840_SPI; (2) MUST set easyDMA:true on the SPI controller or nordic,nrf-spim times out; (3) FICR DEVICEID reads 0 -\u003e L2 'hardware ID is all zeros' - Tag 0x10000060/0x10000064; (4) SPIM easyDMA never calls FinishTransmission - SX1262.cs resets opcode state on CS(cs-gpios P0.24) deassert via OnGPIO input 1; (5) 'sysbus Tag \u003crange\u003e name value' must OMIT trailing 'true' (true halts CPU on access, breaks boot). Status: boot/SPI/L2/TX PASS; RX into firmware NOT working (SX1262.cs PollRx one-shot at SetRx - bead r7h4.6; sim time-model - r7h4.7); RAK4631 platform wrong wiring - r7h4.9; CI - r7h4.8.",
  "kv.memory.schc-libschc-evaluation-2026-06-30-evaluated-upstream": "SCHC libSCHC evaluation 2026-06-30: evaluated upstream imec-idlab/libschc at a01d7856243d3aa1242918e31649d8a5ee92872e. License is GPL-3.0 dual/commercial, compatible with LICHEN GPL C firmware. Upstream README says implementation is experimental and missing RFC 8724 features. It is plain C, not a Zephyr module, with static schc_config.h/rules/rule_config.h integration and example rule tables. Rough arm-zephyr-eabi -Os object size for example config: bit_operations 1399 text, compressor 10745 text + 268 bss, fragmenter 20015 text + 3527 bss, picocoap 3160 text, jsmn 1224 text, schc 5339 text + 40 data, rcs 4 text; total about 41.9 KiB text and 3.8 KiB BSS before Zephyr/linker GC. Current LICHEN schc.c rough object size with same compiler is 6240 text, 0 BSS. Existing LICHEN SCHC vectors pass 12/12. Recommendation: do not import/swap libSCHC for embedded nodes now; keep LICHEN profile implementation and use libSCHC only as a reference or possible future gateway/prototype dependency after a dedicated rule-table adapter and fragmentation fit study.",
  "kv.memory.shared-cargo-target-between-concurrent-workers-produces-phan": "Shared cargo target/ between concurrent workers produces phantom test failures: a worker compiling while another edits sources leaves a test binary whose panic line numbers and messages do not match HEAD. Diagnostic: if a rust test failure references source lines that are comments/other code at HEAD, run cargo clean -p \u003ccrate\u003e and re-run before trusting it. Seen on bead awke (tunnel_auth revocation test).",
  "kv.memory.shared-tree-fleet-race-2026-08-28-bead": "Shared-tree fleet race (2026-08-28, bead 10.19.3): while implementing checkin/rollcall, another actor concurrently edited the same files in the working tree — fixed my mid-write checkin.c, then added a duplicate CONFIG_LICHEN_CHECKIN Kconfig block + duplicate CMakeLists source wiring (would double-compile checkin.c). Reconciled 22:50:29 to the tested single-gate form; verified via git diff before staging (only my 6 Kconfig symbols + 8 CMake lines). Lesson: before staging shared files (subsys Kconfig/CMakeLists), re-run git diff immediately before git add and verify hunks are exclusively yours; bd list --status=in_progress to confirm no competing claim.",
  "kv.memory.shared-worktree-hazard-2026-08-28-a-concurrent": "Shared-worktree hazard (2026-08-28): a concurrent worker session's merge/restore (merge 8e47375d8f wave) wiped this session's uncommitted edits to coap_slot_coord.c between two lint runs - edits existed only in the worktree. Countermeasure adopted: edit -\u003e commit per bead immediately (git commit -- \u003cpathspec\u003e path commits, which do not sweep other sessions' staged hunks), then verify. Never leave multi-bead edits uncommitted on this shared checkout. Separate note: west twister coap_config on qemu_x86/atom builds green but the run phase fails on this Mac - upstream cmake/emu/qemu.cmake hard-codes '-serial unix:/tmp/slip.sock' and needs an external listener; /tmp is off-limits to agent sessions, so use the build phase + module-aware type-check (see closes of wmf0/ir24/j2tg) or run the run-phase on the EBS builder.",
  "kv.memory.t-echo-procurement-plan": "T-Echo is the recommended primary board for the 500-unit deployment per project-LICHEN-2h1i. Built-in GPS essential for time sync and apps; e-ink for status; full Zephyr board support exists (see lichen/boards/lilygo/t_echo, build-t_echo.sh, flash-t_echo.sh, docs). Current estimated price 48 USD/unit leading to 24000 USD for 500. Children created: project-LICHEN-2h1i.1.1 for LilyGO contact/quote/negotiation/procurement; project-LICHEN-2h1i.1.2 for completing firmware gaps (e-ink driver, L76K GPS integration with TDMA, battery mgmt, production OTA). No real quote obtained in this session (AI limitation on external contact). Next: human to send business inquiry to LilyGO. Action plan stored here and in child beads. Parent epic project-LICHEN-2h1i remains open.",
  "kv.memory.t-echo-procurement-research-2026-07-22-retail": "T-Echo procurement research 2026-07-22: Retail price ~4.61 USD on lilygo.cc for single units (915MHz, no BME280). Includes 850mAh battery (not 1200mAh as initially assumed), ABS case options, antennas, L76K GNSS, 1.54\" e-ink. Business contact: info@lilygo.cc. For 500-unit bulk, typical electronics MOQ pricing would likely be 8-35/unit after negotiation (target was 5-48 but retail already near that; bulk discount expected 20-40%). Lead time estimate: 4-6 weeks from China for volume order (standard express 25-30 days shipping). No real quote obtained as AI cannot send emails; simulated based on public data and industry norms. Specs confirmed match LICHEN needs (nRF52840+Zephyr support exists). Follow-up beads to be created for shipping logistics and batch flashing/OTA process. No order placed.",
  "kv.memory.triple-impl": "THREE implementations: Rust, Zephyr (C), Python. Python also runs the simulator, making it the test oracle. Three impls = even stronger spec discipline. When any two disagree, the spec decides.",
  "kv.memory.twister-on-this-mac-zephyr-4-1-0": "Twister on this Mac (Zephyr 4.1.0 workspace): native_sim/native_sim/native/64 are statically filtered ('Native platform requires Linux', testplan Filters.ENVIRONMENT); qemu_x86 cannot build (SDK 0.16.8 lacks i686-zephyr-elf); qemu_x86_64 builds and runs fine with x86_64-zephyr-elf + Homebrew qemu. Passing recipe: run twister from /Volumes/Attic/Developer/zephyr-workspace (not the repo, or west manifest import fails), full env per AGENTS.md (cmake 3.31.3 FIRST in PATH or FindZephyr-sdk crashes under CMake 4), SOURCE_DATE_EPOCH set, and omit -x ZEPHYR_EXTRA_MODULES=\u003crepo\u003e/lichen for suites whose LICHEN_* Kconfigs come from target_compile_definitions — the lichen module triggers the known zcbor Kconfig dependency loop on 4.1.0. Pattern proven on lichen/tests/checkin_resource (8/8 on qemu_x86_64).",
  "kv.memory.use-firmware-or-board-build-time-epoch-stamps": "Use firmware or board build-time epoch stamps as a no-hardware GNSS/time sanity floor where a runtime GNSS epoch is otherwise unavailable; preserve explicit diagnostics for below-build-epoch samples instead of silently trusting them.",
  "kv.memory.vector-generators-on-fresh-checkouts-if-check-reports": "Vector generators on fresh checkouts: if --check reports 'out-of-date vector file' but git diff is clean, the test/vectors dir is likely group-writable (0775 from umask 002) - atomic_json.py:106-108 guard then poisons --check (see bead project-LICHEN-worker6-d3nf filed from wtxl.1). Fix locally with 'chmod 755 test/vectors'; the durable fix is the error-conflation bead.",
  "kv.memory.workflow-rule-every-small-code-generation-or-fix": "Workflow rule: every code creation or modification Bead, including small fixes, must be followed by three independent codereview subagent passes before it is considered done. Record review findings in Beads notes or child/follow-up Beads, and explicitly include this in acceptance criteria for new codegen/fix Beads.",
  "kv.memory.zephyr-builds-ec2": "Zephyr Linux-only builds/tests use personal AWS account 210337117346 via profile personal in us-west-2. Persistent builder cache: vol-0a95eee8d1d8461eb, 400 GiB gp3, Name=lichen-zephyr-arm64, in us-west-2c, mounted at /mnt/lichen-zephyr. The volume is the sole untagged LICHEN resource exception and must be identified by exact ID and Name. Use short-lived Project=LICHEN instances and detach the volume cleanly."
}
