{
  "id": "project-LICHEN-worker6-l1qw.20.4",
  "title": "Rust receiver replay gate: pure claim_seq high-water (drop (superframe,seq) tuple compare)",
  "description": "Child of l1qw.20 (receiver semantics leg). SlotClaimVerifier::verify (rust/lichen-gateway/src/slot.rs:849-856) rejects only when (superframe_id, claim_sequence) tuple fails to advance. spec/08 step 8 + claim_seq persistence table mandate a PURE claim_seq high-water per gateway IID: reject iff claim_seq \u003c= cached_seq, independent of superframe. Divergence: cached (N,5), current superframe N+1, validly-signed claim (N+1,4) is ACCEPTED by Rust but MUST be rejected (4 \u003c= 5) — this defeats the anti-rollback design for rebooted/NVS-wiped senders (spec: re-init to 0 after wipe must be rejected by coordinators holding the old high-water). Fix: compare claim_sequence only; keep last_seen entries (iid, superframe, seq) and the sealed SlotReplaySnapshot format UNCHANGED so persisted state loads without migration. Keep StaleSuperframe exact-match gate upstream. Keep loser-reclaim (higher seq, same superframe) working. Align with test/vectors/gcp_slot_claim_cose_sign1.json claim_seq_replay_equal/claim_seq_replay_lower. Note: capacity 256/no-LRU is nug9's scope, not this bead.",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T01:08:05.010329469Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-09T01:27:17.490944596Z",
  "started_at": "2026-09-09T01:08:38.350166021Z",
  "closed_at": "2026-09-09T01:27:17.490944596Z",
  "close_reason": "Landed: SlotClaimVerifier::verify replay gate (rust/lichen-gateway/src/slot.rs:857) now enforces the pure claim_seq high-water per gateway IID per spec/08 GCP-6.5 step 8 — reject iff claim_seq \u003c= cached_seq, superframe-independent. Old (superframe, seq) tuple ordering admitted seq rollback (seq 4 after 5) and equal-seq re-use in a newer superframe, defeating the anti-rollback design for rebooted/NVS-wiped senders; fixed with a 1-line comparison change. Persisted SlotReplaySnapshot format and last_seen tuple storage UNCHANGED (only the comparison reads .1) so old sealed state loads without migration; loser-reclaim (higher seq, same or newer superframe) preserved; StaleSuperframe exact-match gate unchanged upstream; sig-check-first ordering unchanged. Tests: new regression replay_gate_is_pure_claim_seq_highwater_across_superframes covers rollback+equal rejection and legit advance across the superframe boundary; validated to FAIL on the old gate and PASS on the new. Full gates: cargo test -p lichen-gateway lib 258 passed (1 ignored), integration 18 passed, e2e 21/22 with the single failure being runtime_ingress_dispatches_authenticated_gcp_slot_claim — pre-existing deliberate red documented in human bead ljgs (verified red on clean HEAD via stash). clippy -D warnings: all 8 errors pre-existing on clean HEAD, none in the diff (slot.rs hit is the untouched SlotClaimRateLimiter Default lint at :691). rustfmt clean. Codereview: 3x delegated to reviewer model (kimi-k3) — correctness PASS, security PASS, edge-cases PASS; each independently flagged the Python replay-cache divergence, already filed as l1qw.20.5 (comment enriched with their specifics). Out-of-diff residuals intentionally NOT in this bead: 256-capacity/no-LRU (nug9 scope), sender-side counter (l1qw.20.6), Python cache (l1qw.20.5).",
  "labels": [
    "gateway",
    "gcp",
    "lci",
    "spec",
    "spec-gap"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-l1qw.20.4",
      "depends_on_id": "project-LICHEN-worker6-l1qw.20",
      "type": "parent-child",
      "created_at": "2026-09-09T01:08:05.184196253Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
