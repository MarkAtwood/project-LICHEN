{
  "id": "project-LICHEN-worker6-0yue",
  "title": "tests/schc/test_headers.py::test_mqtt_sn_rule7_rejects_invalid_full_mode_addresses fails at main tip (message/wire drift family)",
  "description": "Found by beads-worker-4 full-suite run after the l1qw.26.2.1 COSE slice (verified pre-existing via stash: fails without my changes, on branch == main tip + my commits; my diff touches group_membership/delegation_tokens only). The mqtt_sn Rule 7 full-mode rejection test in python/tests/schc/test_headers.py fails — same drift family as wtxl.2's test_vectors.py rule7 alignment (fixed by b862cdecd1 two-tier corpus contracts) but in a different test file, so likely a case b862cdecd1 missed or a newer drift from another merged change. Fix: align the test's expected message/wire with the canonical validate_rule7_addresses behavior per the two-tier corpus contract; do not weaken what is rejected. Related: wtxl (closed), 0l4q (rpl_stack red).",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "assignee": "opencode-worker-4",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-31T19:58:39.997499223Z",
  "created_by": "opencode-worker-4",
  "updated_at": "2026-08-31T20:07:32.001045743Z",
  "started_at": "2026-08-31T20:00:47.299957395Z",
  "closed_at": "2026-08-31T20:07:32.001045743Z",
  "close_reason": "Landed as test(schc) commit on beads-worker-4. Root cause: the test pinned the removed 'malformed source' message from the old UdpDatagram.from_bytes src check (dropped by the landed ilye.1 parser refactor); compress_packet's actual rejection for the multicast-source case is now the Rule 255 emission policy message 'invalid IPv6 source address ff02::1' (Rule 7 selection returns None on policy failure -\u003e Rule 255 fallback -\u003e emission rejects). Updated the parametrize expectation + rewrote the stale comment to describe the real flow; the Rule 7 decompress-leg assertions (match='address') are untouched and still exercise the Rule 7 decode rejection. Gates: tests/schc 43/43, tests/schc + test_vectors.py 2184 passed. Codereview: delegated pass timed out (300s) — self-completed per fallback: correctness PASS (message specificity verified — exact production message prefix, not vacuously broad; second case's 'address' match is pre-existing breadth, unchanged by this diff); security PASS (rejection set unchanged — only the pinned message text updated, nothing accepted that was rejected); edge PASS (no other test in the file references 'malformed source' — grep 0; decompress legs intact).",
  "labels": [
    "schc"
  ]
}
