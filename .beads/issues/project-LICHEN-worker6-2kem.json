{
  "id": "project-LICHEN-worker6-2kem",
  "title": "one-shot NON capability re-announce has no recovery when the datagram is lost",
  "description": "Robustness/security review of 99sg.2 (pass 2, finding 3): node.py _reannounce_capabilities_to_new_root drains take_root_changes() before transmission and a send() failure only logs; the NON is never retransmitted and the ~200B announcement fits one frame so SCHC fragment ARQ does not apply. spec/06-security.md:1046-1048 MUSTs re-announce after root change; a jammed/lost single datagram leaves the new root's capability table permanently missing the node until the next root-change event. Needs bounded retry, a refresh trigger (spec 8.12 lists approaching-expiry refresh), or explicit best-effort sign-off.",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T17:42:55.997436676Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-10T02:07:37.262061236Z",
  "closed_at": "2026-09-10T02:07:37.262061236Z",
  "close_reason": "FIXED on beads-worker-4 (landed via janitor syncs eb2429a7ef + 3e5e094a5d): failed capability re-announces now retry with a hard bound — _retry_capability_announcement runs attempts 2..CAPABILITY_ANNOUNCE_MAX_ATTEMPTS=3 with jittered delay random.uniform(2.5,5.0)s (jitter desynchronizes mesh-wide retries and defeats jamming-predictable fixed windows, same rationale as scheduled_send). Round-2/3 review fixes folded in: retry aborts if dodag.dodag_id != new_root before each attempt (stale-root disclosure, v89j); one-pending supersede (cancel+sync-discard prior retries); _cleanup_started cancels retry tasks AFTER the receive-task cancel and the receive gather itself is wrapped in try/except BaseException so a cancelled cleanup cannot skip it. 6 new tests: recovery-after-loss (seq 2 delivered), abandon-after-3 (independent oracle calls==3), cleanup-cancels-pending (real 5s delay, suite \u003c0.4s proves cancel), stale-root abort, supersede, jitter pinning (midpoint 3.75). 16/16 + node suite pass under -W error::RuntimeWarning; ruff/mypy clean. 3x codereview across 3 convergence rounds (kimi): final round correctness PASS, security PASS, robustness PASS. One finding filed: wzsf P3 (fragment-session leak window when a retry fires mid-cleanup).",
  "labels": [
    "python",
    "coap"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-2kem",
      "depends_on_id": "project-LICHEN-worker6-99sg.2",
      "type": "discovered-from",
      "created_at": "2026-09-09T17:42:56.118301965Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
