{
  "id": "project-LICHEN-worker6-m4yk",
  "title": "ping_l2: UDP-observer test fixture ordering — TX fails -ENOKEY after teardown re-init",
  "description": "Found while verifying 0l5j.4 (worker-1, heft ping_l2 native_sim): lichen/tests/ping_l2/src/main.c test_udp_payload_reaches_socket_after_l2_injection (:450) fails with TxError no-signing-key (-126) because it runs after test_disable_retries_incomplete_queue_destruction (:578), whose teardown wipes the shared link_ctx signing key. That test calls reprovision_after_reinit() at its end, which loads the key and re-adds the peer — but the UDP test's send still fails -ENOKEY, so the reprovision does not cover the ordering in which ztest runs the cases. INVESTIGATE: (a) confirm the actual ztest execution order vs definition order; (b) check whether reprovision_after_reinit's lichen_l2_test_load_key targets the same link_ctx the L2 send path reads (the disable test ran lichen_lora_l2_init() which may re-init a DIFFERENT context instance); (c) either make reprovision_after_reinit idempotent across the lichen_lora_l2 re-init boundary or reorder the udp test before the teardown test. CONTEXT: 12/13 ping_l2 ztests now pass on native_sim/native/64 (this commit fixed the duty-vector threshold bugs — spec 02a.9 says Density\u003e10 = dense, so dense_start_region0 is density 11 not 9, and moderate_end is 10 not 8; the C implementation adaptive_duty_permille was already spec-correct). Remaining: 1 UDP-observer failure. Discovered from 0l5j.4 verification.",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-04T19:07:44.223448032Z",
  "created_by": "opencode-worker-1",
  "updated_at": "2026-09-09T15:28:43.208291817Z",
  "started_at": "2026-09-09T15:27:13.46485905Z",
  "closed_at": "2026-09-09T15:28:43.208291817Z",
  "close_reason": "ALREADY-FIXED, verified green. Ran ping_l2 suite on heft native_sim/native/64 (west build -p always + run): 8/8 PASS, including the bead's failing test_udp_payload_reaches_socket_after_l2_injection (0.290s) and test_disable_retries_incomplete_queue_destruction. The fix is already in tree: main.c:487-493 keys recovery off lichen_lora_l2_is_running()/needs_reinit() state (not teardown folklore) and calls idempotent reprovision_after_reinit() (-EALREADY tolerated) explicitly per this bead — comment at :492 cites project-LICHEN-worker6-m4yk. The two -126 (no signing key) lines at boot are pre-init probes before ping_l2_setup loads the deterministic key at :198; not failures. No code change needed; test-ordering defect (a)(b)(c) resolved by the state-keyed recovery. Investigate items answered: ztest runs sorted section order; reprovision targets the live link_ctx (atomic restored at :167); the healthy-path cycle is skipped to avoid deafening a live RX arm.",
  "labels": [
    "zephyr",
    "tests"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-m4yk",
      "depends_on_id": "project-LICHEN-worker6-0l5j.4",
      "type": "discovered-from",
      "created_at": "2026-09-04T19:07:44.339248355Z",
      "created_by": "opencode-worker-1",
      "metadata": "{}"
    }
  ]
}
