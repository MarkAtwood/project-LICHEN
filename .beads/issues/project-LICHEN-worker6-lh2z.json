{
  "id": "project-LICHEN-worker6-lh2z",
  "title": "Fleet waste alarm resets its 24h baseline",
  "description": "Read-only viability assessment found scripts/fleet-guards.sh:88-114 requires AGE \u003e 86400 before computing cost-per-close, but overwrites burn-snapshot.json with the current timestamp on every cycle (default 10 minutes). Under normal continuous operation AGE never reaches 24 hours, so the waste alarm does not evaluate. This is separate from the intentional budget policy (the 2000 USD milestone is advisory and the ceiling was removed); do not change that policy as part of fixing the baseline. No fleet state or source changed in the assessment.",
  "acceptance_criteria": "Use a retained baseline or rolling history so a deterministic independently specified sequence of 10-minute observations crosses 24 hours and triggers the configured cost-per-close alarm. Cover first run and restart behavior without real provider calls; preserve existing budget/pause policy. Run tests and three independent correctness/security/robustness reviews before closure.",
  "status": "closed",
  "priority": 1,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T19:37:18.4382825Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-10T02:29:40.218880804Z",
  "started_at": "2026-09-10T02:18:19.662435024Z",
  "closed_at": "2026-09-10T02:29:40.218880804Z",
  "close_reason": "Fixed: snapshot logic extracted to scripts/fleet_burn.py with a retained baseline — written only on first valid observation or after a full \u003e24h window elapses and is evaluated (previously overwritten every cycle so AGE\u003e86400 never fired). Shell block in fleet-guards.sh now a thin call into the helper; marker set/clear parity preserved (strict \u003e3.0 threshold; closes\u003c=0/burn\u003c=0 roll without eval; used=0 invalid observation never poisons the baseline — strict improvement over old). Atomic tmp+os.replace writes. 12 new pytest tests (deterministic epochs, hand-computed literal oracles): 12/12 pass; bash -n clean; end-to-end shell chain smoke OK. 3x codereview delegated to kimi-k3 (correctness/security/edge) — 4 findings filed as new beads: P2 fail-open on helper error, P3 uncaught OSError in _write_baseline, P3 semantic baseline validation (negative usage/future ts), P3 /tmp state-dir ownership. Reviewers verified: window math parity, corrupt/missing/symlink safety, concurrent-instance race-safety, test oracle independence.",
  "labels": [
    "fleet"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-lh2z",
      "depends_on_id": "project-LICHEN-worker6-4rmb",
      "type": "discovered-from",
      "created_at": "2026-09-09T19:37:18.55885584Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
