{
  "id": "project-LICHEN-ntx4",
  "title": "Ack.to_bytes uses fixed bitmap encoding that may not match RFC 8724",
  "description": "File: python/src/lichen/schc/fragment.py:105-119\n\nAck.to_bytes() encodes the ACK as:\n  byte1 = ((self.window \u0026 1) \u003c\u003c _W_SHIFT) | (0x01 if self.complete else 0)\n  ...\n  body = (bits \u003c\u003c pad).to_bytes((n + pad) // 8, 'big') if n else b''\n  return bytes([self.rule_id, byte1, n]) + body\n\nThis format is:\n  [rule_id][W|flags][bitmap_len][bitmap...]\n\nHowever, RFC 8724 section 8.3.3 specifies the ACK format differently depending on the mode. For ACK-on-Error with 1-bit window:\n  [rule_id][W][C][bitmap (if C=0)]\n\nWhere:\n- W is 1 bit\n- C is 1 bit (indicates completion)\n- Bitmap follows only if C=0 (incomplete)\n\nThe current implementation:\n1. Uses a full byte for W+flags instead of bit-packing\n2. Always sends the bitmap length explicitly\n3. Sends bitmap even when complete=True\n\nThis may cause interoperability issues with strict RFC 8724 implementations.\n\nFix: Verify the ACK format against RFC 8724 section 8.3.3 and adjust encoding accordingly.\n\nCategory: PROTOCOL",
  "status": "closed",
  "priority": 3,
  "issue_type": "bug",
  "assignee": "AI Agent",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-07-12T06:30:47.895897Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-07-22T19:47:46.937730867Z",
  "started_at": "2026-07-22T19:34:22.256533696Z",
  "closed_at": "2026-07-22T19:47:46.937730867Z",
  "close_reason": "Fixed per all 3 codereview passes (project-LICHEN-ntx4.1-3). Ack.to_bytes now omits n+bitmap on complete=True for 2-byte ACKs; from_bytes supports both paths with conditional logic. Tests updated to independent hardcoded byte oracles derived from RFC bit-packing (not self roundtrip). Verified with pytest on test_fragment.py and test_reassembly.py (all pass). Minimal grep/read used only on target files. Style, no comments added, test integrity followed. No P0-P2 in reviews. Beads workflow complete.",
  "labels": [
    "human"
  ]
}
