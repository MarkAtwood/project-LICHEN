{
  "id": "project-LICHEN-worker6-p21s",
  "title": "lichen-node: clippy::duplicated_attributes fails -D warnings (dao_tx_timing.rs:14 vs mod.rs:7)",
  "description": "Found while verifying u80l. rust/lichen-node/src/rpl_stack/dao_tx_timing.rs:14 has #![allow(dead_code)] while rpl_stack/mod.rs:7 already applies #[allow(dead_code)] to 'mod dao_tx_timing;' (comment: dead until b7z9.16 wires the scheduler). cargo clippy -p lichen-node --all-features -- -D warnings errors with clippy::duplicated_attributes (rust 1.96). Fix: remove the in-file inner attribute at dao_tx_timing.rs:14 (mod.rs declaration already covers the whole module); keep mod.rs's comment. Blocks the workspace-wide clippy -D warnings gate (kpv0.6 tracks the full workspace goal; this is one blocker of it).",
  "status": "closed",
  "priority": 3,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T13:49:47.308048767Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-09T13:58:38.647692823Z",
  "started_at": "2026-09-09T13:49:54.042551874Z",
  "closed_at": "2026-09-09T13:58:38.647692823Z",
  "close_reason": "Fixed: removed the inner #![allow(dead_code)] at dao_tx_timing.rs:14 (+blank); mod.rs:7's outer #[allow(dead_code)] on the mod declaration already covers the whole module, so dead-code behavior is unchanged and clippy::duplicated_attributes no longer fires (verified: cargo clippy -p lichen-node --lib --all-features -- -D warnings exits 0; the lib check compiles clean). Pre-existing 4-test rpl_stack failure set is identical with and without the change (60/4 both ways; attribute-only diff cannot affect runtime — failures tracked by 5217). Next pre-existing --all-features clippy blocker (unused_mut tests.rs:3029) filed as 725b. 3x delegated codereview (kimi-k3): all PASS.",
  "labels": [
    "rust"
  ]
}
