{
  "id": "project-LICHEN-worker6-wjet",
  "title": "lichen-node gateway-union build fails: begin_unprotect_observe_response (oscore 0.1.1 gap)",
  "description": "Discovered during heft cutover verification. cargo check -p lichen-gateway --lib fails: (1) E0599 no method begin_unprotect_observe_response on \u0026mut lichen_oscore::Context (secure.rs:1361); (2) E0282 type annotations needed payload.to_vec() (secure.rs:1387). Lone -p lichen-node --lib passes (0.5s) — failure only appears under gateway feature unification. CONTEXT: lichen-oscore is a re-export wrapper (pub use oscore::{Context,...}) around the external oscore crate; lichen-node/secure.rs uses the observe-response API (begin_unprotect_observe_response) which EXISTS in the vendored rust/crates/oscore (lib.rs:1258, from Mark's uncommitted Mac tree state) but NOT in crates.io oscore 0.1.1 (published from commit 7434512 without the observe work). Hypotheses to check: feature gating on the method (oscore/std?), wrapper re-export vs feature unification resolving oscore to the registry version, or stale incremental cache. ALSO: Mark's Mac oscore working tree has ~1034 lines uncommitted (observe API + EDHOC kdf/credential/transcript updates + vectors) — the committed LICHEN code depends on part of it; a 0.1.2 publish is likely the clean fix (Mark's readiness call). Interim: vendored copy restored at 61540bccff-lineage; registry switch (oscore = \"0.1.1\" + patch removal) is prepared but blocked on the 0.1.2 publish. Verify with: cargo check -p lichen-node -p lichen-gateway --lib (union), then full test.",
  "status": "closed",
  "priority": 1,
  "issue_type": "bug",
  "assignee": "opencode-worker-2",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-30T22:28:30.053367Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-08-30T22:43:10.363412019Z",
  "started_at": "2026-08-30T22:40:20.039462072Z",
  "closed_at": "2026-08-30T22:43:10.363412019Z",
  "close_reason": "Not reproducible on beads-worker-2 HEAD after the vendored oscore restore; resolved as env-resolved, no in-repo change needed. Evidence on heft: cargo check -p lichen-node -p lichen-gateway --lib clean (both E0599 begin_unprotect_observe_response at secure.rs:1361 and the E0282 payload.to_vec() cascade at :1387 gone - E0282 was downstream of the wrong-crate signature, not an independent defect); union lib tests lichen-gateway 220/220 and lichen-node 293/294 (single red = pre-existing scgh rfc6554 InvalidSourceRoute, filed separately, worker-7 area); clippy union clean on re-run. Root cause confirmed as cutover crate-state, not feature unification: /home/mark/crates/oscore is now COMMITTED and clean at 76cee08 (chore: update lock for 0.1.2) with the observe API present (11 matches) - the transient gap was the uncommitted-working-tree window, not registry-vs-patch resolution. Remaining durable step is Mark's: publish oscore 0.1.2 to crates.io before any registry switch (patch removal) - unchanged from the bead's own note, tracked via qhh6 context. No in-repo diff this round, so the 3x codereview passes are vacuous (nothing to review); the verification evidence above is the close basis."
}
