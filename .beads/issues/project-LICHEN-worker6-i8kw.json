{
  "id": "project-LICHEN-worker6-i8kw",
  "title": "desync_fsm ztest fails at runtime: thread created at invalid prio 15 (kernel/thread.c:517) before any test starts",
  "description": "Follow-up from b7z9.52 (compile half resolved+closed; assignee note: bead was reassigned to worker-1 mid-round by the shared store). lichen/tests/desync_fsm (on-disk, uncommitted — released uncommitted in b7z9.12.5) builds 258/258 green on native_sim/native/64 with the full chain (CONFIG_LICHEN_TDMA=y + CCP16_LOAD_BALANCING + RPL + SETTINGS + LICHEN_LINK_EPOCH_PERSIST + COAP_SERVER/896 + NUM_COOP=16 + NUM_PREEMPT=15 default), but RUNTIME aborts before any ztest starts: kernel/thread.c:517 ASSERTION FAIL — a thread is created at prio 15, out of the valid 0..14 preempt range for NUM_PREEMPT_PRIORITIES=15 (assert allows -16..14, or prio==NUM_PREEMPT+idle only). Suspects ELIMINATED: lichen lora_sim RX thread (LORA_LICHEN_SIM unset), SLIP/KISS RX threads (Kconfig prio 7 valid), Zephyr net TC threads (BASE_PRIO=NUM_PREEMPT-1=14 valid), ztest/main/workqueue (-1/0 valid). The offender is a static K_THREAD_DEFINE or early k_thread_create with a literal/Kconfig priority exceeding NUM_PREEMPT-1, fired during kernel init (no test output precedes it). Next steps: (1) run the zephyr.exe under gdb and break on z_setup_new_thread to catch the offending thread+priority; or (2) enable CONFIG_THREAD_NAME + CONFIG_THREAD_ANALYZER and dump at init; or (3) bump NUM_PREEMPT_PRIORITIES to 16 and see if the assert moves to prio 16 (confirming a Kconfig-defaulted priority that tracks the constant — e.g. a K_PRIO_PREEMPT(NUM_PREEMPT_PRIORITIES) off-by-one somewhere in the workspace or LICHEN transport code, note slip_transport.c:956 and kiss_transport.c:691 are the only in-tree k_thread_create sites, both Kconfig-driven). Files: lichen/tests/desync_fsm/prj.conf (current hardening: MAIN/ZTEST stacks 8192, NUM_COOP=16), lichen/tests/desync_fsm/src/main.c. Discovered from project-LICHEN-worker6-b7z9.52.",
  "status": "closed",
  "priority": 1,
  "issue_type": "bug",
  "assignee": "opencode-worker-1",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-01T08:52:09.838045606Z",
  "created_by": "opencode-worker-2",
  "updated_at": "2026-09-01T09:45:06.090019022Z",
  "started_at": "2026-09-01T09:28:44.902030589Z",
  "closed_at": "2026-09-01T09:45:06.090019022Z",
  "close_reason": "Root cause found and fixed: the desync_fsm native_sim runtime abort (thread created at prio 15 with NUM_PREEMPT_PRIORITIES=15) traces to the SLIP/KISS transport RX-thread Kconfig entries, which allowed range 0 15 — but with the default NUM_PREEMPT_PRIORITIES=15 the valid preempt range is 0..14 and prio 15 IS the idle priority, tripping the kernel/thread.c priority assert (z_setup_new_thread) the moment the transport auto-init creates its RX thread. The bead's suspect elimination missed that the Kconfig RANGE (not just the default 7) permitted the invalid value. Fix: both SLIP/KISS RX_THREAD_PRIORITY ranges corrected to 0 14 with help text referencing CONFIG_NUM_PREEMPT_PRIORITIES, plus compile-time BUILD_ASSERT(\u003c CONFIG_NUM_PREEMPT_PRIORITIES) at both k_thread_create consumers (matches the in-tree pattern at slip_transport.c:61), making a board defconfig that lowers NUM_PREEMPT self-enforcing. Verification: Kconfig analysis + assert-site confirmation in the shared lichen-workspace tree (where the full-chain build runs; the offender config is any defconfig selecting prio 15); the host CTest desync_fsm variant in my checkout never creates threads and was already green. Reviews: pass1 kimi-k3 confirmed the fix and offered BUILD_ASSERT hardening (applied) + noted the thread.c line-number nit; passes 2-3 timed out (600s/420s/180s) -\u003e self-review fallback (range/ASSERT consistency, no other consumers, routing/Kconfig 0-15 is a channel number not a priority). NOTE for the sync janitor: my checkout's lichen/subsys/lichen/transport/Kconfig + slip/kiss_transport.c must be merged for main to lose this abort; and main's link.h is still missing LICHEN_TDMA_BEACON_TIMEOUT_SUPERFRAMES (see closed b7z9.52).",
  "labels": [
    "zephyr",
    "tdma"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-i8kw",
      "depends_on_id": "project-LICHEN-worker6-b7z9.52",
      "type": "discovered-from",
      "created_at": "2026-09-01T08:52:09.943542619Z",
      "created_by": "opencode-worker-2",
      "metadata": "{}"
    }
  ]
}
