{
  "id": "project-LICHEN-worker6-lagn",
  "title": "b7z9.16.1(a): DAO TX scheduler state on RplStack (join hook + phase/deadline tracking)",
  "description": "First slice of b7z9.16.1 (spec 09 14.2 R-09-017..019): add a DAO TX scheduler to RplStack (lichen-node/src/rpl_stack/mod.rs) using dao_timing.rs exports as the single oracle — DaoInitialDelayMin/MaxMs for the 0-2s initial delay after DODAG join, DaoRetryBackoff for the 4/8/16s retry ladder, DaoRefreshTimer for the 900s refresh. State: phase enum (Idle/Initial{deadline_ms}/Retry{attempt, deadline_ms}/Refresh{deadline_ms}) + last sent marker; a join hook (leaf role transition -\u003e schedule Initial with a random 0-2s offset via now_ms) and advance(now_ms). NO TX-path behavior change in this slice (send_dao untouched) — pure state addition, independently landable; (b) wires send_dao to it. Unit tests with a controllable now_ms: join schedules Initial in [0, 2000]ms; advance past deadline -\u003e due; retry ladder advances 4000/8000/16000; refresh at 900s.",
  "status": "closed",
  "priority": 2,
  "issue_type": "task",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-01T03:48:00.147192259Z",
  "created_by": "opencode-worker-6",
  "updated_at": "2026-09-01T04:22:08.639950812Z",
  "closed_at": "2026-09-01T04:22:08.639950812Z",
  "close_reason": "Landed slice (a) of b7z9.16.1: DaoTxScheduler in lichen-node/src/rpl_stack/dao_tx_sched.rs — phase enum (Idle/Initial/Retry/Refresh/Exhausted) driven entirely by the lichen_rpl::dao_timing oracle (dao_initial_delay_ms, dao_retry_delay_ms 4/8/16s ladder 0-indexed, dao_retry_exhausted, DAO_REFRESH_INTERVAL = half the 30-min soft-state lifetime); RplStack field + dao_tx_advance(now_ms) with join-transition detection (idle until router.is_joined(), then schedules the initial DAO 0-2s out using a now_ms/IID-derived word through the oracle's acceptance window; re-idles on disjoin). NO TX-path behavior change (send_dao untouched) — slice (b) wires send_dao to advance(). Tests: 4 scheduler unit tests (idle-until-join, initial window [0,2000]ms via the oracle's acceptance mapping, refresh at half-lifetime 450s, retry ladder 4/8/16s with exact boundaries and exhaustion) + an RplStack-level integration test (idle before join, scheduled after the DIO join via the rpl_dispatch join_leaf fixture). Verified: lichen-node 333+5 green, clippy --all-features -D warnings clean, fmt clean. clippy's dead-code expectations for the not-yet-consumed API were pruned after rustc's reachability analysis made them unfulfilled (pub(crate) reachability); the (b) slice consumes the API naturally. Slice (b) bead: mieh.",
  "labels": [
    "rust",
    "rpl"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-lagn",
      "depends_on_id": "project-LICHEN-worker6-b7z9.16.1",
      "type": "discovered-from",
      "created_at": "2026-09-01T03:48:00.245928998Z",
      "created_by": "opencode-worker-6",
      "metadata": "{}"
    }
  ]
}
