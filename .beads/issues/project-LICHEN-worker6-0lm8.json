{
  "id": "project-LICHEN-worker6-0lm8",
  "title": "L2 TX: replace blocking lora_send() with lora_send_async() + TX_DONE completion",
  "description": "Child of parent project-LICHEN-worker6-9hfe (async LoRa API refactor). Driver async TX ALREADY EXISTS (lr1110.c:484 lr1110_lora_send_async, lora_sim.c:426, lora_renode.c:272; Zephyr lora.h:244 lora_send_async) — do NOT touch drivers.\n\nScope (files): lichen/subsys/lichen/l2/lora_l2_tx.c, TX-side of lichen/subsys/lichen/l2/lora_l2.c, lichen/subsys/lichen/l2/lora_l2_internal.h.\n\nWork:\n1. Replace blocking lora_send(lora_data.lora_dev, tx_buf, pop_len) at lora_l2_tx.c:352 with lora_send_async(); register a TX_DONE callback that transitions L2 state TXING-\u003eIDLE/RX and signals completion.\n2. Completion semantics: current public TX path is synchronous (callers expect send completed or failed on return). Provide completion via k_sem taken by the synchronous wrapper OR an async completion callback API — pick one, document in lora_l2.h, and keep the existing public function signature working (wrapper waits on completion). No k_sleep-based waits; use K_FOREVER on the completion semaphore only in thread context (assert !k_is_in_isr()).\n3. Error paths: TX_DONE with error status (driver callback args) must propagate errno like the blocking lora_send() return did; zeroize tx_buf after TX (lora_l2_tx.c:365 comment preserves this).\n4. Concurrency: lora_l2.c:293-298 documents mutex ordering vs lora_send(); rework comments/locking for the async split (state snapshot under lock, actual radio op async).\n5. Remove retry k_sleep() delays noted in parent description; retries (if any live in this path) become state-machine transitions, not sleeps.\n\nAcceptance: rg -n 'lora_send\\(' lichen/subsys/lichen/l2/ returns no blocking-send hits; ping_l2 and l2_dispatch tests pass; no k_sleep in tx path. Depends on A (project-LICHEN-worker6-sb0b) for shared lora_l2.c lifecycle/state — avoid merge collisions by sequencing.",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-29T03:52:14.678718Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-08-31T01:18:30.699595154Z",
  "closed_at": "2026-08-31T01:18:30.699595154Z",
  "close_reason": "Fixed: L2 TX now uses the 4.1.0 async API. lora_l2_tx.c arms lora_send_async(dev, tx_buf, len, \u0026tx_done_signal) (static k_poll_signal, reset immediately before each arm) and waits via k_poll(K_FOREVER, thread context asserted with __ASSERT_NO_MSG(!k_is_in_isr())); the poll-signal result becomes the return status fed into CSMA tx_complete and duty-cycle recording exactly as the old blocking lora_send() result was; public signature and synchronous contract preserved; tx_buf zeroing still after completion. Driver prerequisite fixed too: the four send_async implementations were sync-delegating stubs that IGNORED the poll signal (a wait would have hung forever) - lora_sim.c, lora_renode.c, lr1110.c now k_poll_signal_raise(async, ret) with the underlying send's result, and lora_loopback.c gained a previously-missing send_async (test driver for ping_l2). VERIFIED on heft: ping_l2 builds and runs identically to the pre-port baseline (4/7 pass; the 3 failures are 5fva children's frame-build-layer issues that never reach the radio TX - unchanged symptom -126 at frame build). Review: pass 1+2 PASS (reset-before-arm ordering prevents stale signals; no deadlock - driver completion never takes L2 mutexes; CSMA/duty-cycle get the final status; zeroing after completion; 4.1.0 poll semantics correct), pass 3 PASS (tx_buf_mutex spans reset+arm+wait so no cross-caller signal pairing; stop() race pre-existing; no stale raise) with the K_FOREVER wedge-shape note deferring to the existing cdoj tracking (bead-prescribed K_FOREVER retained).",
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-0lm8",
      "depends_on_id": "project-LICHEN-worker6-sb0b",
      "type": "blocks",
      "created_at": "2026-08-29T03:52:26.353861Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    },
    {
      "issue_id": "project-LICHEN-worker6-0lm8",
      "depends_on_id": "project-LICHEN-worker6-9hfe",
      "type": "discovered-from",
      "created_at": "2026-08-29T03:52:38.297075Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
