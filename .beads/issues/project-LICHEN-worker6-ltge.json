{
  "id": "project-LICHEN-worker6-ltge",
  "title": "l1qw.32(a): Python 4.01 oscore_required body + /config/privacy(/allowed) resources",
  "description": "Smallest child of worker6-l1qw.32 (spec 18.2.4, R-12-026..028), Python side: (1) unauthenticated queries to non-public nodes must receive 4.01 Unauthorized with CBOR body {error: 'oscore_required', mode: ...} (mirror the deaddrop.py:593 oscore_required body pattern) wherever position_privacy currently rejects without a body; (2) add /config/privacy (GET current mode) and /config/privacy/allowed (PUT {peers: [...]} whitelist) resources backed by position_privacy.py's allowed-peer state (today a single constructor param allowed_peer_iid, position_privacy.py:44-50 — generalize to a list). Tests in python/tests/coap. NO group-mode encrypted beacons (child c) and no C changes (child b).",
  "status": "closed",
  "priority": 2,
  "issue_type": "task",
  "assignee": "opencode-worker-7",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-31T21:02:52.196848003Z",
  "created_by": "opencode-worker-7",
  "updated_at": "2026-08-31T21:20:43.219818157Z",
  "started_at": "2026-08-31T21:03:05.103971947Z",
  "closed_at": "2026-08-31T21:20:43.219818157Z",
  "close_reason": "Python privacy-config slice landed (spec 18.2.4): (1) position_privacy.py whitelist generalized from single allowed_peer_iid to an _allowed_peers set with set_allowed_peers(); the legacy param seeds the set (vectors test unchanged and passing); check_read consults the set via _peer_allowed — group/private gating semantics unchanged. (2) New resources/privacy_config.py: GET /config/privacy returns {mode, allowed_peer_count} (count only — SECURITY per review: the peer-IID list is the private-mode trust root and is not disclosed unauthenticated); GET/PUT /config/privacy/allowed gated behind allow_writes=False default mirroring the ConfigResource precedent (PUT/full-list GET return 4.01 unless enabled) after the security pass flagged unauthenticated whitelist mutation as a P1 defeat of private mode; PUT validates {peers:[tstr]} via _decode_single_cbor bounds, malformed -\u003e 4.00, echo on 2.04. (3) build_site gained privacy_policy/privacy_allow_writes (keyword-only, all existing call sites unaffected). Dead code per review: the interim oscore_required_body helper removed — the 4.01 body belongs with the position-read consumer (spec 18.2.4 body shape recorded in bead text + children). Tests: 18/18 across test_privacy_config.py + test_position_privacy_vectors.py; ruff clean. Reviews: 4 Kimi K3 passes (correctness/security/edge/final re-review) — initial security pass P1 (unauthenticated whitelist PUT defeats private mode) and P3 (whitelist disclosure) fixed via the allow_writes gate + count-only disclosure; P4s (docstring, dead helper) fixed; no remaining findings. Out-of-scope filed: worker6-4dmd family (per-originator keying) unrelated; the C mirror is worker6-ulum.",
  "labels": [
    "python",
    "lci"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-ltge",
      "depends_on_id": "project-LICHEN-worker6-l1qw.32",
      "type": "discovered-from",
      "created_at": "2026-08-31T21:02:52.29482155Z",
      "created_by": "opencode-worker-7",
      "metadata": "{}"
    }
  ]
}
