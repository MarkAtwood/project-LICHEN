{
  "id": "project-LICHEN-worker6-104v",
  "title": "Fix pre-existing RoutingError import break in python/tests/test_dtn_option.py",
  "description": "Found while landing b7z9.8.3 (Rust DTN option parity): python/tests/test_dtn_option.py:12 imports RoutingError from lichen.routing.router, but that name is no longer exported there (router.py:28 imports it as RplRoutingError from lichen.rpl.routing; router.py raises its own local RoutingError-ish errors - verify what the DTN option tests actually need: the import may simply be unused, or should come from lichen.rpl.routing). The test module has been failing COLLECTION since before 2026-09-04 (it was in the pre-existing 6-error httpx-era set), so the Python DTN-option vector tests (consuming test/vectors/dtn_sflag_hbh.json, the same corpus as the Rust dtn_option_vectors.rs and the C routing_dispatch tests) are not running. Fix the import (drop if unused), run the module (should pass ~10 cases), and record the three-way parity: C routing_dispatch (green), Rust dtn_option_vectors 8/8 (green, commit pending this round), Python test_dtn_option (blocked only by this import).",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "assignee": "opencode-worker-1",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-04T10:21:42.41986122Z",
  "created_by": "opencode-worker-1",
  "updated_at": "2026-09-04T10:26:55.955258923Z",
  "started_at": "2026-09-04T10:23:03.746813846Z",
  "closed_at": "2026-09-04T10:26:55.955258923Z",
  "close_reason": "RESOLVED (the break had migrated one level since the bead was filed): the collection failure was NOT the test file's import (a later quality commit b957311b25 had already cleaned it) but lichen/routing/__init__.py:13 re-exporting RoutingError from lichen.routing.router, which no longer defined it - router.py imported the canonical class as RplRoutingError while raising/catching the bare RoutingError name (F821) and the package __init__ demanded the re-export. Fix (commit dfb326b7f8): router.py imports the canonical class as RoutingError and uses it at both raise sites (:197, :788) and the catch (:485, collapsing the redundant two-class tuple); the __init__ re-export now resolves. VERIFIED: python/tests/test_dtn_option.py 13/13 green (the Python leg of the DTN S-flag/HBH three-way parity now runs and consumes the shared corpus dtn_sflag_hbh.json, completing b7z9.8.3's parity matrix: C routing_dispatch green, Rust dtn_option_vectors 8/8, Python 13/13). Routing/dtn/router test subset: 277 passed; the one failure (test_ipv6_routing_relay_vectors::test_committed_icmpv6_vectors_match_stdlib_generator) is PRE-EXISTING (fails on a stashed clean tree, verified) - not this fix's scope; ruff clean after import-sort autofix.",
  "labels": [
    "python",
    "phy"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-104v",
      "depends_on_id": "project-LICHEN-worker6-b7z9.8.3",
      "type": "discovered-from",
      "created_at": "2026-09-04T10:21:42.533984921Z",
      "created_by": "opencode-worker-1",
      "metadata": "{}"
    }
  ]
}
