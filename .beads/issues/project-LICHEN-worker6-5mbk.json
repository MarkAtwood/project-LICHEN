{
  "id": "project-LICHEN-worker6-5mbk",
  "title": "Rust encode_rule255 reports profile-ceiling overflow as retryable-looking BufferTooSmall",
  "description": "Confirmed empirically (discovered-from project-LICHEN-worker6-xuc8, robustness pass). encode_rule255 (rust/lichen-schc/src/codec.rs:1415-1422) checks needed \u003e single_frame_limit.min(SCHC_FRAG_MAX_PACKET_SIZE) before consulting out.len() and returns BufferTooSmall::new(needed, profile_limit). Probe: raw 22554-byte nonzero-TC packet -\u003e BufferTooSmall{required:22555, provided:22554} regardless of actual out capacity, so a grow-and-retry caller loop never terminates. Python raises unambiguous SchcError 'Rule 255 raw IPv6 packet exceeds 22553 bytes' (python/src/lichen/schc/headers.py:146-147). Both fatal; only the error classification differs. Fix: return a terminal profile-limit SchcError::InvalidPacket (or dedicated variant) when needed exceeds the profile ceiling, reserving BufferTooSmall for genuinely too-small caller buffers.",
  "status": "closed",
  "priority": 3,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-30T16:37:08.79472Z",
  "created_by": "opencode-worker-5",
  "updated_at": "2026-09-09T13:34:07.287446045Z",
  "started_at": "2026-09-09T13:16:21.836056185Z",
  "closed_at": "2026-09-09T13:34:07.287446045Z",
  "close_reason": "Fixed in encode_rule255 (rust/lichen-schc/src/codec.rs): out-capacity check reordered first (retryable BufferTooSmall), profile/single-frame ceiling now terminal SchcError::InvalidPacket(\"Rule 255 raw IPv6 packet exceeds profile single-frame limit\") matching the compress-path precedent and the Python reference's terminal rejection; removed the subsumed packet.len() \u003e SCHC_FRAG_MAX_PACKET_SIZE pre-check (also retryable-looking, fully covered by the ceiling check). rule255_profile_size_boundary updated per the bead-prescribed classification change: over-ceiling probe with sufficient buffer expects terminal error, short-buffer probe still expects retryable capacity error, plus a new single-frame-limit terminal probe. Reviewer delegation timed out (rc=124) mid-pass; three self-passes per protocol fallback: correctness (branch exhaustiveness, subsumption proof, boundary math), security (no caller depends on old classification, write still capacity-guarded), edge-cases (sfl=0 terminal, out.len()==needed succeeds, saturating_add safe) - one finding filed as follow-up bead project-LICHEN-worker6-9sns (decode_rule255 twin). cargo test lichen-schc 92+all vector suites green; lichen-client/core/gateway green; 18 lichen-node RPL failures pre-existing (proven via git stash rerun; tracked by 8rm3/ehcn deliberately-red set). clippy -D warnings clean, codec.rs rustfmt clean. Committed 1bdf6e9c15.",
  "labels": [
    "schc"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-5mbk",
      "depends_on_id": "project-LICHEN-worker6-xuc8",
      "type": "discovered-from",
      "created_at": "2026-08-30T16:37:09.22579Z",
      "created_by": "opencode-worker-5",
      "metadata": "{}"
    }
  ]
}
