{
  "id": "project-LICHEN-worker6-jg6w",
  "title": "[bug] lichen-rpl cargo test broken on main: dao_origin_vectors.rs includes nonexistent dao_origin_signature_upstream.json",
  "description": "Pre-existing on main (introduced by 46ea60fcaf 'chore(beads): worker8 sync'): rust/lichen-rpl/tests/dao_origin_vectors.rs:28 include_str!(\"dao_origin_signature_upstream.json\") but no file by that name exists anywhere in-tree (only test/vectors/dao_origin_signature.json). Breaks 'cargo test -p lichen-rpl' compilation entirely (all test targets fail to build). Discovered by beads-worker6 while verifying the rpl_messages.json corpus regen (project-LICHEN-worker6-1p2r.3) via cargo test -p lichen-rpl. Likely fix options: (a) the file was meant to be committed with worker8's dao work — locate/restore it; (b) rename the include to the existing test/vectors/dao_origin_signature.json; (c) if upstream-regen dao corpus is pending lptm (same as project-LICHEN-worker6-1p2r dao child), gate the test. Needs the dao-domain context of worker8's change. NOTE: cargo test on this host needs CARGO_TARGET_DIR=build-worker6-target (rust/.cargo/config.toml points at the Mac /Volumes/Attic path).",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T12:00:49.497050847Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-09T12:30:03.109449799Z",
  "started_at": "2026-09-09T12:07:20.618443283Z",
  "closed_at": "2026-09-09T12:30:03.109449799Z",
  "close_reason": "Root cause corrected: main was NOT broken — main (and origin/main, beads-worker-8) carry dao_origin_signature_upstream.json (added by 85a70ec445); the include_str! break reproduces on worker branches that lost the file in merges (confirmed: beads-worker-5 and beads-worker-6 both lacked it). Fix on beads-worker-5: (1) 5c875d7693 merge of main restoring rust/lichen-rpl/tests/dao_origin_signature_upstream.json + rust/lichen-schc/tests/authenticated_schc_dio_upstream.json + docs/ca-lichen-tech-design.md — the only 3 non-.beads files main had that the branch lacked; all 7 real merge conflicts resolved to main's reviewed side. (2) 87f166a5e4 restores main's exact versions for 7 files auto-checkpointed from a crashed round as debris commit 17e39dbb31 (unreviewed edits incl. a 51-line binding-test deletion in lichen-link/src/trust.rs); debris fully recoverable at 17e39dbb31 for triage. Verification: git diff main HEAD excluding .beads and rust/crates/oscore = EMPTY (branch == main); cargo test -p lichen-rpl -p lichen-schc -p lichen-link: 0 failed across all targets — dao_origin_vectors now compiles and passes (2/2) against the restored upstream corpus (also demonstrates the Rust DAO validator correctly accepts upstream AddrForKey sources, unlike the design-blocked Python side tracked under 1p2r.5/lptm). 3x reviewer-model passes: correctness PASS, security PASS, edge-cases FINDINGS -\u003e filed project-LICHEN-worker6-ox2o (P3: .beads merge-resolution friction, policy call for Mark). Notes: (a) beads-worker-6 still lacks the corpus file — its next main merge fixes it, evidence in this thread; (b) cargo on this Linux host requires CARGO_TARGET_DIR override (rust/.cargo/config.toml targets the Mac /Volumes/Attic path); (c) transient index.lock contention with a concurrent git process on the shared repo — resolved itself, no action.",
  "labels": [
    "rust",
    "networking"
  ]
}
