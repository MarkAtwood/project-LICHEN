{
  "id": "project-LICHEN-worker6-b7z9.29.3",
  "title": "b7z9.29.3: Python+Rust: implement BusyPercent TX-time airtime sampler",
  "description": "Spec R-02a-131: BusyPercent MUST be TX-time based occupancy, MUST NOT be RSSI-derived. No sampler exists in any language. Implement: accumulate own TX airtime per superframe, compute BusyPercent = (tx_airtime / slot_duration) * 100 over RF_METRICS_WINDOW_SF rolling window. Feed into interference_score_tenths() which already exists in Rust (rf_health.rs:100-107). Add unit tests.",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "assignee": "opencode-worker-7",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-01T01:48:52.683904881Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-01T03:36:06.214619757Z",
  "started_at": "2026-09-01T03:14:58.638389913Z",
  "closed_at": "2026-09-01T03:36:06.214619757Z",
  "close_reason": "BusyPercent TX-time sampler landed (spec R-02a-131 / 2a.10.3, MUST: TX-time occupancy, never RSSI-derived): (1) Rust BusyPercentSampler in rust/lichen-core/src/rf_health.rs — heapless::Vec\u003c(u64 superframe, u32 airtime_ms), 32\u003e rolling window; record_tx_airtime folds same-superframe repeats and evicts the oldest-by-superframe entry when full; busy_percent prunes entries outside the EXCLUSIVE window (sf + 32 \u003e current, underflow-safe), sums TX airtime, computes total*100/(slot_duration_ms * RF_METRICS_WINDOW_SF) clamped 0..100, zero-duration -\u003e 0; never RSSI-derived. (2) Python BusyPercentSampler in ccp.py with identical semantics; TDMA_SLOT_MS=2346 added to python constants.py (was Rust-only). (3) Tests: Rust busy_percent_rolling_window_semantics (2 slots in one SF -\u003e 6-7%, window slide -\u003e 0, exclusive boundary at current-32 dropped, full window -\u003e 100, over-budget clamp, zero-duration); Python mirrored (test_partial_and_window_slide, test_saturation_and_zero_duration); interference_score_tenths (already existing) is the consumer. Verification: cargo 365/365 lib green incl. all pre-existing rf_health tests; clippy --all-features -D warnings clean; fmt clean; Python 101/101 test_ccp green; ruff clean. Codereview rounds (Kimi K3): pass 3 (edge) found 5 findings — all fixed (map-count mask already OK in committed version; 2-byte uint out of scope for this module; clear-band/3.xx and s_backoff race were coap_client beads — N/A here; the real ones were the capacity-vs-window off-by-one and the exclusive-boundary underflow) — the off-by-one (capacity 32 vs inclusive 33-window parity divergence between Rust heapless eviction and Python dict pruning) and the underflow-at-boot (saturating_sub + \u003e= excluded sf 0) both fixed with the underflow-safe exclusive formulation, re-verified; pass 2 (narrow) PASS on formula/zero-guard/parity/capacity; the boot-case fix verified in both test suites. 365/365 + 101/101 green at commit.",
  "labels": [
    "ccp15",
    "spec-gap"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-b7z9.29.3",
      "depends_on_id": "project-LICHEN-worker6-b7z9.29",
      "type": "parent-child",
      "created_at": "2026-09-01T01:48:52.826527098Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
