{
  "id": "project-LICHEN-worker6-mfab",
  "title": "sos_origin: upstream AddrForKey pinned-vector oracle test skipped in no-alloc builds",
  "description": "Security-pass finding on 1k6n fix (commit 26f158b4c9). origin_addr_matches_upstream_pinned_vector (sos_origin.rs:321) is cfg(feature=\"alloc\")-gated, but the function it tests, origin_addr_from_pubkey (:96), is ungated — the test is gated only because its helper hex_to_bytes (:244) is alloc-gated. domain_matches_vector demonstrates the pattern: std::vec::Vec resolves under cfg(test) without the alloc feature (lib.rs:127 cfg(any(test, std)) extern crate std). Fix: rewrite hex_to_bytes with std::vec::Vec (ungated), drop the alloc gate from the vector test so the pinned upstream yggdrasil-go AddrForKey byte-equality oracle (project-designated independent conformance oracle) runs in no-alloc test configurations too. Default-feature runs currently silently skip the only in-file upstream-address check.",
  "status": "closed",
  "priority": 3,
  "issue_type": "task",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-10T03:57:49.847283312Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-10T04:06:34.015163622Z",
  "started_at": "2026-09-10T03:58:05.928162176Z",
  "closed_at": "2026-09-10T04:06:34.015163622Z",
  "close_reason": "Ungated hex_to_bytes (std::vec::Vec, resolving under cfg(test) via lib.rs cfg(any(test,std))) and the origin_addr_matches_upstream_pinned_vector oracle test; tracker tests keep their alloc gates. The pinned upstream yggdrasil-go AddrForKey byte-equality oracle now runs in no-alloc builds (default leg 94-\u003e95 tests). Verified: default 95 / alloc 99 / all-features 171 pass; clippy -p lichen-link --all-targets -D warnings clean; fmt clean. Codereview: reviewer (kimi-k3) timed out on all 3 passes for this diff (service instability this session — 3/4 earlier passes also hit 240s+ timeouts); passes done by self per loop protocol: correctness PASS (gating subset proof + all-leg runs), security PASS (test-only diff), edge-cases PASS (no combo leaves gated items ungated; no dead imports).",
  "labels": [
    "rust"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-mfab",
      "depends_on_id": "project-LICHEN-worker6-1k6n",
      "type": "discovered-from",
      "created_at": "2026-09-10T03:57:49.976987202Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
