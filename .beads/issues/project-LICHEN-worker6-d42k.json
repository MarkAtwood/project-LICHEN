{
  "id": "project-LICHEN-worker6-d42k",
  "title": "sync script: LLM session can git commit the merge itself; no HEAD pin — main advances ungated while caller reports manual-resolution",
  "description": "Reviewer kimi-k3 pass 2.1. Session runs with shell/git access mid-merge up to 900s (sync-beads-workers.sh:110); pre-commit hook only blocks .beads/rust/crates/oscore. If the session runs 'git commit' itself: staged_now empties → missing gate returns 1 → caller's 'git merge --abort' (:316) silently no-ops (no merge in flight) → branch reported as manual-resolution while main ALREADY advanced with an ungated merge commit, and rerere records it. Fix: snapshot 'git rev-parse HEAD' before :110; if HEAD moved after the session, reset --hard to it before any gate.",
  "status": "closed",
  "priority": 1,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-09T01:17:22.339619302Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-09-09T02:02:25.306282224Z",
  "started_at": "2026-09-09T01:59:41.567025608Z",
  "closed_at": "2026-09-09T02:02:25.306282224Z",
  "close_reason": "Fixed: git-state pin in llm_semantic_merge() snapshots symbolic-ref, HEAD, MERGE_HEAD hash, and the staged-name set (conflicted paths excluded, --no-renames) before the timeout-900 LLM session; any mutation post-session triggers a status-checked symbolic-ref repoint + reset --hard (FATAL exit 2 on any rewind failure — set -e is suppressed in the if-condition call context; DETACHED-start fail-stops). Verified with a 12-scenario sandbox suite (rogue commit, branch switch, self-abort, merge-parent swap, foreign staging, stale index.lock, detached start, .beads discard policy, accept-ours/accept-theirs no-false-positive, happy/fail paths) — all pass; bash -n + shellcheck clean. 4 delegated reviewer rounds (kimi-k3): rounds 1-3 found branch-switch bypass, unchecked reset, MERGE_HEAD content swap, foreign staging, accept-ours false positive — all fixed; round 4 converged (one finding deduped into x79p via comment). Follow-ups filed: nm0h (residual attack surface: refs/hooks/push/untracked), ka0l (pre-existing dead staging block). One security pass (round 2) timed out at 300s and was rerun successfully at 600s.",
  "labels": [
    "fleet-implementable"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-d42k",
      "depends_on_id": "project-LICHEN-worker6-1dyx",
      "type": "discovered-from",
      "created_at": "2026-09-09T01:17:22.470614827Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
