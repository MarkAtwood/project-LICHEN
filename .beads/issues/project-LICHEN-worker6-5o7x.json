{
  "id": "project-LICHEN-worker6-5o7x",
  "title": "schc_parity gen_vectors.py treats expect_error as presence-only; a false value silently inverts rejection semantics",
  "description": "P3 hardening, found by worker5 subagent robustness pass of project-LICHEN-worker6-hahk. lichen/tests/schc_parity/gen_vectors.py:176 generates the C table with 'true' if 'expect_error' in vector - presence-based, not value-based. Empirically probed: a corpus with expect_error: false generates C expecting REJECTION (inverted); Python's consumer is also value-agnostic (test_vectors.py:722-731 - malformed_input ignores the value entirely, size_boundary checks presence not truthiness). The two consumers are consistent with each other so no live divergence today, but a future expect_error: false silently inverts meaning instead of failing closed. Everything else in the generator is fail-closed (verified by probe: unknown category, missing/extra fields, missing expect_error, duplicate JSON keys, bad/empty hex all exit 1 -\u003e CMake FATAL_ERROR; CMAKE_CONFIGURE_DEPENDS covers JSON + generator; C main.c default: result=1 treats unknown kinds as failure). Fix: require expect_error to be a nonempty string when present (sibling corpus ipv6_malformed.json uses string error enums) and reject otherwise, in both gen_vectors.py and the Python consumers.",
  "status": "open",
  "priority": 3,
  "issue_type": "task",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-30T21:40:48.509434543Z",
  "created_by": "opencode-worker-5",
  "updated_at": "2026-08-30T21:40:48.509434543Z",
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-5o7x",
      "depends_on_id": "project-LICHEN-worker6-hahk",
      "type": "discovered-from",
      "created_at": "2026-08-30T21:40:48.595690104Z",
      "created_by": "opencode-worker-5",
      "metadata": "{}"
    }
  ]
}
