{
  "id": "project-LICHEN-worker6-b7z9.37.3.1",
  "title": "b7z9.37.3(i): RootSeqCache module in lichen-rpl (replay + wrap fail-closed)",
  "description": "Parent: project-LICHEN-worker6-b7z9.37.3 (spec/06-security.md:668,687). Standalone rust/lichen-rpl/src/root_seq_cache.rs: RootSeqCache keyed by (dodag_id [u8;16], instance u8); accept(record) only when root_seq strictly greater than the cached value for that key (equal-or-lower = replay; any non-increase also enforces the MUST-NOT-WRAP rule since a post-wrap counter appears lower); missing key = first observation, accepted. Follow lichen-rpl crate conventions (see dao_timing.rs/table.rs for collection style; check no_std posture of siblings). Unit tests: replay of equal seq rejected, lower rejected, post-wrap lower rejected, per-(dodag_id,instance) isolation, first-observation accepted, distinct instances independent. No wiring — sibling (ii) integrates into RplStack; child .1 consumes at receiver validation.",
  "status": "closed",
  "priority": 2,
  "issue_type": "task",
  "assignee": "opencode-worker-5",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-31T18:34:36.167672536Z",
  "created_by": "opencode-worker-5",
  "updated_at": "2026-08-31T18:55:01.845129126Z",
  "started_at": "2026-08-31T18:34:37.472311724Z",
  "closed_at": "2026-08-31T18:55:01.845129126Z",
  "close_reason": "Implemented rust/lichen-rpl/src/root_seq_cache.rs (+lib.rs registration): RootSeqCache — fixed-capacity [Option\u003c([u8;16],u8,u64)\u003e;16] anti-replay table keyed (dodag_id, instance), no_std/no_alloc per crate conventions. accept() enforces strictly-increasing root_seq (equal=Replay, lower=Regression which also enforces spec L687 MUST-NOT-WRAP since a post-wrap counter appears lower, matching the Python oracle is_valid_dao_sequence/verify_root_dio_signature cached_seq semantics); first observations accepted while capacity remains; full table rejects NEW keys with RootSeqReject::Capacity fail-closed WITHOUT evicting live high-water marks (eviction would reopen replay windows); rejections leave the cache untouched. Caller contract documented at module level: accept MUST only be called after root-signature verification (security reviewer's P2: admitting attacker-claimed keys pre-verification would let an unauthenticated peer pin the table; wiring children .3(ii)/.37.1 own the verified call site). Evolution of this file during the round: initial std::collections::HashMap version broke the crate's no-default-features (#![no_std]) build (E0433, verified) and had unbounded memory — rewritten to the fixed table; match arms simplified (dead defensive arm now unreachable!-documented; derive(Default) instead of hand-rolled impl). Tests 7 (first-observation, replay, regression, post-wrap, (dodag,instance) isolation, rejection-leaves-untouched, capacity fail-closed + known-key still updatable). Gates: no-default-features build OK, 244 lib tests pass, clippy -D warnings clean, fmt clean. 3x review (kimi-k3): correctness re-review PASS after rewrite (oracle parity confirmed), security PASS with the P2 caller-contract point addressed via docs (enforcement lands with the wiring children), edge PASS (root_seq=0, u64::MAX pin, same-dodag-different-instance, no panic paths).",
  "labels": [
    "decomposed",
    "rust",
    "rpl",
    "python",
    "security",
    "spec-gap",
    "zephyr"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-b7z9.37.3.1",
      "depends_on_id": "project-LICHEN-worker6-b7z9.37.3",
      "type": "parent-child",
      "created_at": "2026-08-31T18:34:36.31594144Z",
      "created_by": "opencode-worker-5",
      "metadata": "{}"
    }
  ]
}
