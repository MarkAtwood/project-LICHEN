{
  "id": "project-LICHEN-worker6-l1qw.16.3",
  "title": "l1qw.16.3: GCP-6.3 silent discard — signature-failed slot claims are dropped, not 4.01'd",
  "description": "Child of l1qw.16 (GCP-6.3): rust/lichen-gateway/src/resources.rs handle_post_slots returns CoapResponse::unauthorized() (4.01, resources.rs:1963) when candidate_verifier.verify fails; spec GCP-6.3 (quoted in python/src/lichen/gateway/slot_claim.py docstring): claims with invalid or missing signatures MUST be SILENTLY DISCARDED. Change the signature-failure path to a silent drop (no response / empty success per the handler's contract — pick what the surrounding resource framework supports and document). Scope check: the OSCORE-missing 4.01s (resources.rs:1946,1949) are transport-layer auth and MAY stay — only the signature-verification failure must discard silently. Mirror in Python wherever a reject maps to an error response rather than discard. Tests: failed-signature POST yields no protocol error response.",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "assignee": "opencode-worker-4",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-08-31T20:36:38.708499919Z",
  "created_by": "opencode-worker-4",
  "updated_at": "2026-08-31T20:48:34.402444788Z",
  "started_at": "2026-08-31T20:37:31.705908596Z",
  "closed_at": "2026-08-31T20:48:34.402444788Z",
  "close_reason": "Landed as fix(gateway) commit on beads-worker-4. GCP-6.3 silent-discard implemented in rust/lichen-gateway handle_post_slots: SlotClaim::decode failure (was 4.00), into_raw failure incl. missing signature (was 4.00), and slot_verifier.verify failure (was 4.01) now return an indistinguishable empty 2.04 (new CoapResponse::empty_success) — the claim never existed as far as the peer can observe. The OSCORE-missing 4.01s (:1946,1949) intentionally remain — transport-layer auth is not GCP-6.3's scope (noted in bead description). Spec-fidelity note: spec 08 GCP-6.5 further distinguishes replay/expiry/invalid-slot (4.03) and unresolved conflict (4.09) from signature-class silent discard; the verifier conflates those causes today (pre-existing — everything was 4.01 before), so the full step-failure matrix is filed as follow-up bead cdfh (P2 gateway) with the exact mapping; my change strictly narrows the emitted signal and matches the GCP-6.3 MUST. Tests: new signature_failed_slot_claim_is_silently_discarded (garbage signature + missing signature both empty 2.04); two existing replay pins updated 0x81-\u003e0x44 per the new contract (commented as GCP-6.3); gateway lib 226/226, lichend bin 18/18; clippy -D warnings clean. Python side: verify_slot_claim already returns (bool, reason) tuples consumed for conflict resolution — no 4.01 mapping exists in the Python gateway to fix (discard semantics live at the caller, none currently wires responses). Codereview: delegated pass timed out (300s; was mid-verification of stale 4.00/4.01 pins + logging idiom) — self-completed per fallback: spec-text comparison (GCP-6.3 silent-discard MUST vs GCP-6.5 step codes), caller audit (lichend.rs:2015 test asserts 0x44 on success — unaffected), full-suite green.",
  "labels": [
    "gcp",
    "lci",
    "spec",
    "spec-gap"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-l1qw.16.3",
      "depends_on_id": "project-LICHEN-worker6-l1qw.16",
      "type": "parent-child",
      "created_at": "2026-08-31T20:36:38.870575843Z",
      "created_by": "opencode-worker-4",
      "metadata": "{}"
    }
  ]
}
